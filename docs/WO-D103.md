# WO-D103 — Frozen Work Order: hostile-act alarm and attack-speed response

**Status: frozen before dispatch.** Written 2026-07-29 against ruling D103 (`docs/IMPLEMENTATION_HISTORY.md`) and the tier-A predictions PR-19–PR-23 with their re-armed stop (`docs/PREDICTIONS.md`, commit `e269be3`, committed alone before this file existed). Written to match those predictions; predictions are never amended to match this file.

**Self-contained. Assume zero prior context.**

## Context

The valley fight currently terminates at step one: the coalition alarm fires when Reno's approach column crosses a 3,000 m radius at minute 587 (133 minutes before the historical alarm), ~900 warriors pre-assemble at the Bench, and Reno's mounted charge is annihilated in five minutes before his scheduled dismount — no skirmish line has ever formed. Three preserved measurements (see the D103 evidence column) established: the radius has no authority over this outcome anywhere in its sourced band; the response march ran at CAVALRY_WALK (83–86.5 minutes — turnout was never arrival); and with an attack-proximate alarm the full historical sequence — charge, halt, dismount, line, remount, retreat — runs end to end on existing machinery.

**The re-armed stop is live this round** (`docs/PREDICTIONS.md`): halt for adjudication if Reno A/G/M killed exceeds 40 in more than 5/50 registered seeds, or any seed reaches 100. Report Reno killed as data otherwise. Never tune in response to results, in either direction.

## Scope — one mechanism, two halves, one round

1. **T3 alarm** (`engine/src/camp-defense.ts`): the camp-defence **alert** (`campDefenseAlert`) forms only when an eligible spotted threat (existing eligibility: non-scout per D99, within `campDefenseRadiusMeters` of a camp — the radius keeps that role) **has committed to a river crossing toward the defended side**: the threat is or has been inside ford cells (`insideFord` / ford-cell occupancy — the reliable signal; do NOT use channel-side classification of positions near the polyline's southern terminus, a recorded artifact) moving camp-ward. **An outbound/retreat crossing must not alarm** — operationalize camp-ward as: at commitment the threat's believed position is on the far side or in the ford, and its approach closes on the defended camp; if the precise camp-ward test is underdeterminable from existing machinery (D98's side classification away from the terminus, movement direction), `TODO-AMBIGUOUS` and STOP per the thrice-proven precedent. Once a camp's alarm has fired, subsequent threat/commitment selection is unchanged (existing D91/D92/D93/D96/D98 machinery).
2. **Attack-speed response** (`camp-defense.ts` `activate()`): mounted responders get `CAVALRY_GALLOP` (existing speed class); dismounted `ON_FOOT` unchanged. No new number.
3. **Tests**: named tests for no-alarm-on-approach (a spotted threat inside the radius but never ford-committed raises no alert), alarm-on-camp-ward-ford-commitment, no-alarm-on-outbound-crossing, and gallop response speed.

## Out of scope — do not touch

Any `[CAL]` value including `campDefenseRadiusMeters` and `moraleSuppressionDrain`; any scenario byte (STOP; stream `ba288f09`, PR-17-class); D93/D96/D98/D99/D102 semantics beyond the alert gate and speed class; `state.ts:241`; frontage internals; `docs/PREDICTIONS.md`; prior codex reports.

## Predictions — PR-19 through PR-23 and the re-armed stop

Governing text at commit `e269be3`; binding; N=50, seeds 18760600–18760649; verdicts as they fall. PR-22 has both branches registered — a below-band median is reported as the registered low-branch finding, not a null. Registered observations (wing/F4 movement, ford-choke composition, frontage-activity first live readings) are reported as data, unscored.

## Proof required

Quartet verbatim (oracle refreshes are behavioral — document cause); before/after composite and envelope (median + mean, same stream); probes re-run from repo root (`h1-*`, `cohesion-asymmetry-probe.mjs` 18760625, `d98-crossing-test.mjs` 18760643 and 18760625 — zero camp-defence crossings must persist); PR-19–PR-23 verdicts with distributions (line-formation count; first-alert and turnout distributions; pool arrival medians; Reno killed distribution vs band with both branches; pre-675 alert count and Ford B alert timing); stop status; registered observations as data; `[CAL]` byte-identity; scenario hash unchanged.

Output: `codex-report-wo-d103.md` at repo root, house format, AMBIGUITIES and DEVIATIONS included. **Do not commit or push.**
