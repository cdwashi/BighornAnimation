# WO-D126 — the corpse-drift guards (DRAFT FOR ADJUDICATION 2026-08-05; number provisional until ruled; frozen on Chuck's word after Fable's attack)

*The queued fix from D121's ruling — "a future frozen WO against a registered
prediction, and it will be among the cheapest this register has specified" — drafted
first of the four-WO arc per the approved order (oldest, and its guards sit on the
seam the taxonomy says produces the next defect). Codex implements on a fresh exec;
CC verifies; fix-nothing discipline binds: any red outside the enumeration is a STOP,
reported not repaired. Conduct statement: the drafter's dry-run opened exactly the
three ruled sites, all inside previously dated-read scopes (`orders.ts:155-175` and
`movement.ts:292-301` within the movement-source read `90818a3`; `score.ts:38-73`
within the corpse-drift amendment `f6bfce9`, the scan region read at
`20c4a16`); no other body opened. THE LEDGER SEARCH RAN FIRST, per the arc's binding
constraint — this WO's own prior rows are D121 (the ruling), the corpse-drift
registration (`61db6f1`) and probes (`0f15e98`), D111's original observation, and
D110's refresh principles.*

## §1 — Ledger basis

D121: the defect is an UNGUARDED SEAM between mechanisms sound alone —
`deliverOrders` activates scheduled orders on dead recipients (`orders.ts:159-171`,
no endState check), `moveUnits` moves whatever has a path (`movement.ts:297`), and
`scoreCheckpoints` scans a unit's ENTIRE track with no destruction filter
(`score.ts:48-57`). Blast radius measured: ONE re-arm across fifty seeds (seed
18760647, co-m, deliveries t1508/t1510 after death t1497), ZERO realized
contamination (co-m carries no checkpoints; HOLDING_AT destruction-guarded
`:320-323`; event conditions pre-drift). The exposure is timetable-dependent —
change the schedule and it changes — which is why the guards are worth having at
zero current cost.

## §2 — Payload: three guards, nothing else

- **T1 — `deliverOrders`:** a delivery whose recipient has `endState` set does NOT
  activate — no order activation, no activation-side events on the corpse.
  Bookkeeping freedom (whether the skipped delivery still records as delivered)
  is Codex's, DECLARED in the report; the frozen predicate is no activation.
- **T2 — `moveUnits`:** a unit with `endState` set does not move — guarded at the
  loop, structural rather than incidental (today a corpse only moves if T1's seam
  re-arms it; the guard holds regardless of future write paths).
- **T3 — `scoreCheckpoints`:** the nearest-sample scan must not select a
  post-destruction sample. `TrackSample` carries only tick/x/y (`score.ts:7-11`),
  so destruction information must reach the scan; THREE routes enumerated, choice
  Codex's, DECLARED with its surface diff in the report: (a) widen `TrackSample`
  with end-state stamped at sample time (touches the track builder `index.ts:100`
  and any serialization surface — the save/replay bit-identity gate compares
  replayed against straight state, so a consistent field addition stays green,
  but the route's surface is the largest); (b) thread per-unit destruction ticks
  into the scan from the scorer's existing inputs (narrowest surface; signature
  change internal to score.ts and its callers); (c) truncate tracks at
  destruction in the builder (semantically strongest — a corpse has no track —
  but changes the `tracks()` surface every downstream consumer sees; NOT
  preferred without a survey of those consumers, which this WO does not order).

## §3 — Registered predictions (frozen; from D121's measurements, not expectation)

- **P1:** the N=50 envelope is IDENTICAL to the committed rows — composite and
  every component equal at full precision on every seed; the annihilation bout
  census matches the committed 120 ROW FOR ROW.
- **P2:** the current stream's single re-arm is REMOVED: on seed 18760647, co-m
  holds its death position from t1497 through day end — `activeOrderId` never
  changes post-death, the path stays empty, zero post-death movement (the C1
  probe's trace re-run shows stillness).
- **P3 (T3's real acceptance, stated per the adjudication):** NO committed figure
  moves through the checkpoint scan — C1 = 0.5000 on all fifty seeds, unchanged —
  because the exposure was measured at ZERO REALIZED (the one corpse has no
  checkpoints). A scoring-path change is not inert by assumption; it is inert
  here by measurement, and the campaign re-run checks it.
- **P4:** the only event-stream change anywhere is the REMOVAL of post-death
  activation/movement events on the affected (seed, unit) pair; no other seed's
  event stream differs.
- **P5:** no RNG draw is added or removed (the guards touch no draw site);
  trajectory identity everywhere except the corpse's own post-death track.
- **STOP-grade side-finding:** discovery of any SECOND re-arm contradicts the
  D121 census and is a finding — STOP, report, do not absorb.

## §4 — Pre-freeze red enumeration (the arc's binding constraint)

**Dry-run executed at drafting (T1+T2 verbatim, full suite, throwaway-patch
discipline, byte-identity verified on restore): `.claude/wo-d126-dryrun.out.txt`.**
Analytic basis registered before the run: every real-scenario suite oracle rides
seed 18760625 — the m4a combat-only behavioral oracle, the save/replay/scrub
bit-identity gate, the D110 pins, the m3a gates — and the census's one re-arm is
seed 18760647: expected ZERO reds. **RESULT: ONE red, PRE-EXISTING and
PAYLOAD-INDEPENDENT — and it is a FINDING, not a payload effect.**
`repository-text-integrity.test.ts` (the D110 index gate) fails with `spawnSync
git ENOBUFS`. **Diagnosis corrected at adjudication:** the `git ls-files` output
is 14.5 KB — nowhere near any buffer — so the overflow is the gate reading blob
CONTENTS through one subprocess: 10.9 MB of tracked text does not fit Node's
default `spawnSync` buffer. Single large artifacts are contributors; **the
ceiling is the AGGREGATE, and ordinary artifact growth will cross it again
regardless of any single file** — which is why the gate-repair WO's fix is
per-blob streaming against a bounded buffer, not a raised `maxBuffer` that
re-arms the same trap. Per D110's own Amendment 3 the
test fails loudly where git is unavailable rather than skipping silently —
which is the design working, on a failure mode nobody enumerated: **the blob
gate has been INOPERATIVE-WHEN-RUN at current repo scale, invisible because no
full-suite run has happened since the threshold crossed** (recent phases ran
scoped pin tests only). **The index it guards is VERIFIED CLEAN by direct scan
at drafting** (`git grep --cached`: zero CR bytes in any tracked text blob;
zero text-blob BOMs; four incidental byte matches inside binaries the gate's
text filter excludes). The remaining 119/119 tests are GREEN under T1+T2.
**Pre-classification, binding on the payload run: this red is
EXPECTED-PRE-EXISTING and is NOT this WO's to fix (fix-nothing discipline; ruled
at adjudication to the GATE-REPAIR WO — subjects + buffer, one gate family, one
two-clause acceptance). Any OTHER red is a STOP.** **T3 route-declaration
requirement (ruled at adjudication): Codex DECLARES its chosen route in the
report, and if the declared route's red surface differs from this section's
analytic enumeration, the difference is REPORTED rather than reconciled.** **FLAGGED DEVIATION, for adjudication
rather than silence: T3 was NOT dry-run.** Its implementation route is Codex's
declared choice, and pre-implementing one would pre-empt that freedom; T3's red
surface is enumerated analytically — no suite fixture exercises a post-death
nearest-sample (drift requires the seam T1 closes), and C1's committed exposure
is zero-realized — and the gap is closed by the payload-run obligation: Codex
runs the full suite post-payload, and ANY red not in this enumeration is a STOP,
not a fix.

## §5 — Scope discipline and acceptance

Exactly three guard sites; NO scenario bytes (content hash byte-identical,
checked in the report); no event-schema changes beyond removals P4 licenses; no
other endState logic touched; no reseed. **Acceptance: P1–P5 verified by the
N=50 campaign re-run and the 18760647 trace, the suite green with zero
unenumerated reds, the T1-bookkeeping and T3-route declarations present, hash
identical.** A P1/P3 divergence means the patch differs from the ruled guards or
the world holds a second coupling — a defect or a finding, never a surprise to
adjudicate as acceptable. Report format per the house pattern: per-prediction
verdicts, the stillness trace, the C1 column, the enumeration table with
dry-run results, file SHAs.
