# CODEX WORK ORDER — WO-D111: break 1, the inert-class commit

*Frozen 2026-08-02 at Fable's pass (three fixes: the restored `§`, the approach-vector counter
as a derived table, the "as annotated" wording). This WO BREAKS THE STREAM — deliberately,
once. Governing rows: D109 as annotated (class separation, per-field membership), D110 (the
pins), D100 (the landmark declaration), D111 (this break). Registration: PR-57..63, committed
alone at `1e0f4bf` ahead of this WO. The registered predictions are COMPUTED AND REPORTED by
this WO, never adjudicated by it — adjudication is on the returned envelope.*

## Scope

IN: three scenario byte groups (five edits); the UI code half; oracle refreshes with cause;
the N=50 campaign with re-baseline instruments and registered counters.
OUT: everything value-class (coalition band, pony, 180-claim transcription — break 2, D112);
any engine-source change beyond the UI files named; any tuning of anything after observing
anything.

## Task 1 — The five byte edits (data/scenarios/little-bighorn-1876/scenario.json)

**Edit 1 — bench provenance (the `bench` entry in `coverFeatures`):**
- `confidence`: `"MEDIUM"` → `"LOW"`.
- Source locus: `"p.105, low hill / Bench"` → `"p.105, low hill / Bench — RELAYED /
  likely-misattributed (O5 B3)"`.
- `note` replaced with (verbatim): `"D90 in-frame terrace-search result (rise 3-18 m, <=3.5 m
  relief across 60 m, downhill break toward Reno's line; selected on absolute geometry, no
  computable flank per the D91 rider). D101 confidence downgrade executed at D111: the Michno
  p.105 attribution is RELAYED/likely-misattributed (O5 B3: 'bench' absent from Lakota Noon's
  index and frequent-terms; p.105 most plausibly falls beyond the valley fight) and the Jackson
  '>500 warriors' pillar is a Wikipedia-mediated gloss of Schultz 1926, not Jackson verbatim
  (O5 B2). The terrain is real - a raster-derived terrace; what is weak is the historical
  attribution. The feature remains declared: sole west-bank camp-defence cover post-D98. See
  the D90 row's dated annotation (2026-08-02) for the Varnum region-level corroboration."`

**Edit 2 — `meta.timeAnchor`** replaced with (verbatim): `"Chronology per Gray (1991), whose
timing baseline is Lt. George D. Wallace's watch set to Chicago time (CBHMA/Heier review;
HistoryNet - RELAYED; Gray's methodology chapter not consulted at page level; O5 B4). The
prior 'local apparent sun time' framing and its ~45-70 min offsets are superseded as
themselves the error. No sun-time offset is encoded: UNRESOLVED until the exact figure is read
from Gray's page. Derived estimates recorded as estimates, not data: ~65-66 min geometric
(90th-meridian Chicago standard vs ~107.3 W plus the late-June equation of time); Wagner's 57
min was St. Paul time, not Chicago."`

**Edits 3–5 — three landmark entries appended to `terrain.landmarks`,** ids and coordinates
exactly as ruled in D100 (names/notes verbatim below; per-entry relief and the shared note
adjusted only for the relief figure):

```json
{ "id": "foothills-1", "name": "Foot-hills, west valley edge (south)",
  "position": { "lat": 45.50579, "lon": -107.40259 },
  "provenance": { "confidence": "MEDIUM", "sources": [
    { "key": "RCOI", "locus": "Varnum, feature identification via O6 secondary synthesis: warriors held '800, some 1000, and some took 1200 yard range... every bullet fell short'; 'rode around on those foot-hills to the left'" } ],
    "note": "D100/D90: in-frame summit identification (O6 §D names the W-edge foothill set; identification relayed via O6 secondary synthesis per the house relay convention), +21.1 m relief, ~1.8-2.2 km from Reno's line; D88 governs (O6 lat/lon non-authoritative). Region-level corroboration per the D90 row's dated annotation (2026-08-02): Varnum's foot-hills are the continuous western rise and these points are its summits, a selection convention on a measured region; NOT claimed as Varnum's firing position. Confidence MEDIUM against the Bench's LOW, asymmetry deliberate: the Bench's LOW is an attribution judgment (whether the sources' 'bench' is that terrace - O5 B3 undercut it), while the foothills' attribution is not in doubt (O6 names the set, Varnum names foot-hills to the left, the DEM shows exactly one western rise); what is weak here is point selection within a region, declared as convention rather than discounted as confidence. Display and ledger vehicle; referenced by no order, checkpoint, assertion, or exam item - engine-inert by construction, attested by the D110 landmark-consumption pin." } }
```

`foothills-2`: name `"Foot-hills, west valley edge (north-west)"`, position (45.51317,
−107.41571), note relief `+31.7 m`. `foothills-3`: name `"Foot-hills, west valley edge
(middle)"`, position (45.50827, −107.41294), note relief `+33.0 m`. All other fields
identical in structure to `foothills-1`.

Assemble, don't invent: every value above traces to the D90 row, its dated annotation, O5
B2/B3/B4, or the O6 report. Nothing else in scenario.json changes. **The hash will change —
once. Record the new short hash; it is the new stream id. A second change is a STOP.**

## Task 2 — The code half (reseed-free)

- `app/battle-map.tsx` `important` set: add `'foothills-1', 'foothills-2', 'foothills-3'`.
- `app/battle-map.tsx:612`: `'JUNE 25, 1876 · LOCAL SUN TIME'` → `'JUNE 25, 1876 · SCENARIO
  CLOCK'` — non-committal by ruling; the UI must not assert a resolution the record withdraws
  in the same commit (B4).

## Task 3 — Oracle refreshes, cause-documented

Refresh the combat and no-combat oracle pins once each, with the documented cause "D111
inert-class byte commit; every stream re-rolls per D31a." Any test pinned to stream-specific
values updates the same way, each with the same cause note. The pinned LIP test is NOT in this
class — it must pass untouched (PR-59).

## Task 4 — The campaign (N=50, seeds 18760600–18760649)

Instrument in the d108-campaign form (`scripts/`), collecting per seed and in aggregate:
- Envelope: per-seed composite + components C1..C4; distribution stats. (PR-58 legs computed
  and reported against the registered referents — 57.63 median / 56.15 ± 1.02 mean / component
  medians 0.5, 6/9, 5/13, 12/13 — labelled HIT/MISS per leg, adjudication reserved.)
- The re-armed stop, live, registered form: >5/50 seeds above 60 Reno killed, or any ≥100.
- Sanctuary hard leg: east-side Reno annihilations (PR-61(a): any = MISS).
- Watch counters, frequency-only (PR-61(c)). **The approach-vector counter is a TABLE, and
  the count derives from it** — no implementer-chosen predicate: for EVERY annihilation,
  record (seed, tick, unit, position/side) plus the distance to the nearest eligible friendly
  under D107's own predicate (same-side combat unit, STEADY, not destroyed, not withdrawn)
  and whether the annihilated unit's last movement step REDUCED that distance. The D108
  breach must be reproducible as a row (co-m, 865 m, closing). Any future threshold gets set
  against this distribution, not against a definition someone chose — PR-61's
  threshold-independence discipline, one level down. The switch-to-nothing stranding counter
  uses the committed state signature (the D98-annotation probes' detection: campDefense held,
  threat changed, featureId cleared to none).
- PR-63: F4 evaluated on the envelope (single-seed flips reported as expected reseed
  behaviour); the D80 typical-seed criteria re-run UNCHANGED, result reported whatever it is.
- Re-baseline instruments, same WO: ground-pressure census and valley-range instrument
  (`.claude/ground-pressure-census.mjs`, `.claude/q3-valley-range-baseline.mjs` patterns)
  against the new stream — their outputs are the NEW BASELINES (PR-62).
- Lip byte-identity check (PR-59): extraction reproduces 85 / 260 m / no gap >20 m / min 51 m
  / all WEST, byte-identical cell list.

## Verification (any failure = STOP and report, fix nothing)

1. Scenario hash changed EXACTLY ONCE; new hash recorded everywhere the old one was pinned,
   each with the documented cause.
2. All three D110 pins green (pin (c) with the three new ids absent from the consumed set —
   PR-60's substance). Gate test green (the new bytes are clean). Full suite green.
3. Campaign complete at N=50 unless the stop fires (a stop fire is a STOP: halt, preserve,
   report).
4. Report `codex-report-wo-d111.md`, house format: per-leg PR-57..63 computed results
   (adjudication reserved — if a leg misses, REPORT ONLY: no bisect, no diagnosis beyond the
   registered computation; the instrument-first reading order belongs to adjudication), the
   new stream id, the approach-vector TABLE and stranding frequency, F4/D80 outcomes,
   re-baseline outputs, files touched, AMBIGUITIES, deviations. No commit, no push.

## Dispatch

Fresh Codex exec (new work order). The stop and STOP conditions are live from the first tick.
