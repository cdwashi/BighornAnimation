# CODEX WORK ORDER — WO-D132: VARIANT TOGGLES + THE SEED SELECTOR (M5 closes on its own sentence)

*FROZEN 2026-08-07 at Fable's three flag rulings: DISTINCTNESS RULED MET on the
measured rosters and PLAUSIBILITY RECORDED-NOT-ADJUDICATED (an open historiographical
observation — pair 1 is Utley's thesis run to its conclusion, THE FIRST GENUINELY
COUNTERFACTUAL RESULT THE PROJECT HAS PRODUCED, and a product milestone does not
silently rule on a historical dispute; the panel shows each variant's proponents, so
the user is told whose thesis they're running); the TWO ACCEPTANCE LEGS separated
with the UI-finding route (§3a); the FIFTY-SEED SET CLOSED. The FIFTY-THIRD catch
recorded, the adjudicator's: the Ford B binary was named from the variant's LABEL,
not from measurement, in the same message that ruled the definition must be
operationalized — the fourth instance of a correct ruling's own gloss being the
failure surface (D115 enumeration, D120 extent, D126 exit criterion, this); the
dry-run killed it before it froze, which is what the dry-run was for. Governing: the
sitting adjudications of 2026-08-07 (the gap read's ruled ordering; the three
variant-WO rulings — the SEED-SELECTOR panel, the OPERATIONAL DEFINITION of visibly
different, DERIVE-NOT-MIRROR); PRD §4.5/§9/§11; the D129 world (nothing
expected-pre-existing); the D80 median seed as app default (`88b7c98`).*

## §0 — The claim

**M5's exit line closes with both deliverables real: variant toggles AND a parameter
panel — the panel being a SEED SELECTOR (ruled: the seed is a genuine parameter the
engine already takes, and the RIGHT one: every register number is an envelope over
fifty seeds, and this turns the distribution from an internal concept into something
a user can traverse).** The §4.5 knob surface (leader ratings, strength sweeps,
timing offsets) DEFERS to the backlog by dated PRD ruling — uncalibrated knobs, §10's
fence, no demonstrated user need — landed alongside this WO.

## §1 — The measured world (the dry-run: THE FIRST COMPLETED VARIANT RUNS IN THE
REGISTER — D125's census called #137/#138 "unscored variant surfaces"; until this
probe, variants had only ever been load-tested)

`.claude/wo-d132-pair-probe.mjs` / `.out.txt`, seed 18760600, read-only, committed
with this freeze. Baseline reproduces the committed world (destroyed roster = the F4
five exactly). **Both §11 pairs pass the ruled operational definition — a difference
a user can see without instruments — on the DIFFERENT-FINAL-ROSTER clause:**

- **Pair 1, `v-mtc-crossing`:** destroyed roster 5 → **1** (only co-l); engagements
  75 → 62; a leader killed; pursuit dynamics ~7× (851 pursuit-starts vs 119). FOUR
  companies that die in baseline survive. **The probe KILLED the assumed observable:
  "the Ford B crossing fires or it doesn't" does NOT discriminate — `ford-crossing`
  events fire in both worlds (18 vs 17; the baseline feint reaches the river too;
  co-e/f min-distance to ford-b is 0/9 m in BASELINE). The discriminator is the
  roster.** Measure-before-freezing caught an assumed binary before it froze.
- **Pair 2, `v-organized-last-stand`:** a DIFFERENT death list, not a shorter one —
  co-c SURVIVES, co-d (a baseline survivor) is DESTROYED, and `lwm-band` (a
  coalition unit) is DESTROYED; engagements 75 → 97; two leaders killed.

## §2 — The payload (app-side only; ZERO engine, scenario, or test bytes)

**T1 — the variants panel** (PRD §4.5's own words): a toggle per committed variant
showing `label`, `description`, and `proponents` from the scenario data;
**exclusion enforcement DERIVED from `excludesVariantIds` — single source, never a
second implementation of the rule** (ruled: two implementations of one rule is the
five-relation-inconsistency pattern in miniature, and that finding stands on this
register's board); "Reset to baseline" always available.

**T2 — worker protocol:** `sim-messages.ts` carries the selected variant ids and
seed; `sim-worker.ts` passes them to `createSim(scenario, { seed, terrain,
variants })`; the sim re-runs on change. Default state: zero variants, seed
18760600 — byte-for-byte the run the app ships today.

**T3 — the seed selector:** the FIFTY COMMITTED SEEDS (18760600–18760649) as the
named set, default 18760600 (the D80 median, rank 26 of 50), presented so the user
can traverse the measured envelope. Active-variant readout beside it. No free-form
entry in v1 (the named set is the measured set; anything else is an uncharacterized
world — the seed-0 lesson, two commits old).

**Untouched, declared:** engine source, scenario bytes (hash byte-identical),
tests, hooks, manifests.

## §3 — Acceptance

- **(a) The §11 metric, TWO LEGS SEPARATED (ruled at freeze) so a failure is
  diagnosable:**
  - **Leg 1 — THE WORLDS DIFFER, MEASURED:** the §1 rosters reproduce through the
    app's own worker path with each pair toggled (probe-leg; a divergence from the
    measured rosters is a defect or a finding).
  - **Leg 2 — THE DIFFERENCE IS APPARENT ON SCREEN WITHOUT INSTRUMENTS:** Chuck
    confirms it visually — §11 says VISIBLY, and a probe establishes difference,
    not visibility; only a human closes the second claim. **ROUTING, ruled: if
    leg 1 passes and leg 2 fails — the roster differs and Chuck can't see it —
    that is a UI FINDING (the panel or map isn't surfacing what the model did),
    routed as its own item, NOT a failed variant and NOT a STOP of this WO.**
- **(b) Exclusion derivation:** toggling a variant disables exactly the set named
  by its `excludesVariantIds` (and reflexively); the report shows the derivation
  reads the data, not a copy.
- **(c) Reset returns byte-identical default state** (zero variants, seed
  18760600) — the run the register characterized.
- **(d) Seed traversal:** selecting any of the fifty seeds re-runs
  deterministically; spot-check at acceptance: seed 18760625 (the D80-era stop
  seed) and one other reproduce their committed campaign-row casualty fields.
- **(e) Suite green in the D129 world:** 22 files / 121 assertions, exit 0, both
  surfaces separately; quartet green; any red on either surface is a FINDING and a
  STOP; a process-surface error is a new RPC-family member, routed per D129.
- **(f) Byte checks:** all touched files LF/no-BOM; scenario hash unchanged.

## §4 — Two-surface pre-freeze enumeration

**ASSERTION SURFACE:** expected NONE red — the payload touches no test surface; the
suite baseline is six-plus clean exit-0 runs. **PROCESS SURFACE:** expected EMPTY
(D129 world; family-watch armed). The §1 probe is the measured counterfactual for
(a); its numbers are the reference, and divergence through the app's worker path is
a defect or a finding, never a surprise.

## §5 — Scope and report

Codex, fresh exec. Files: `app/battle-view.tsx`, `app/lib/sim-messages.ts`,
`app/sim-worker.ts`, `app/styles.css`, plus (Codex's declared route) a small
`app/lib/variants-panel.ts` helper if wanted; report `codex-report-wo-d132.md` —
route declared, both §3(a) rosters shown from the app path, exclusion-derivation
evidence, quartet verbatim with surfaces separate, deviations reported never
reconciled. No commit, no push.

## §6 — Adjudicated at freeze (Fable, 2026-08-07)

1. **Distinctness RULED MET; plausibility RECORDED, NOT ADJUDICATED.** Five-to-one
   is distinct beyond argument; whether it is plausible is a historiographical
   question this WO does not rule. The panel's proponents display is the honest
   treatment: the user is told whose thesis they're running. **For the row: the
   first genuinely counterfactual result the project has produced — a claim about
   the battle under a contested premise, which is what the variant system was
   built for, and nobody had run one.**
2. **Chuck's eyes STAY in the exit criterion** — §11 says visibly; a probe
   establishes difference, a human establishes visibility. Legs separated in
   §3(a) with the UI-finding route.
3. **The fifty-seed set STAYS CLOSED** — free entry puts users in worlds with no
   committed row, no envelope position, and nothing the report card can say: the
   exact traceability gap the seed-median change closed. Arbitrary seeds are a
   backlog item with a stated cost, if ever.
