# CODEX WORK ORDER — WO-D110: the pre-break pass (three pins, one gate carried twice, two METHODS lines)

*Frozen 2026-08-02 at Fable's three amendments (side-scoped sum; integer-recovery; fail-loud
no-git). Reseed-free by construction: this WO touches no scenario bytes. Acceptance is gated on
the scenario content hash remaining `ba288f09` byte-identical — a hash change is a STOP, not a
thing to fix. Governing rows: D109 (class separation, two-break sequencing) and D110 (this
pass) in IMPLEMENTATION_HISTORY.md.*

## Scope

IN: three pin tests; the BOM/CRLF gate carried twice (suite test + pre-commit hook); two prose
edits to docs/METHODS.md §6.
OUT: any engine-source change; any scenario byte; the UI banner and `important`-set (break 1's
WO); any campaign; any predictions entry (ruled in the D110 row — the one genuine prediction in
this material, pin (a) firing at break 2, is registered in D112's predictions commit).

## Task 1 — Pin (a): the ratio duplicate, rescoped (Fable's pre-freeze catch: the original
draft was red on the current tree — `sideCasualties` holds ONE key, and us-7th is
mixed-provenance across its bounds)

New test (extend `tests/data-integrity.test.ts` or a sibling file, Codex's choice), three
assertions against `KILLED_TO_WOUNDED_RATIO_RANGES` (engine/src/combat-config.ts:142–156):

1. **Coalition**: `low = killed.low/wounded.high`, `best = killed.best/wounded.best`,
   `high = killed.high/wounded.low` against
   `scenario.calibration.sideCasualties['lakota-cheyenne-coalition']` (the file's own
   most-conservative-cross-products convention, comment at :139–141; current values
   31/200, 60/160, 300/100).
2. **us-7th low and high**: same cross-product form against the summed per-company
   `calibration.casualties` bounds, **scoped by side, not by table**: sum only entries whose
   `unitId` resolves to a unit with `sideId === 'us-7th-cavalry'`, and assert the filtered set
   is non-empty. (Today all twelve entries are us-7th, so table-sum and side-sum coincide —
   the scoping exists so a future coalition entry in that table cannot silently corrupt the
   numerator while the pin stays green. Sums verify K 235/–/285, W 45/–/60 → low 235/60,
   high 285/45.)
3. **us-7th best**: the literal `268/52`, pinned by value, with a comment stating it is the
   M5-SPEC hilltop-inclusive figure, sourced outside the scenario, and DELIBERATELY diverges
   from the per-company sum of 253 — a declared exception; silently reconciling 268 to 253
   turns this red and requires an argument. Do NOT change the 268: `range.best` is consumed
   (combat-config.ts:231) and this WO is reseed-free and campaign-free. The byte-side question
   (does `sideCasualties` gain a us-7th entry; does the scenario carry 268) is registered for
   break 2.

Also assert every `sideCasualties` key appears in the ratio map (this direction holds; do NOT
assert identical key sets — that is the assertion that was red).

**Integer-recovery rule (not a tolerance):** the per-company sums carry float noise on
quantities the record holds as integers (D26; measured residue 2×10⁻¹⁰ on killed low). Two
steps: assert `|sum − round(sum)| < 1e-6` — the integrality check, which fires on any real
drift and passes at the measured residue — then divide using the rounded integer and compare
the ratio EXACTLY. The pin reads as what it is: these bounds are integers, here they are, here
is the ratio. A drift of 0.4 men breaks integrality and reports it, where a bare epsilon would
have swallowed it.

This test MUST pass green on the current tree as rescoped. It is designed to go red at break 2
(D112) when the coalition band changes — that red is the pin doing its job, the break-2 WO
carries the simultaneous code-side update, and D112's predictions register the firing.

## Task 2 — Pin (b): the camp-defence candidate set

New test: construct `DefenseFeature`s exactly as `defenseFeatures()` does
(engine/src/camp-defense.ts:211–233) from the committed scenario + terrain, and assert the
scenario-derived subset (`scenario-*` ids) equals exactly `['scenario-bench']`. Any future
`coverFeatures` entry turns this red and forces a ruling at the moment of addition. Use the real
scenario and terrain fixtures (the d91/d108 test loaders show the pattern), not synthetics —
the pin's subject is the committed data.

## Task 3 — Pin (c): landmark consumption equals landmark reference

New test asserting the set of landmark ids CONSUMED engine-side ⊆ the set REFERENCED by data:

- Referenced set, computed from the scenario: every `objective.landmarkId` in orders, every
  checkpoint `landmarkId`, every `calibration.endState[].landmarkId`, every observation-event
  `target.landmarkId`, plus `ford-a` (envelope.ts:33's direct lookup — include it as a named
  literal with a comment saying why).
- Consumed set, preferred implementation: wrap `scenario.terrain.landmarks` in a tracking Proxy
  (or replace with an instrumented array) inside the test; run one short deterministic sim
  (seed-stable, a few hundred ticks), `runObservationExam`, and `scoreCalibrationRun`; collect
  every id looked up.
- Assert consumed ⊆ referenced, and assert every referenced id exists in
  `scenario.terrain.landmarks` (subsumes the existing reference-integrity intent).

Fallback if the proxy proves impractical in the harness: a static assertion that every
`scenario.terrain.landmarks` access site in `engine/src` is a `.find()` by id (source-level
check) plus the referenced-id integrity assertion. The report MUST state which implementation
landed and why. **Pre-committed consequence (D110 row): if the fallback lands, D111's row must
state that its inertness-by-construction claim rests on a grep rather than an instrument** —
this link exists so the weakening cannot be discovered after the fact. This pin is the standing
instrument for D111's inertness-by-construction claim on the D100 landmark bytes: an
unreferenced landmark is consumed by nothing, and a future iterator turns CI red instead of
silently making declared landmarks load-bearing.

## Task 4 — The gate, carried twice: hook AND suite test (hooks don't travel; tests do)

The same predicate lands in two vehicles, and the report says both did:

**(4a) Suite test — the binding form.** A test that walks every TRACKED text file's INDEX/HEAD
blob (`git ls-files` + staged/committed bytes, or the equivalent through the test harness'
process API; skip binaries by extension allowlist) and asserts: no UTF-8 BOM (EF BB BF) at
offset 0, no CR (0x0D) anywhere. This travels with every clone, CI run, and Codex exec — it is
what makes the rule binding on everyone. Must pass green on the current tree. **Fail loudly
where git is unavailable, never skip silently:** if the test cannot reach a `.git` (packaged
checkout, sandbox, some CI layouts), it ERRORS with a named reason — a skip would make the
binding form silently absent, which is the exact defect that made the hook insufficient. If a
legitimate no-git environment turns up, that is a finding for adjudication, not a condition to
tolerate.

**(4b) Pre-commit hook — the fail-fast courtesy.** `scripts/hooks/pre-commit` (POSIX sh,
executable) + activation via `git config core.hooksPath scripts/hooks`, documented in the
report and in the script's comment header (local config; does not travel — that is why (4a)
exists). Behavior:

- For every STAGED text file, read the INDEX bytes (`git show :path` on the staged blob —
  **never the working copy**: Windows working copies are legitimately CRLF under autocrlf;
  the repo's index is LF-normalized, and a hook that rejects the working copy's normal state
  gets disabled within a week).
- Reject the commit, naming file and offense, on BOM-at-0 or any CR.
- Must produce no rejection on the current tree. Include one self-test (stage a synthetic
  BOM'd file in a temp repo/index and assert rejection — Codex's mechanics, report shows it).

## Task 5 — METHODS §6, two prose lines

(a) The measure-before-freezing paragraph (docs/METHODS.md:72) currently restates the
fallback-clause death inline ("an anticipated fallback clause (0 of 96 samples ...)"). Replace
that parenthetical with a pointer to its register entry: NEGATIVE_RESULTS §4, the
"Anticipated fallback clause for camp-defence feature loss (D98 draft)" row — keep the item in
the four-designs list, point instead of restate. Do not touch the other three examples.

(b) After the three standing practices, add the yield sentence — take final wording from the
committed D110 row verbatim (the five-catches sentence ending "the incidence is manufactured by
running them").

## Verification (all must hold; any failure = STOP and report, fix nothing)

1. Scenario content hash `ba288f09` byte-identical.
2. All three pins GREEN on the current tree AS RESCOPED (Task 1's three-assertion form with the
   side-scoped sum and integer-recovery rule). A red pin beyond the rescoping is a finding for
   adjudication — the pin has caught a divergence this WO's premises missed — halt and report
   it; do not adjust the pin or its subject.
3. Full existing suite green, zero modifications to existing tests except the chosen extension
   point of data-integrity.
4. Gate test (4a) green on the current tree; hook (4b) self-test passes and produces no
   rejection on the current tree; `git status` clean of incidental changes.
5. Report: files touched (exact list), pin implementations chosen (esp. Task 3's — with the
   D111-consequence line triggered if the fallback landed), both gate vehicles confirmed
   landed, hook activation instructions, METHODS diff quoted in full.

## Dispatch

Fresh Codex exec (new work order, not a fix round), per the dispatch pattern. Stop discipline
standing: any STOP condition above halts the WO for adjudication with the tree preserved.
Report to codex-report-wo-d110.md, house format. No commit, no push.
