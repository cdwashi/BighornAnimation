# CODEX WORK ORDER — WO-D130: PIN (e) + THE FILTER-POPULATION RIDER

*FROZEN 2026-08-06 at Fable's rulings: BROAD predicate (the fourteenth block is the
argument — a predicate that enumerates by key name inherits the naming conventions in
force the day it was written); B8 named the rider's KEYSTONE; §4's D129 dependency
stated; the FORTY-SEVENTH catch recorded (the adjudicator's — D125's "thirteen" was a
count without its filter, the twenty-first's family); the D125 dated annotation lands
with this freeze; committed on Chuck's word, dispatch on the same word. Governing rows:
D125 (pin (e) ruled as the ruling's real output — "the set of DISPUTED-confidence
provenance blocks on GATED surfaces equals the ruled set — today exactly
`sideCasualties.lakota-cheyenne-coalition.killed`"; converts caught-by-accident into
caught-by-CI); the D128 dated annotation (the rider's license: "hook scripts are the
last files that should sit outside the gate's population"; ride pin (e)'s WO, split if
the surfaces prove too different — the drafter judges them compatible: two one-claim CI
additions, zero scenario bytes, one report with surfaces stated separately); D126's
two-surface enumeration; D129's world (vitest 4.1.10, exit-0 quartet).*

## §0 — Two claims, stated separately

**Claim 1 (pin e):** the set of DISPUTED-confidence blocks on gated surfaces in the
committed scenario equals the ruled set of one, and any addition turns CI red at the
moment of addition — where #133's exclusion should have been ruled and wasn't.

**Claim 2 (the rider):** the BOM/CRLF gate's population includes hook scripts — the
extensionless files whose corruption would be self-defeating (the mechanism that
catches the offense, broken by the offense, unguarded because its own filter doesn't
reach it).

## §1 — The measured world (probes committed with outputs)

**Pin (e) (`.claude/pin-e-probe.mjs` / `.out.txt`, run at `e5d4716`):** 14
`confidence: 'DISPUTED'` blocks scenario-wide under the BROAD predicate (any key).
**GATED subset: exactly one —
`calibration.sideCasualties.lakota-cheyenne-coalition.killed.provenance`. GREEN today.**
Ungated thirteen: two orders, ten variant-family blocks, and
`leaders[7].ratingsProvenance`. **Count reconciliation, flagged:** D125's scenario-wide
check reported THIRTEEN — the `provenance`-keyed blocks; the fourteenth is
`ratingsProvenance`, a DISPUTED block under a different key, ungated (ratings feed
behavior, not scoring gates). **Predicate choice, flagged for the freeze (CC's
recommendation carried): the pin enumerates the BROAD predicate** — any block carrying
`confidence: 'DISPUTED'`, whatever its key — because the pin exists to catch the next
#133, and the next #133 does not promise to spell its block "provenance."

**The gated-surface classification** mirrors the four confidence-gate call-site
families (D109's dated annotation; read at drafting): `score.ts` checkpoints (:177),
sideCasualties bands (:211), calibration assertions/timing (:259, :342); `exam.ts`
observation events (:51). Gated prefixes: `checkpoints[`, `observationEvents[`,
`calibration.sideCasualties.`, `calibration.endState[`, `calibration.timing`. Variant
interiors are unscored until applied (D125: #137/#138); orders and leader ratings feed
behavior, not gates.

**The rider:** both committed hook scripts' INDEX bytes verified clean at drafting
(pre-commit 1,440 B, commit-msg 574 B; zero BOM, zero CR). Population under the
extended filter: 455 → 456, **delta exactly `scripts/hooks/commit-msg`** (pre-commit
was already hardcoded). The `scripts/hooks/*` sh case pattern verified to match hook
scripts and nothing else (`scripts/other/notahook`, `dist/x.js` excluded).

## §2 — The payload

**T1 — pin (e), new `it` block in `engine/tests/d110-pins.test.ts`** (the (b)/(c)/(d)
family shape): walk the committed scenario for every object with
`confidence === 'DISPUTED'` (broad predicate, any key, the pin-(d) path-walker
pattern); classify against §1's gated prefixes; assert the gated set equals exactly
`['calibration.sideCasualties.lakota-cheyenne-coalition.killed.provenance']`. The
assertion message names the pin and the D125 row. Zero scenario bytes, code-only,
reseed-free.

**T2 — the rider, three touches:**
(a) `tests/repository-text-integrity.test.ts` `isTrackedText`: replace the hardcoded
`path === 'scripts/hooks/pre-commit'` with `path.startsWith('scripts/hooks/')`;
(b) `scripts/hooks/pre-commit` `text_path`: replace the `scripts/hooks/pre-commit`
case arm with `scripts/hooks/*`;
(c) `.claude/gate-repair-matrix-probe.mjs`: one new blob cell **B8 — THE RIDER'S
KEYSTONE (ruled at freeze, as B5 was D128's): an offense in an EXTENSIONLESS file at a
`scripts/hooks/`-shaped path** (BOM'd synthetic hook script, staged in the temp repo),
REF verdict FAIL(BOM), plus a CR variant if the cell design prefers one file per
offense class. It is the cell where the whole rider lives — a repaired gate passing
every cell but B8 has not extended its population. The matrix output regenerated and
committed.

**Untouched, declared:** everything else — no scenario byte (hash byte-identical), no
engine source, no schema, no manifests.

## §3 — Acceptance (surfaces separate, per claim)

**Claim 1:** the pin runs INSIDE the full suite (probe-verified is not suite-verified)
and is GREEN asserting exactly §1's measured set; a deliberate temp-copy mutation check
is NOT required (the pin is an equality assertion on committed data — its red path is
exercised by construction when the set differs; Codex may include a self-test if the
route wants one, declared).
**Claim 2:** matrix cell B8 FAIL(BOM) under BOTH repaired vehicles (the suite test via
the D128 root-override route; the real pre-commit hook via the probe); the committed
hooks stay clean in the live index (the extended gate green on the real repo, 456
population, delta exactly commit-msg); D128's cells B1–B7/M1–M5 unchanged.
**Both:** full suite green, exit 0 (D129's world), quartet green; the process surface
EMPTY — post-D129 the onTaskUpdate error cannot fire, so ANY process-surface error is
a NEW FINDING and a STOP (family-watch routing, D129 §3(a)).

## §4 — Two-surface pre-freeze enumeration (executed by the §1 probes + today's baseline)

**ASSERTION SURFACE:** the new pin measured GREEN on the committed world
(`pin-e-probe.out.txt`); the extended filter measured against the live index (both
hooks clean; population 456); all 120 existing assertions green in SIX exit-0 runs
today (D129's acceptance + verifier). Expected reds: NONE.
**PROCESS SURFACE:** expected EMPTY (the D129 world); any error is a finding and a
STOP. **The suite-baseline reasoning, stated for attack rather than silently assumed:**
no additional pre-freeze full-suite run was executed for this WO — the baseline world
was measured six times today, and this WO adds assertions to it; the payload run is the
first execution of the combined world, and any red there not enumerated here is a STOP.
**THE D129 DEPENDENCY, ruled load-bearing at the freeze:** this reasoning is LICENSED
BY D129's retirement, not assumed generally. Under the old world "suite exit 0" was
unreachable and every WO carried two expected-pre-existing reds; today nothing is
expected-pre-existing, which is exactly what makes skipping the dry-run defensible. A
process-surface error in the payload run is a NEW RPC-FAMILY MEMBER under D129's
family-watch, routed as its own finding — this WO stops rather than absorbing it.

## §5 — Scope and report

Codex, fresh exec. Files: exactly the four §2 paths + `codex-report-wo-d130.md`.
Report: both claims' acceptance stated separately; the matrix reproduction including
B8; the live-index population figure with its delta; pin (e)'s green shown inside the
suite run; quartet verbatim, both surfaces; deviations reported, never reconciled. No
commit, no push.

## §6 — Adjudicated at freeze (Fable, 2026-08-06; rulings in the message body)

1. **The broad predicate: APPROVED, the fourteenth block the argument.** "The pin
   exists to catch the next #133, and the next #133 doesn't promise to spell its block
   'provenance'" — ruled as the ruling. The register has been caught twice on absence
   claims scoped to one shape (D115's 993 points; the D128 extensionless gap).
2. **The D125 annotation LANDS (with this freeze), and the FORTY-SEVENTH catch is the
   adjudicator's:** a scenario-wide enumeration reported without its predicate stated —
   "thirteen DISPUTED provenance blocks" where the true statement was "thirteen blocks
   keyed `provenance`." The twenty-first's family: a count without its filter. The
   gated subset is unaffected at one; the ruling stands.
3. **Two claims, one WO: APPROVED** — surfaces stated separately; T1 lifting cleanly if
   split is the property that makes bundling safe.
4. **No-self-test: APPROVED** — pin (e)'s reference world is the probe's own
   enumeration, measured and frozen here, the same guarantee a self-test would give.
