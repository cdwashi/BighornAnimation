# THE −1.6879 RESIDUAL — registration (the expectation audited before the world is hunted)

*FROZEN 2026-08-06 at Fable's two flag rulings: (1) the anchor 56.7179 is AUDITABLE
(the adjudicator recomputed it from `reports/d111-campaign-results.json`, mean over the
fifty committed rows, matched to four decimals) but is RE-DERIVED in step 1 like every
other term — "the anchor is the term most likely to be taken on faith precisely because
it looks like a fact rather than a calculation"; the cannot-audit conditional stays; and
step 1 re-runs the SUBTRACTION at full precision, printing both chains — the arithmetic
becomes a checked object, not a narrative. (2) STEP 1 FREEZES AND RUNS ALONE; its
result RE-FREEZES the registration for steps 2–3 with H-IDENTITY's ceiling computed
against the audited quantity (the D112 bands-amendment pattern: structure frozen first,
magnitudes completed by dated amendment before the next leg runs) — registering
hypotheses against a number under audit is the shape of every registration this arc has
had to amend mid-flight, and step 1 is a complete item with two registered outcomes,
not a preliminary. Drafted by CC after the ledger search; the framing APPROVED at
adjudication ("the ordering is the ruling"); committed on Chuck's word. Governing rows: D112 (origin, with its 2026-08-06 dated annotation —
the 48th catch: the printed intermediate did not close; the flips term verified as the
observed 24/50; the carried −1.6879 unaffected), D113 (the breach hypothesis DEAD at a
0.28 pp ceiling; the QUANTITY held open, pointing elsewhere), D119/D120 (the
arrival-micro-geometry family, at result weight), D130 (the corrected carry: the item
opens with a quantity that has no owner and one eliminated candidate, not a closed
question being reopened).*

## §0 — The quantity

The unexplained MEAN gap at D112's value-class break, stream `68325eff`:

    expected = 56.7179 − 1.3333 (flips, OBSERVED 24/50 × 2.7778)
                       − 0.989  (the C4 denominator identity)
                       − 0.1667 (killed three-flip)
             = 54.2289   [the row printed 54.2256 — corrected by annotation]
    observed = 52.5409
    residual = −1.6880 pp (carried as −1.6879), against the ±1.14 band floor

The residual EXCEEDS the floor: a real deficit, not noise. One candidate eliminated
(sanctuary breaches, ceiling 0.28 pp, D113). No owner in twenty-two rulings.

## §1 — The three steps, in order (the ordering is the ruling)

**STEP 1 — TERM-BY-TERM RE-DERIVATION from committed artifacts. Zero probes.** Every
term of the decomposition re-derived at full precision with its provenance stated:
56.7179 (the pre-break expectation — WHERE does it come from, at what precision, from
which committed artifact?); 1.3333 (verified observation at the opening — re-verified
against the committed flip census with the per-flip weight's own derivation); 0.989
(the C4 identity — re-derived from the lattice arithmetic (12/13 − 12/14) × 0.15 at
full precision: 0.98901…); 0.1667 (the killed three-flip — its window, its seeds, its
weight); 52.5409 (the observed mean — re-derived from the committed 50-seed envelope
artifact). The estimate-versus-observation question settled for EVERY term, and the subtraction
re-run at full precision with BOTH chains printed (the row's and the audited one).

**STEP 1'S FIRST TWO RESULTS, produced at the adjudication that approved this framing
and carried into the frozen text as results:** (1) the printed intermediate WRONG at
0.0033 — 54.2289, not 54.2256 (the 48th catch, annotated on D112); (2) the flips term
CONFIRMED as the observation, 1.3333 = 24/50 × 2.7778, not the 29/50 estimate (which
would have given 1.6111 and a residual of −1.41). **THREE TERMS REMAIN: the anchor
56.7179, the C4 identity 0.989, and the killed three-flip 0.1667** — plus the observed
52.5409 and the per-flip weight 2.7778, each to its committed artifact. Two checked,
one caught — the rate that argues the ordering was right.

**THE TWO BRANCHES, registered so neither reads as the other:**
- **SHRINK:** a mispriced term is found; the residual shrinks (possibly under the
  floor). THIS IS A RESULT, NOT A DISAPPOINTMENT — the mechanism hunt gets easier or
  becomes unnecessary.
- **HARDEN:** every term re-derives clean; the residual stands at a VERIFIED value for
  the first time in twenty-two rulings, and the item proceeds to step 2 with a real
  target.

**STEP 2 — THE C4-CLASS IDENTITY HUNT, registered as a hypothesis with a computed
ceiling, not run as a fishing expedition.** H-IDENTITY: a second lattice/denominator
identity separates the D111-stream expectation from the D112-stream observation — a
component-count or denominator change between streams whose arithmetic price was never
taken. The only attribution at the residual's scale the register ever made precisely
was exactly this class (the C4 identity, caught by double-denominator reporting ordered
for another purpose). Before any candidate is pursued: its identity is written down and
its exact pp price COMPUTED — the price either fits inside the (possibly shrunk)
residual or the candidate is dead on arithmetic, the D113 method.

**STEP 3 — ARRIVAL-MICRO-GEOMETRY CANDIDATES, each entering with a computed ceiling
first.** D119's bout-order sensitivity (2.27 pp of mean spread — the only measured
mean-scale lever in the register) and D120's halt-geometry (deletion-scale). **THE
BRIDGE REQUIREMENT, ruled load-bearing:** both were measured under COUNTERFACTUAL
PATCHES, not as within-world deltas — neither number transfers to the residual without
a bridge stating how an order/geometry mechanism produces a BETWEEN-EXPECTATION-AND-
OBSERVATION gap inside one committed world. **A candidate whose scale comfortably
covers the target is the easiest kind to wrongly convict** — the ceiling comes before
any probe, and the bridge before the ceiling.

## §2 — Discipline

Step 1 is committed-artifact arithmetic only (fourth practice's shelf); any probe in
steps 2–3 runs under throwaway discipline (byte-identity verified, no reseed, stream
`68325eff` on every figure); two-surface enumeration on anything byte-bearing; the D129
world's rule holds — nothing is expected-pre-existing, any red is a finding and a STOP.

## §3 — Flagged for the freeze

1. Step 1's artifact list — the audit needs the D111-stream expectation's source named
   before it runs; if 56.7179's provenance is not recoverable from committed artifacts,
   THAT is a finding (an expectation that cannot be audited cannot anchor a residual).
2. Whether step 1's outcome (shrink vs harden) re-freezes the registration before step
   2 runs, or the three steps run as one frozen item.
