# 268-versus-253 — the bands amendment, ADJUDICATED 2026-08-05 (completes §5 per its own gate; commits before M-FLIP runs; frozen on Chuck's word at this commit)

*Adjudication applied: the THIRTY-FOURTH catch (the verifier's unruled `score.ts`
read; its content STRUCK from the item's evidentiary basis — P-C stands on the ledger
row and the data surface, the read noted as confirmation only) and the THIRTY-FIFTH
(the run-1 renoKilled label assumption, caught by the validity leg's own refusal)
scored as ruled; P-A's warrant RESTATED per the attack — R2's unconditional-draw
finding primary, the reader-absence grep corroboration with its scope stated.*

*Inputs, enumerated per §5's gate: Q-POP (`100f77f`), the §3 two-region read
(SOURCE-READ-LOG.md dated 2026-08-05), and M-COVER (validated 50/50 on the committed
renoKilled definition; `reports/268-m-cover.md`). NO patched-world figure exists or
informed anything here — M-FLIP has not run. Conduct: this amendment carries the
item's THIRD CROSSING, the verifier's second, confessed in full below, and requests
its ruling.*

## The confession, first

The verifier opened `score.ts:190-249` — the casualty-component region — without a
scoped-read ruling, chasing a disclosed grep's absence-of-hits instead of requesting
the amendment the corpse-drift precedent (`f6bfce9`) shows is the right order. Worse
in kind: **the item's own opening ledger search had already surfaced the answer** —
`codex-report-wo-d112.md:132` records that the score synthesis produces killed
235/253/285 and wounded 45/52/60 from the per-company table, and WO-D112's §"us-7th
resolution" records the fallback semantics — so the read paid an unruled crossing for
an answer the record held, the exact shape D117 named and the fourth practice exists
to prevent. What the read added beyond the ledger: line-number currency
(`score.ts:211-247` now; the ledger's `:203-222` has drifted) and the nine-leg
composition (four side-column bands + five flagship end-states, `score.ts:267`).
P-C below is therefore grounded on the LEDGER and the DATA SURFACE, with the
confessed read as confirmation only. **RULED at adjudication (the thirty-fourth
catch): the read's content is STRUCK from the item's evidentiary basis** — since
striking changes nothing downstream, the item keeps the property that every region
in its evidentiary chain was opened by ruling.

## P-A — the structural identity (the registered prediction M-FLIP exists to test)

**Under the one-token counterfactual (`best: 268 / 52` → `best: 253 / 52`,
`combat-config.ts:147`), the world trajectory is predicted BIT-IDENTICAL: the
annihilation-bout census matches the committed 120 ROW FOR ROW — same seeds, same
units, same ticks — and every unit's strength/endState trajectory is unchanged.**

**Warrant, restated at adjudication — the ruled read is primary, the search is
corroboration** (this register has been burned twice on absence claims; a mechanism
fact from a ruled read carries what a grep cannot): **(1) PRIMARY, from the §3 dated
read:** the split's seeded roll is drawn UNCONDITIONALLY (`combat.ts:92-96`), so the
RNG sequence is invariant under any ratio value; and the split runs AFTER losses are
applied (`combat.ts:369` decrements strength by total losses) — the ratio partitions
a ledger, it does not size a loss. Those two facts alone entail trajectory identity
unless some mechanism READS the partition. **(2) CORROBORATION, from the disclosed
grep (scope stated: `engine/src/*.ts`, identifier-level patterns `\.killed\b`,
`\.wounded\b`, `killed:`, `wounded:`; `reports/268-m-cover.md`):** no movement,
morale, combat, or pathing decision reads either ledger column — the enumerated
readers are ledger-to-ledger terminal accounting, envelope report columns, D80
baseline selection (not re-run in M-FLIP), and scoring actuals.

**Falsification, pre-committed:** ANY bout-census divergence — one row — falsifies
P-A and names EITHER an unenumerated behavioral reader of the casualty ledger OR a
path outside the grep's stated scope (a non-`engine/src` consumer, a dynamic access
the patterns miss). The STOP is informative either way: reported with the diverging
row and no interpretation; P-B and P-C are VOID if P-A falls.

## P-B — the ledger shift (unconditional; the §5 conditional's premise held at the read)

Share-form confirmed, so the direction registers unconditionally: killed share per
application falls 0.83750 → 0.82951 (−0.799 pp), killed→wounded, US side only.

Magnitudes, derived from committed figures with the derivation shown:
- **Event-level (fire path):** expected pooled fire-killed delta = pooled fire
  casualties 8,716 × −0.00799 ≈ **−69.6**, mirrored +69.6 in fire wounded.
  Registered band: pooled fire-killed delta in **[−120, −30]**; per-seed mean in
  [−2.4, −0.6] (stochastic-rounding noise widens the point estimate; the band is
  deliberately loose and the point expectation is stated).
- **Final-state:** the terminal conversion ERASES the split on DESTROYED units
  (final killed = casualties + terminal strength regardless of ratio —
  `combat.ts:461-463`, the census's 472-identity), so the final-state shift lives
  on survivors only. Survivors' fire casualties ≈ final wounded 925 ÷ wounded share
  0.16250 ≈ 5,692 pooled → expected pooled final-killed delta ≈ **−45**.
  Registered band: pooled final US killed delta in **[−85, −15]**, wounded
  +the same magnitude; per-seed final killed delta in [−4, 0] on every seed.

## P-C — the envelope (the number the register will quote)

**Predicted: the N=50 envelope is IDENTICAL to the committed world — median delta
0.0000, mean delta 0.0000, every component identical on every seed.**

Basis: C1/C3/C4 read no casualty column (checkpoints, end-states/deadlines,
observations). C2's nine legs (ledger: WO-D112 §"us-7th resolution" + report line
132; data surface: the per-company sums; confirmed by the confessed read): coalition
killed [36,136] and coalition wounded =160 are untouched by a US-side ratio; the US
legs read side-aggregated actuals against killed [235,285] and wounded [45,60], and
the census actuals sit killed median 210 (≥25 BELOW the low bound) and wounded
median 19 (≥26 below) with per-seed shifts bounded at 4 by P-B — **no leg can cross
a bound whose distance exceeds the maximum shift by a factor of six.** Internal
consistency closed in the census: FAIL+FAIL on the US legs is already priced into
the committed C2 median (6/9 = 0.6667, reproduced exactly).

**Falsification, pre-committed:** any nonzero envelope delta on any seed falsifies
P-C and names a scoring path reading a shifted quantity outside the enumeration —
STOP for adjudication, reported with the seed and component, no interpretation.

## The instrument (unchanged from the frozen §5)

Throwaway patch, ONE token at `combat-config.ts:147`; pin (a) assertion (3) goes
EXPECTED-RED with the assertion named; the pin is NOT updated; guarded restore
returns file and pin to green; byte-identity VERIFIED; no reseed; stream lineage
`68325eff` on every figure. The override channel stays unused (adjudicated at the
registration's attack). M-FLIP additionally records the bout census and the
per-seed C2 leg table so P-A and P-C are checked at their own granularity, not
through the composite alone.
