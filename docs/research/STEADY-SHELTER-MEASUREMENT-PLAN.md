# STEADY-shelter measurement plan — FROZEN 2026-08-02 (Probe 1 replaced by ruling; committed before any breach seed is read)

*Measurement before mechanism. Probe 1's resampling design was withdrawn as degenerate at
adjudication (twenty-second catch, the adjudicator's: mean-lift from removing a triple is
`(3M − S)/47`, a strictly decreasing function of the triple's composite sum — lift IS
lowness, and no resampling statistic can separate "breaches drive composites down" from "low
composites happen to contain breaches" at n = 3). Replaced by leg-level attribution, whose
predictions are registered HERE, before the breach seeds' component legs are read — that
ordering is the whole difference between this and a story.*

## Probe 1 (replaced) — leg-level attribution

**The legs an east-side Reno annihilation SHOULD break, named in advance:**

1. **PRIMARY — C3:** the annihilated company's own end-state assertion (HOLDING_AT reno-hill
   by minute 1080) must FAIL — a destroyed company cannot hold the hill. One leg per breach,
   identifiable by unit id.
2. **SECONDARY — C2:** the us-7th killed total rises by roughly the annihilated company's
   strength (~45 booked at destruction); if the seed's baseline US killed sits near the
   synthesized band's high (285), the killed leg may cross out of band. Conditional,
   direction stated: breach seeds should sit HIGHER in US killed than their composite-matched
   neighbours.
3. **CONDITIONAL — C1:** only if the annihilated company carries a reno-hill checkpoint that
   scores after its destruction tick; otherwise no C1 effect is predicted.
4. **C4: NO predicted effect** — the observation events precede the late-day annihilation
   window.

**Method:** for seeds 617/626/635 and, for each, the non-breach seed nearest in composite
(matched neighbours chosen by committed composite alone, before any leg is read), run and
score the full day; report per-leg pass/fail. Evidence = breach seeds failing the PREDICTED
legs while their matched neighbours fail different ones. **Kill-branch, registered:** the
breach seeds failing no more of the predicted legs than their matched neighbours kills the
hypothesis, and the −1.6879 stays open pointing elsewhere. **Bound on survival:** three seeds
license the next measurement, never a conclusion.

## Probe 2 — the predicate census (approved as designed, with rulings applied)

For every row in the committed D112 `annihilations` array (the measured count stated in the
probe header — not carried from elsewhere), re-run its seed to the destruction tick and
record: nearest same-side combat unit of ANY morale state (distance, state); the count of
same-side combat units within 650 m by morale state; **the full distance distribution, not
only counts** — a broken friendly at 300 m and one at 640 m are different claims about the
predicate; and the committed nearest-ELIGIBLE distance as cross-check. Aggregate three-way
split: {none within 650} / {STEADY within 650 — SHOULD BE EMPTY, non-emptiness is a defect
finding of its own} / {only non-STEADY within 650}.

**The adjudicator's registered prediction (ruled operationalization):** COMMON — at least one
non-STEADY same-side combat unit within 650 m in a MAJORITY of annihilations; the fraction
reported regardless, no threshold on the report. **Kill-branch:** rare → the predicate is not
the mechanism behind the three breaches, and they need a different cause.

## Discipline

Both probes preserved in `.claude/` with outputs, evidence-first commits; results to
adjudication carrying nothing beyond the registered branches; the mechanism question opens
only after the measurement is ruled.
