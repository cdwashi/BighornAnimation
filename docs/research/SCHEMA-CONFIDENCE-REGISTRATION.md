# The schema confidence-field item — registration, ADJUDICATED 2026-08-05, two amendments applied (POST-READ; the ledger search ran first; frozen on Chuck's word at this commit)

*Adjudication applied. The reach correction STANDS as the FORTY-FIRST catch (the
adjudicator's): "a break for a field no scoring path reads" contradicted his own
D109 dated annotation establishing confidence gates scoring at four call sites — a
pattern-match to D123/D124's nil-reach shape asserted for a field whose reach he
had personally established; the correction INVERTS the item's cost. The item's
sharpest form ruled as its statement: not one field doing two jobs, but ONE FIELD
WHERE A VALUE ON AXIS B IS ORDERED AGAINST VALUES ON AXIS A, and the ordering is
arbitrary because the axes are not commensurable. Amendment 1: Q-COST's PRIMARY
leg is the gate-exclusion hunt — the only evidence that could reach the split
outcome — with the check-not-infer requirement on `sideCasualties` (D112 measured
its `excluded` riding the note flag, not confidence; that inference is exactly the
shape the forty-first catch got wrong). Amendment 2: the rule text goes to METHODS
as its OWN SECTION — a semantic rule about what a field means, not a sixth
procedure — in the adjudicator's three clauses, the third naming the live defect
as a KNOWN INCORRECTNESS IN THE ENCODING, recorded not repaired.*

*D112's bench observation ("the schema's single `confidence` field does two jobs —
rating source quality and rating band contestation — which is why this round's tier
ruling needed three sentences of explanation") arrives as the last pre-WO item.
Conduct statement (POST-READ): drafted from ruled rows and data surfaces only; no
file body opens under this registration; every scoring-path claim below cites the
D109 dated annotation or PR-68, not fresh reads.*

## §1 — The ledger search: the overload is real, and its reach is NOT zero

**The two jobs, which-one-specifically, both ledger-attested:** JOB ONE, source
quality — the tier ladder LOW < MEDIUM < HIGH gates scoring: observation events
below MEDIUM are scoped `excluded-confidence` (`exam.ts:50-54`),
`isCalibrationExcluded` (`score.ts:144`) scopes checkpoints, side targets, and
assertions, HIGH filters at `:182`/`:338` (all cited from the D109 dated
annotation — the reclassification that established per-field inert-class
membership); PR-68: checkpoint confidence feeds the `:182` HIGH-subset gate. JOB
TWO, band contestation — DISPUTED marks "sources disagree," lives in the SAME
enum, and is ordered BELOW MEDIUM in gate terms: **#133's DISPUTED→MEDIUM was an
UPGRADE** (D112, via RCOI). **The sharpest form of the overload, stated so the
item is about the right thing: contestation semantics leak into quality gates — a
claim with excellent sources that disagree is gate-excluded as if its sources were
weak.** The D112 collisions the observation recorded: the coalition wounded tier
needed the referent clause ("the tier rates the SOURCE, not the band") plus the
non-tension clause ("absence of dispute through absence of alternatives is not
agreement"); killed stays DISPUTED beside wounded MEDIUM on the same object
(#166/#167).

**THE REACH CORRECTION, flagged against the adjudicator's sketch (the fifth
practice cuts every direction):** the sketch said this would be "a break for a
field no scoring path reads." **The ledger says otherwise, in the adjudicator's
own D109 dated annotation:** confidence values DO gate scoring at four cited call
sites — that finding is why the O5 transcription had to SPLIT by field. This item
is the reverse of D123/D124: the value has REAL reach, and that makes any byte
answer MORE expensive, not less — a schema change here is scenario bytes PLUS
scoring-source changes, value-class at minimum, the costliest shape on the board.

## §2 — The question, and the cost census the ruling needs

- **Q-COST (measure before freezing, free, from the ledger and committed
  artifacts):** **PRIMARY LEG (Amendment 1) — the gate-exclusion hunt: is there
  any claim whose DISPUTED gate-exclusion moved a scored figure?** The only
  evidence that could reach SPLIT-QUEUED-AS-VALUE-CLASS; empty confirms the lean
  on measurement rather than preference; non-empty changes the cost calculus
  entirely. Where to look, pre-named: the four gated surfaces; the two DISPUTED
  `sideCasualties` claims (killed #166, wounded-history #167) — **CHECKED from
  committed artifacts, not inferred** (D112 measured `sideBand`'s `excluded`
  riding the note flag, not confidence; the committed leg records show whether
  the DISPUTED coalition legs are scored); the DISPUTED variants (#137/#138,
  unscored surfaces); and **#133 pre-upgrade** — the C4 identity (D112's
  nineteenth catch) already priced that exclusion at 0.989 pp of every
  composite. SECONDARY LEG — the prose-collision count: D112's three-sentence
  tier ruling; the referent clause; the non-tension clause; #133's RCOI
  reasoning across the incommensurable-axes boundary; the
  DISPUTED-spreads-preserved clauses (D71/D81 era). No probes; the evidence
  base is prose and artifacts the register already committed.
- **Q-FIX-SHAPE:** what would a split even be? `confidence` (quality ladder,
  gates unchanged) + a second axis (`contested: bool` or a `contestation` field)
  — touching every provenance block in `scenario.json`, the schema, `exam.ts`
  and `score.ts` gate sites, the tier map's 180 rows, and every pin instrumenting
  provenance. Value-class at minimum; its own break; full pre-freeze red
  enumeration; predictions for every gate whose membership could shift when
  DISPUTED stops being an ordering value.
- **Q-ALTERNATIVE:** the register already fixed each collision with PROSE — and
  the prose worked (no collision has recurred once its clause existed). The cheap
  ending promotes the clauses to standing law: a docs-side SEMANTIC RULE — "the
  tier rates the source, not the band; DISPUTED is a contestation label ordered
  below MEDIUM for gate purposes, and any ruling crossing the DISPUTED/MEDIUM
  boundary states which axis moved" — written once, cited thereafter, zero bytes.

## §3 — Outcomes

- **STANDING-SEMANTIC-RULE (the verifier's declared lean, carried as opinion;
  rule text and filing RULED at adjudication):** the rule lands in METHODS as
  its OWN SECTION — a semantic rule, not a sixth practice — in three clauses:
  **(1)** `confidence` rates source quality; DISPUTED records contestation among
  sources and is not a rung on the quality ladder. **(2)** The two are not in
  tension: absence of dispute through absence of alternatives is not agreement —
  unavailable, not agreed. **(3)** The gate consequence, stated because it is
  the live defect: DISPUTED currently gates below MEDIUM, so a well-sourced
  contested claim is excluded as if weakly sourced — **recorded as a KNOWN
  INCORRECTNESS in the encoding, not repaired.** The third clause is what turns
  a documentation fix into an honest one: without it the rule would read as
  "the field is fine, just explain it," and the field is not fine.
- **SPLIT-QUEUED-AS-VALUE-CLASS:** only if Q-COST's census shows collisions
  recurring or gate distortions with measurable consequence (a claim wrongly
  gate-excluded through DISPUTED whose exclusion moved a scored figure — none is
  currently known); the split then registers as its own future break under full
  discipline, and this item only rules it worth that, never executes it.
- **OBSERVATION-STANDS:** the census shows the cost already fully paid and
  non-recurring; the observation stays recorded, nothing else moves.

## §4 — Bound

No byte moves under this registration — scenario, engine, or scoring — under any
outcome. The DISPUTED tiers themselves are untouched (#137/#138/#166 correct per
O5). The −1.6879 residual, the standing findings, the checkpoint-scan exposure,
and the three queued WOs are untouched. Reading order: Q-COST's ledger census,
then the outcome ruling; no verdict before the census's instances are listed.
