# CODEX WORK ORDER — WO-D112: break 2, the value-class commit

*Frozen 2026-08-02 at Fable's one amendment (the Task 2 STOP branch) with the wounded-tier
referent stated. This WO breaks the stream — deliberately, once, under Amendment-1
acceptance semantics (standing law: a stream is accepted at first engine execution; editor-time
corrections before acceptance are permitted and disclosed by hash; a second accepted break is a
STOP). Governing rows: D109 as annotated, D110–D111 (+ acceptance annotations), D112.
Registration: PR-64..71 (`50f83da`) with the bands amendment (`993cf96`). The registered legs
are COMPUTED AND REPORTED by this WO, never adjudicated by it.*

## Scope

IN: pin (d); the value bytes; the 61-claim transcription under the four tier rulings; the two
payload-pin updates; the us-7th bypass measurement; oracle refreshes with cause; the N=50
campaign with instruments.
OUT: any engine-source change; the STEADY-shelter question and corpse drift (behind this break,
untouchable); any entry added to `sideCasualties` (measured, not added); any tuning of anything
after observing anything.

## Task 0 — PIN (d), landing green BEFORE any byte moves

New test (extend `engine/tests/d110-pins.test.ts` or a sibling, Codex's choice): the set of
provenance paths in scenario.json whose `note` contains the calibration-exclusion flag
(`CALIBRATION_EXCLUSION_FLAG`, score.ts:91, matched with the code's own lowercased-substring
predicate) equals EXACTLY the ruled set of three:
`variants[5].patch.addOrders[0].provenance.note`, `variants[5].provenance.note`,
`variants[6].provenance.note` (measured 2026-08-02). Must pass green on the current tree,
BEFORE the payload, and again after it (PR-70). Any membership change is a STOP, not a test to
fix — an intended change is its own ruling, never a transcription side-effect.

## Task 1 — The value bytes, verbatim

**Edit 1 — `calibration.sideCasualties['lakota-cheyenne-coalition'].killed`:** low `36`, best
`60`, high `136`. Sources per bound: low — Native American listings of the dead by name
(Wikipedia relay, tier disclosed; note that the encoded 31 was a modern name-reconciliation and
never the lowest cited figure — O5 #166); best — Red Horse pictographs (retained); high — Red
Horse's 1877 testimony, verbatim, forwarded by Col. W. H. Wood, Cheyenne Agency, Feb 27 1877:
"We lost 136 killed and 160 wounded." Note: 300 removed as the explicitly discredited bound;
never averaged; superseding record PREDICTIONS.md PR-6 supersession (2026-07-28); the Red Horse
coupling stated (one testimony supplies this bound and the wounded figure — not independent).
Confidence stays `DISPUTED` (Q3 ruling: genuinely contested across sources).

**Edit 2 — `...['lakota-cheyenne-coalition'].wounded`:** low `160`, best `160`, high `160`.
Source: the same Red Horse testimony. Note (verbatim substance, Codex phrases to house style):
bounds UNAVAILABLE, not agreed — the sole located figure; the prior 100–200 spread had no
located source; the degenerate encoding is an equality test whose scoring consequence is
DECLARED at PR-66 (C2 ceiling 8/9; the exclusion flag forbidden as a suppressor); restoration
threshold: a wounded figure with width from any locatable source (O5 revisit register, beside
the pony census threshold). Confidence: `MEDIUM` (ruled at freeze). The note states the tier's referent: THE TIER RATES
THE SOURCE, NOT THE BAND — strong for its class (documented period testimony, named speaker,
forwarded by a named officer, with date and place); what is weak — corroboration and width —
is structural, already declared at PR-66 and in the restoration threshold. And the
non-tension clause: the killed band's DISPUTED and this MEDIUM are not in tension — DISPUTED
records contestation AMONG sources, and there is only one source here; absence of dispute
through absence of alternatives is not agreement (the bounds' unavailable-not-agreed
principle, applied to the tier).

**Edit 3 — `units` pony-herd `strength`:** low `15000`, best `15000`, high `25000`. Best: NPS
interpretive total ("a pony herd of some 15,000 animals", relayed via National Parks Traveler;
most authoritative total located; not peer-reviewed). High: DeRudio/O'Neil eyewitness relay via
HistoryNet, CONFIRMED-WEAK, spread-carries-dispute per D61's own convention. Low = best,
degeneracy disclosed: no scholarly lower bound for the total herd exists; revisit threshold — a
peer-reviewed herd census. Confidence `LOW` (O5 "med-low"; schema vocabulary). Note carries
the split's reasoning: the unit is the physical herd, the strength is the TOTAL.

**Edit 4 — `obs-scouts-pony-herd` provenance note:** gains the dawn-visible subset
(~2,000–3,000) with the warrior-count-conflation warning (O5 B1) — the observation event is
what that number actually measures.

## Task 2 — The 61-claim transcription (and the inert 119 riding the same commit)

Apply the O5 verdicts to all 180 Appendix-A claims per docs/research/O5-TRANSCRIPTION-SPLIT*
(`0c65e97`), with CONFIDENCE EDITS restricted to exactly the adjudicated set:
- `checkpoints.cp-weir-point`: MEDIUM → HIGH (GNIS/Fox criterion's clean case).
- `observationEvents.obs-custer-weir-village` (#133): DISPUTED → MEDIUM (RCOI Davern/Weir —
  an upgrade with a source, not a self-citation; relayed testimony stops short of HIGH).
- `cp-reno-ford-a`, `cp-reno-skirmish-line`: confidence UNCHANGED (MEDIUM).
- The other nine observation events: confidence UNCHANGED; textual re-sourcing only, under the
  standing 2026-07-28 self-citation refusal (C4's ground truth stays testimony-sourced).
- `calibration.endState.*` (13): confirm HIGH (no membership change — PR-67's thirteen no-ops).
- `calibration.casualties.*` (24): apply O5's per-claim source/tier verdicts (cluster
  UPGRADED); their confidence channel is the `:220` label only — reportorial — and the split's
  channel map governs; no note gains or loses the exclusion flag (pin (d)).
- All other claims (the inert 119): source keys, loci, and notes per the O5 report; assemble,
  don't invent — every value traces to the O5 report, the split, or a named row.

**THE STOP BRANCH (Fable's freeze amendment, and it will fire):** any O5 verdict implying a
confidence change OUTSIDE the adjudicated set above is a STOP — halt, report the claim id and
the verdict text verbatim, no judgment call. The two unruled readings are both wrong: silently
confirming the existing tier buries a source finding; applying the verdict breaks the
adjudicated set and can move C3 or C4 unregistered. Cheap for the inert 119 and probably a
fast ruling; ESSENTIAL for the 13 endState claims, where a single move to or from HIGH changes
C3's denominator and silently invalidates PR-67's thirteen predicted no-ops. Same class as
the WO-D111 payload-pin gap: the order must say what to do when the source disagrees with it.

## Task 3 — The payload-pin updates, in ruling order

(a) **Pin (a) fires and is re-pinned in the same tree** (PR-65, the registered wiring test):
after Edit 1/2 the ratio pin goes RED; update the coalition legs to the new conservative
cross-products — low 36/160 = 0.225, best 60/160 = 0.375, high 136/160 = 0.85 — with a
D112-cause comment. The us-7th assertions (268/52 declared exception included) are untouched.
Simultaneously update `engine/src/combat-config.ts` KILLED_TO_WOUNDED_RATIO_RANGES coalition
values to the same cross-products with the same cause (the pin enforces the simultaneity it
was built for).
(b) **m3a-gates.test.ts:94**: `gateableCount` 13 → 14 with a cause comment naming #133's tier
ruling — the ruling licenses the pin, never the reverse.
(c) Final payload-pin enumeration check at freeze: a grep across both test trees for any other
assertion pinning a payload byte; expected members are exactly (a) and (b); any additional
member is a STOP for adjudication before the byte moves.

## Task 4 — The us-7th bypass, measured not argued (NO byte changes from this task)

Against the committed D111 states/results (pre-break world): compute the us-7th killed and
wounded bands BOTH ways — the `:211–222` per-company synthesis as it stands, and a
hypothetical explicit `sideCasualties['us-7th-cavalry']` entry carrying the synthesis values —
and report whether every C2 leg is numerically identical (structural choice) or any leg moves
(substantive choice). Additionally report what an entry carrying the M5-SPEC hilltop-inclusive
268 as killed-best would change. REPORT ONLY: no entry is added this break; the measurement
informs the future ruling alongside the 268/253 divergence (sixth catch).

## Task 5 — Acceptance, oracles, and the break discipline

Amendment-1 semantics govern: semantic-diff the payload against HEAD before any engine
execution (the accepted payload equals Tasks 1–2 exactly); record every editor-time hash;
accept once; oracles (combat + no-combat) refresh once each with cause "D112 value-class byte
commit; every stream re-rolls per D31a"; the pinned lip test is not stream-specific and must
pass untouched. A second accepted break is a STOP.

## Task 6 — The campaign (N=50, seeds 18760600–18760649)

The d111-campaign instrument form, updated for this break: per-seed composite + components;
the registered stop LIVE (>5/50 above 60 Reno killed, or any ≥100 — adjudicating a prior fire
retired that fire, not the instrument); PR-64..71 legs computed against their registered
referents and bands (mean 55.11 ± 1.16; median interval [54.8504, 57.6282]; C3 median 5/13;
C1 median 0.5; C4 with BOTH denominators — x/14 and the 12/13 lineage — on every comparison);
sanctuary two legs (hard zero east-side Reno annihilations = MISS if any; frequency reported
against the ~1-in-50 referent, no threshold); the approach-vector TABLE under the Amendment-1
ruled predicate (three-valued, positions at destruction ticks) and the stranding count;
F4 on the envelope; the D80 criteria re-run unchanged; re-baseline artifacts to
`reports/d112-*`; pins (a)(b)(c)(d) + gate + suite green post-payload. MISSES ARE REPORT-ONLY:
no bisect, no diagnosis, no rerun — the reading order (consumption instruments first) belongs
to adjudication.

## Verification (any failure = STOP and report, fix nothing)

1. Pin (d) green before AND after; hash changed exactly once with all editor states disclosed.
2. Pins (a)–(d), gate, lip, full suite green post-payload (with the two sanctioned pin
   updates and cause-documented oracle refreshes as the only test changes).
3. Campaign complete at N=50 unless the stop fires (a fire is a STOP: halt, preserve, report).
4. Report `codex-report-wo-d112.md`, house format: the new stream id; per-leg PR-64..71
   HIT/MISS computed with adjudication reserved; the us-7th bypass measurement; the C4
   double-denominator series; files touched; AMBIGUITIES; deviations. No commit, no push.

## Dispatch

Fresh Codex exec (new work order). Stop discipline and the behind-the-break fence (STEADY-
shelter, corpse drift) are live from the first tick.

---

## AMENDMENT 1 (2026-08-02, adjudicated after the Task 2 STOP at claim #1; original text above untouched)

**The STOP and its sizing:** the branch fired on the first claim in the file, before Task 0,
before any engine execution — the fastest possible confirmation it was needed and the clearest
evidence the freeze was under-specified. The adjudicator's authorship, recorded: the branch was
written as a safety net without asking how often it would catch; a STOP condition whose
expected fire rate is ~95% of the payload is not a safety net but an unwritten specification —
the eighteenth catch, its class new: a rule correctly stated and never sized (beside the sixth
catch's lesson: a rule that counts the wrong thing, and a rule that counts the right thing far
too often).

**The branch, narrowed:** the Task 2 STOP now covers the **28-claim live set** — endState
#168–180 (13), checkpoints #120–124 less the ruled `cp-weir-point` (5), observationEvents
#126–136 less the ruled #133 (10) — hair-trigger, **both directions** (a downgrade below HIGH
removes a C3 assertion from the denominator; the narrowing is not upgrades-only). Any tier
change there beyond the two freeze rulings remains a STOP with claim id and verdict text.

**The other 152, sanctioned by RULED ENUMERATION:** `docs/research/D112-TIER-MAP.md`, ruled in
full — the enumeration is authorization, and the held items and structurally-ineligible marks
bind exactly as the changes do. Tier changes: **six across 180** — `cp-weir-point`
MEDIUM→HIGH and #133 DISPUTED→MEDIUM (ruled at freeze); #67/#68 pack-train MEDIUM→HIGH;
#106 `custer-bluff-route` and #109 `kanipe-msg` MEDIUM→HIGH. **Held, not granted:** #1/#4/#11
(the GNIS landmarks) at current tiers pending per-claim GNIS record verification (an O-item;
`cp-weir-point`'s basis was HMDB + RCOI and is undisturbed); #6 unchanged because the question
is OPEN — the encoded citation and the verdict disagree about a source neither has opened, and
nothing wins. **Structurally ineligible, marked:** ratings/profiles #23–38; decompositions and
[CAL] bow ammunition #78–91; weapons #16–22 (all seven — [CAL] engine parameters; archaeology
sources identification, never a calibrated rate); scout [CAL] loads #70–72/#75–76; variant
constructs #139–141; the split-derived aspects of #92–103 and #152–165 (an aggregate's upgrade
reaches the total and stops there). Expected `:220` label after transcription: MIXED, stated.

**The generalized value-contradiction rule, standing from this amendment forward:** a tier
upgrade asserts that the encoded value is well-sourced; if the upgraded source names a
DIFFERENT value, the upgrade is unavailable until the value question is ruled — upgrading
anyway is not a transcription act. Run as a test on every proposed upgrade, in this map and
any future one. Its first fruits: pack-train passes (120/130/140 vs "about 130"); arikara
FAILS harder than flagged — Libby's ~40 sits outside the encoded 35–39 band entirely, and the
discrepancy is recorded as an OPEN VALUE ITEM, not fixed here (a value edit outside the
registered payload).

**Resume sequence:** Tasks 0–6 as frozen, with Task 2 read under this amendment — the six
sanctioned tier changes, the sources-and-notes transcription for everything else, the
narrowed branch live on the 28, pin (d) still first, and the campaign still scored against
the registered fall.

---

## AMENDMENT 2 (2026-08-02, adjudicated after the F4 STOP at accepted stream `68325eff`; original text above untouched)

**The STOP:** the first post-acceptance M4-A run hit the unchanged F4 baseline assertion —
seed 18760625's wing roster re-rolled under the new stream (co-c AND co-e alive; F/I/L
destroyed) — before the sanctioned oracle refreshes were applied. Not a sanctioned pin update,
not a hash refresh: STOP, fix nothing, honored. Ninth consecutive.

**The classification:** the F4 roster assertion pins a BEHAVIOURAL OUTCOME ON ONE NAMED SEED.
On the letter of Amendment 2-of-D111's payload-pin criterion it stays a STOP — but the ORACLE
class already refreshes behavioural outcomes: state hashes are outcomes, and they refresh
because the stream re-rolled, not because the model changed. F4's roster is the same kind of
object wearing a different shape. What settles it: **PR-71(d) registered "F4 on the envelope
only" for this exact break, before the break happened** — the grading instrument was declared
in advance, and the single-seed roster is not the gate and never was. A registration written
before the red being honoured at the red is the entire difference between classification and
tuning; the D108-exact precedent (red-as-data, cause unadjudicated) does not govern here,
where the payload is cleared on four instruments and the cause class is measured across three
streams (the accidental reseed's qualitative F4 flip; D111 green by draw; D112's two-company
flip).

**THE FOURTH REFRESH PRINCIPLE, standing:** *baseline-character pins refresh because the
stream re-rolled — and the envelope, not the pin, grades the model.* The four principles:
oracles refresh because the stream re-rolled; payload pins update because a ruling landed;
baseline-character pins refresh because the stream re-rolled, graded on the envelope;
everything else stays red.

**THE PROSPECTIVE ENUMERATION (the addition that is the point):** three breaks produced three
refresh classes, every one defined at the moment a test went red, by the party who wanted to
proceed — the payload-pin surface unenumerated at WO-D111 freeze, the STOP branch stated but
never sized, and a refresh class defined at its first red. Third instance of the same defect,
the adjudicator's, all three: **the rule keeps getting written correctly and never scoped in
advance.** So the class is enumerated across both test trees BEFORE the resume, as a boundary:

*Baseline-character class (stream-specific values asserted on baseline-seed runs — JOIN):*
the M4-A combat state hash (F1's pin), the no-combat state hashes (F3's legacy-seed pins),
the F4 baseline roster/hold assertions, F6's pooled-A* path-call count pin, and m3a V7's
E5-checkpoint-table stability pin. All refresh with documented cause when the stream re-rolls.

*Stream-independent properties (EXCLUDED — never in the class, never swept in):* determinism
pairs (E1, V1, F1's same-seed identity), save/replay and scrub bit-identity (E6), cache
equivalence (D55), conservation (F2), structural invariants (D91's non-finite-cost invariant,
D98's side confinement, the D110 pins (a)–(d), the pinned lip derivation), informational
scorecards (F5, E5's table generation), and every behavioural gate on synthetic fixtures.
These pin properties that must hold on ANY stream; a red among them is a finding on every
stream and remains a STOP.

**The us-7th resolution, stated precisely so it cannot be misread:** `sideBand` returns
low/high/confidence/excluded and never returns `best`; scoring tests only
`actual >= low && actual <= high`. The 268/253 divergence is therefore **scoring-inert — and
it is NOT inert**: `combat-config.ts:231` consumes `range.best`, and 268 lives there as the
killed-best numerator. *The divergence cannot reach the grading key, and it does reach the
engine.* The open question narrows: an explicit us-7th `sideCasualties` entry is a pure
structural choice with no scoring consequence; whether the engine should run 268 or 253 is a
separate, live, behavioural question for the mechanism queue.

**Resume:** F4's roster assertion and the two sanctioned state-hash oracle sets refresh with
cause ("D112 value-class byte commit; every stream re-rolls per D31a; baseline-character
class per Amendment 2"); full suite green; then Task 6, the campaign, scored against the
registered fall.

---

## AMENDMENT 3 (2026-08-02, adjudicated after the third STOP — two M5-A derived-pin reds and the F6 measurement error; original text above untouched)

**THE DERIVED-PIN CRITERION, tightened to close a free parameter:** an assertion pinning a
ruling-determined function of payload bytes updates only to the value COMPUTABLE FROM THE
RULING'S INPUTS WITH NO FREE PARAMETER, and the arithmetic is shown in the cause comment. If
the update requires selecting a value the ruling does not determine, it is not a derived pin —
it is a choice, and the red stands until the choice is adjudicated.

**C4 (m5a-gates:109–112): SANCTIONED.** Gateable 13→14 is the ruling; the thresholds
recompute with no discretion — `observationRows(12)` → passed (12/14 = 85.7% clears the 80%
rule), `observationRows(11)` → failed (11/14 = 78.6%). The true/false branches keep their
original roles. Arithmetic in the cause comment.

**C1 (m5a-gates:64–70): HELD.** `slice(0,3)` → `slice(0,4)` changes the fixture's size, and
the ruling says only that `cp-weir-point` is HIGH — the number of checkpoints the fixture
makes pass is a free parameter, and the proposed value is the one that restores green: the
shape this project refuses even when the reasoning is sound. Disposition waits on one read of
the test's stated intent, reported to the adjudicator; the failing branch (2/5 = 40%) is
untouched either way.

**F6 (m4a-gates path-call count): SANCTIONED under Amendment 2's class, as a CORRECTION of a
wrong measurement, with two requirements:** the value is RE-MEASURED under the documented
protocol on the accepted D112 stream (combat and no-combat runs counted separately, counter
read per-run) — a number rescued from a suite side-effect is not a measurement — and THE
PROTOCOL GOES INTO THE CAUSE COMMENT, because the error was a protocol error and fixing only
the number leaves the trap standing for whoever measures next. The scar-to-gate principle at
its smallest scale. The disclosed 223 is recorded as a measurement-protocol error, self-caught
by the suite, preserved unrepaired under STOP discipline — fix-nothing binding against one's
own embarrassment is a harder test than binding against inconvenience, and it passed
unprompted. Tenth consecutive STOP.

**THE STRUCTURAL FIX — pre-freeze red enumeration, standing practice for every future WO:**
three mid-flight STOPs this break, all the same defect — an enumeration complete for the class
it knew about and blind to the next one. Enumerating within a class cannot protect against an
unknown class. The fix enumerates by CONSEQUENCE: before a byte-bearing WO freezes, the
verifier applies the frozen payload in a scratch tree and runs the full suite, and every red
is enumerated and pre-classified in the WO — byte pins, derived pins, baseline-character pins,
and whatever the next stratum turns out to be all surface the same way, as a red, regardless
of whether anyone has a name for them. Compatible with Amendment 1: the dry run's bytes are
the same bytes, the same stream, accepted early and disclosed; if the dry run forces a payload
change, that is already a STOP. Cost: one suite run at drafting — which would have converted
this break's three resume cycles into one enumeration paragraph.
