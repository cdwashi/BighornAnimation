# IMPLEMENTATION_HISTORY — Historical Battle Simulator

Authoritative record of design objectives, decisions, and session history.
Maintained across sessions. Newest entries appended at the bottom of each section.

- **Project:** Historical Battle Simulator (working name)
- **First scenario:** Battle of the Little Bighorn, June 25, 1876
- **Stack:** TypeScript. Headless engine + Next.js UI (mirrors the Mah Jongg Simulator architecture)
- **Verification standard:** quartet — typecheck · tests · build · lint (eslint-plugin-react-hooks)
- **Record started:** 2026-07-14

---

## 1. Design Objectives

1. **Primary experiential goal:** let the user "see what Custer saw" — a first-person
   viewshed from any commander's position at any minute, rendering what was and was
   not visible (terrain masking, haze, dust). The Little Bighorn is treated as a
   terrain-visibility story; the LOS model is the thesis of the project, not decoration.
2. **Simulation, not choreography:** an order-driven engine resolves movement, line of
   sight, engagement, morale, and casualties itself. Historical fidelity is achieved by
   feeding historical orders and calibrating parameters — not by scripting positions.
3. **Controllable counterfactuals:** once calibrated, parameters (leader ratings, order
   timings, strengths, variant interpretations) become real knobs. Changing them shows
   a plausible alternate outcome.
4. **Battle-agnostic engine, data-only battles:** a new battle is a new scenario JSON
   file against a fixed schema. No battle-specific code. (Same pattern as the NMJL
   card data feeding the Mah Jongg engine.)
5. **Honest history:** provenance, confidence ratings, and source citations are
   first-class data. Disputed history is never silently blended; competing published
   interpretations are labeled, toggleable variants.
6. **Match the historical timeline** on a simulated clock (Gray local-sun chronology
   for Little Bighorn), with playback controls: scrub, speed, unit inspection.

## 2. Decision Log

| # | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| D1 | 07-14 | Simulation approach: **order-driven** (option 2 of 3: choreographed / order-driven / fully emergent) | Choreography makes knobs meaningless; fully emergent AI stops being the historical battle. Order-driven gives real simulation constrained by historical orders, calibrated against checkpoints. | Approved |
| D2 | 07-14 | First battle: **Little Bighorn** | User's stated interest (Custer's miscalculation); battle is fundamentally a terrain-visibility story, ideal for the LOS engine; preserved battlefield → modern DEM ≈ 1876 terrain; exceptionally deep scholarship incl. physical archaeology. | Approved |
| D3 | 07-14 | **Provenance is first-class** in the schema: disputed quantities are `Estimate {low, best, high}`; orders/checkpoints/etc. carry `confidence` + `sources` | Averaging contradictory accounts can produce positions nobody was ever at. Weighted reconciliation with archaeology > time-motion scholarship > official records > native testimony > reminiscence. | Approved |
| D4 | 07-14 | **Orders drive, checkpoints score.** Checkpoints are calibration rubric only; the engine never teleports units to them | Keeps it a real simulation; calibration score = weighted checkpoint hits + casualty match + end-state + observation events. | Approved |
| D5 | 07-14 | **Disputes are variants:** labeled overlay patches (add/remove/modify orders, checkpoints, units), mutually-exclusive sets supported; never blended | More honest and more interesting: user can watch competing interpretations play out on the terrain. | Approved |
| D6 | 07-14 | **ObservationEvent** type: documented sightings AND failures-to-sight, weighted in calibration | Validates the LOS model itself against history (Crow's Nest haze; village extent masked from Reno; Custer field terrain-masked from Reno Hill). Core to the experiential goal. | Approved |
| D7 | 07-14 | Per-side **command models**: HIERARCHICAL (7th Cav: courier delays, order friction) vs CONSENSUS_INITIATIVE (coalition: leader influence + individual initiative, per Fox) | The two sides genuinely fought under different command cultures; modeling this is part of explaining the outcome. | Approved |
| D8 | 07-14 | **Weapons as stepwise range-band tables** (+ malfunction estimate), not formulas | Research data (period tests, archaeology) maps directly into bands; tuning is data editing, not code changes. Carries the Springfield extraction debate as an Estimate. | Approved |
| D9 | 07-14 | **TacticsProfile** behavior weights incl. `targetHorses`, `infiltration`, `dispersion`, `withdrawalDiscipline`; horse-holder fraction (1-in-4) modeled | Encodes doctrine (US skirmish drill) vs Fox's warrior tactical model (infiltration, suppression + rushes, horse targeting). | Approved |
| D10 | 07-14 | Research gathered via **Deep Research prompt engineered to the schema** (deliverables A–K map to schema types; hard conventions: Gray sun time, WGS84, low/best/high, 4-level confidence scale, never-average rule) | Research output becomes near-mechanical to transcribe; disputes arrive pre-labeled for `variants`. | Executed — output received 07-14 |
| D11 | 07-14 | **Clock window:** start 03:00 (Crow's Nest approach), end ~21:00 June 25. Coarse ticks for the approach phase, fine ticks from the ~12:00 division onward | Captures the dawn Crow's Nest viewshed moment inside the sim clock rather than as a bolt-on vignette. | Approved by Chuck |
| D12 | 07-14 | **Baseline interpretation = archaeology-weighted (Gray/Fox):** MTC feint (F1), unequal wings / no C-split (F2), tactical disintegration (F3), E Co in Deep Ravine (F4). Traditional readings (serious crossing, organized last stand) + Reno-timber and Weir-initiative disputes ship as named variants | Follows the declared source hierarchy; physical evidence outranks testimony where they conflict. | Approved by Chuck |
| D13 | 07-14 | **Native strength defaults:** warriors 900 / 1,750 / 2,500 (low/best/high); armed fraction ⅓ / 0.40 / ½; repeaters 25–30% of armed | Matches research "best working figure 1,500–2,000"; all sweepable — sets the default run only. | Approved by Chuck |
| D14 | 07-14 | **Warrior unit decomposition:** one defense-pool WARRIOR_BAND per village circle (strength ∝ lodges) + named-leader bands for documented axes (Gall south/Calhoun; Crazy Horse north sweep; Crow King ~80; Lame White Man's charge) drawing from circle pools | The coalition did not fight in discrete units; this is acknowledged model invention. Every such unit marked LOW confidence with rationale in provenance. | Approved by Chuck |
| D15 | 07-14 | **Leader ratings (0–100)** proposed by Claude from documented behavioral evidence, evidence quoted in `ratingsProvenance`; Chuck reviews the numbers explicitly | Research deliberately supplied evidence, not numbers; the numbers are argued opinions and are flagged as such for PR-style review. | Approved — ratings approved as proposed (Chuck, 07-14); O2 closed |
| D16 | 07-14 | **Weak citations recorded faithfully** (Wikipedia, forum-tier, "subagent" relays) rather than laundered | Honest accounting; flags where re-sourcing is warranted before publication-grade claims. | Approved |
| D17 | 07-14 | **schema v0.1.1** (weaponMix sum rule removed, RIFLE_BREECHLOADER added, tickPhases deferred). | First contact with real data exposed the sum-rule bug. | Approved |
| D18 | 07-14 | **O1 split** Fable froze all judgment into TRANSCRIPTION-DECISIONS, Codex assembles + builds the harness, Fable reviews. | Judgment/typing split per workflow. | Approved |
| D19 | 07-14 | **Post-O1 review rulings R1–R9:** DEM south bound 45.42 ratified; civilians-interpreters kept as 16th US unit (flagged); camp strengths via lodge-proportional method (5,250 across six camps); pack-train mix 0.8/0.8 + GRAHAM source key; scout ammo mirrored from tables; courier orders target co-f; checkpoint representative mappings ratified; blackfeet-santee position interpolated; empty variant exclusions confirmed correct. Full detail: codex-report.md AMBIGUITIES + CODEX-WORKORDER-O1B.md. | Codex ask-don't-guess flags → Fable rulings; most traced to spec bugs owned in-session. | Approved; O1 closed |
| D20 | 07-14 | DEM: USGS 3DEP 1/3 arc-second, TNM API, bounds sw(45.42,−107.48) ne(45.60,−107.15) | Free, seamless, adequate for battalion-scale LOS; preserved battlefield ≈ 1876 surface. 1 m lidar noted as upgrade path. | Approved |
| D20a | 07-14 | Two-tier grid: core battlefield 10 m, full box (approach + Crow's Nest) 30 m | Full 10 m ≈ 10 MB asset; two tiers keep the static app light while serving both phases. | Approved |
| D21 | 07-14 | Project to UTM 13N; engine in local meters, origin SW corner | No degree math in the engine; authoring stays WGS84. | Approved |
| D22 | 07-14 | Contours: 5 m interval, 25 m index | ~100 m total relief; 10 m too sparse to read coulees. | Approved |
| D23 | 07-14 | LOS requires earth-curvature + refraction correction (k=0.13) | Crow's Nest sighting is ~24 km; raw curvature drop ~45 m exceeds village-site relief. Gate G3 proves it live. | Approved |
| D24 | 07-14 | O3 protocol: adjudicate Crow's Nest candidates by viewshed-scoring against documented dawn observations | HIGH-confidence observation events legitimately adjudicate a disputed input coordinate. | Approved |
| D25 | 07-14 | O4 Tier A for v1: river/timber corrections from NPS GRI figures + Maguire 1876 map, LOW–MEDIUM flagged; Tier B (QGIS georeferencing) backlogged | Ships honest corrections now without blocking M1. | Approved |
| D26 | 07-14 | Integer-strength rule: all unit strength Estimates (incl. variant-added/modified) are whole men; splits use parent-keeps-remainder (co-c 19/20/23, co-c-det 19/20/22). Enforced by new data-integrity assertion. | Found via O1b review (22.5-man bug); bug becomes a permanent gate, per house pattern. | Approved |
| D27 | 07-14 | Marker-relocation convention: marker-sourced landmark coords may move to the DEM terrain feature when DEM+EPQS confirm, historical function requires it, and provenance keeps both. Applied: sharpshooter-ridge → 45.5336,−107.3927 (crest, 1039 m), MEDIUM. | HMDB marker sits on the flank (1025 m), below Reno Hill; ridge must command Reno Hill per plunging-fire accounts. Found by gate G2; second instance of the Crow's Nest-decoy pattern. | Approved |
| D28 | 07-14 | Movement-cost slope factor 1+tan(slope), [CAL] placeholder; Tobler's hiking function noted as M5 upgrade candidate. | No formula in spec; monotonic and adequate pending calibration. | Approved |
| D29 | 07-15 | Large derived vector assets: commit compressed (.br) variants only; gitignore raw derived files >5 MB; loader reads .br. Pipeline additionally caps GeoJSON coordinate precision at 5 decimals (~1 m — ample for 5 m contours). | 43 MB raw GeoJSON bloats every future clone; GitHub warns at 50 MB. Raw remains reproducible via `npm run terrain`. | Approved |
| D30 | 07-15 | Engine in `engine/src/`, headless and pure: no React/DOM/Node-only APIs in hot path (terrain loader injected); no `Date.now()`/`Math.random()` — all randomness via seeded PRNG; no key-order-dependent iteration; pure functions over explicit state. Root `tsc -b`, no workspace restructuring. | Determinism and testability; mirrors the Mah Jongg headless-engine pattern. | Approved |
| D31 | 07-15 | State/replay: event-sourced canon (re-run from tick 0 is canonical), keyframe snapshots for UI scrubbing only. Save = scenario id + content hash, variant ids, parameter overrides, seed, target tick; load refuses on scenario-hash mismatch. Typed engine events feed UI index and tests. | Full-day replay is sub-second (2,160 ticks × ~33 units); hash check inherits the Mah Jongg save-integrity rule. | Approved |
| D31a | 07-15 | PRNG: mulberry32, seeded from scenario + user seed; PRNG state serialized in SimState. | Substrate established before M4 needs combat rolls; replay stays bit-identical. | Approved |
| D32 | 07-15 | Speed table as engine config constants, all [CAL]: cav walk 1.8 m/s, trot 3.6, gallop 5.4; dismounted skirmish 1.1; pack train 1.2 (caps co-located escort); warrior mounted = cavalry parity [CAL]; on foot 1.3. Effective = base × cost-grid factor × formation modifier (COLUMN 1.0, LINE 0.8, SKIRMISH 0.7, DISPERSED 0.9). Fords: impassable river except ford cells + crossingPenaltyMinutes hold. Fatigue deferred to M4. | Standard cavalry-manual rates as starting values. **Chuck: approved as-is; tune later against the E5 report if needed.** | Approved |
| D33 | 07-15 | Pathfinding: A* on core-tier movement-cost grid, 8-connected, √2 diagonals, cost = step distance × mean adjacent cell cost; string-pulling smoothing; paths cached per order; 30 m tier when a path exits the core box. Units are points at battalion scale (no friendly collision in M2). | Grid already exists from M1; caching keeps the full-day run trivial. | Approved |
| D34 | 07-15 | Order lifecycle: issued → in-transit (transmissionMinutes + issuer orderDelayMinutes) → received → active → done/superseded. **Supersede-on-receipt: a newly received order replaces the active order; no queueing.** `order-superseded` audit event emitted. Pursuit objectives (targetUnitId): repath every 10 ticks or on >250 m target displacement; park at 150 m standoff emitting `contact-pending` (M4's engagement seam). | Queueing can silently reorder history when transmission delays overlap; supersede keeps the timed order list authoritative and matches how verbal battlefield orders worked. **Chuck: supersede approved.** | Approved |
| D35 | 07-15 | Couriers are delivery-queue timers in M2 (data transmissionMinutes authoritative); queue entries carry route endpoints so M4 can upgrade couriers to killable entities without schema change. | Honors FR4's courier-loss requirement later without blocking M2. | Approved |
| D36 | 07-15 | M2 executes scheduled orders only; DEFEND_CAMP and tactics-profile trigger behaviors dormant until M3 provides spotting. Scheduled coalition orders (gall-response, gall-calhoun, crazy-horse-sweep, ch-strike, lwm-charge) run as movement. | Trigger condition is "enemy spotted," which requires LOS; running them blind would fabricate behavior. | Approved |
| D37 | 07-15 | Movement-only checkpoint pre-score (gate E5) is informational, non-blocking: report emitted via CLI runner (`npm run sim`); misses expected pending M4 combat delays, but gross misses (wrong side of river, hours-scale error) investigated before M2 closes. Keyframe interval for UI scrubbing: every 10 ticks (5 min). | E5 is the baseline M4 must beat; gross-miss clause catches engine bugs hiding behind "combat will fix it." **Chuck: 10-tick keyframe default approved.** | Approved |
| D38 | 07-15 | HOLD with an objective means "proceed to objective, then hold position" (amends M2-ENGINE-SPEC §6, which said stationary). Engine change only; verified safe for all other HOLDs in data and variants (both degenerate to stationary). | E5 gross-miss root cause: right-wing-ridges froze co-c/i/l at Cedar Coulee for 5+ hrs; plain military reading of the order was inexpressible. Found by gate E5 + escalation rule. | Approved |
| D39 | 07-15 | Add MOUNT order: id reno-mount, minute 749, issuer reno, recipients co-a/g/m, transmission 0, historicalText "Mount!" — split from reno-retreat per research §E verbatim commands. 749 (not 750) guarantees the 2-tick state change completes before reno-retreat's WITHDRAW arrives under D34 supersede. Order count 22→23; data-integrity expected-count test updated; TRANSCRIPTION-DECISIONS §8 addendum noted. | E5 gross-miss root cause: the rout ran dismounted (+195 min to Reno Hill). Transcription refinement of recorded commands, not invention. | Approved |
| D40 | 07-15 | M2 closed with Reno Hill +95.5 min documented as a placeholder-river artifact (three mechanisms: southern-endpoint walkaround, retreat-crossing ford ~2 km off channel, timber objective on the channel line — all pre-flagged "pending O4/O6"). Resolution deferred to O4 Tier A; E5 re-baselines via `npm run sim` when real geometry lands. Escalation adjudicated: not engine, not D32. | Engine met every M2 exit gate; miss is known-placeholder data with an approved fix path (D25). Holding M2 open buys no information. D32 speed table survives second contact untuned. | Approved |
| D41 | 07-16 | Tier A channel: 298-point DEM-thalweg trace replaces the 4-point placeholder, and becomes the river-1876-channel correction geometry. Confidence MEDIUM as 1876 meander-belt proxy per Martin (2010) via NPS GRI 2011; Garryowen loop and Ford B reach flagged LOW (documented 1876 deltas). Validation: 0 elevation reversals over 1,192 rows, monotone 957.9→921.2 m descent, sinuosity 2.09, passes 4 m from the USGS gauge. | USGS hydrography APIs robots-blocked; DEM thalweg is self-contained, validated, and uses committed data. Dissolves all three D40 mechanisms. | Approved |
| D42 | 07-16 | Ford relocations: ford-a 45.49473,−107.39305 validated (MEDIUM); ford-b → 45.5468,−107.4164 snapped to channel (LOW — the 1938-realigned marker sits 336 m east; GRI documents realignment from the 1876 channel; exact 1876 point unknown); retreat-crossing → 45.52081,−107.38558, channel point nearest Reno Hill at 461 m (MEDIUM — matches bluff-base retreat and Water Carriers Ravine accounts). | Closes M1-A ambiguity #4. | Approved |
| D43 | 07-16 | Tier A timber polygon (loop bend 45.526–45.534 + 250 m east-bank belt) and village polygon (600 m west-bank strip, Garryowen reach → Ford B) at LOW; timber landmark relocated to loop centroid per D27 convention. Maguire-map georeferencing remains the Tier B upgrade. | Per D25; extent approximated from channel geometry, GRI terrace description, and accounts. | Approved |
| D44 | 07-16 | cp-reno-skirmish-line and cp-reno-timber checkpoint positions relocated to corrected timber geometry; times and tolerances unchanged; provenance records both coordinates. | Source-geometry correction of APPROX-flagged, LOW-confidence checkpoint coordinates that we now know sit ~1 km east of the actual river — not tuning to fit a run. Explicitly human-approved (Chuck, 07-16) because checkpoints are calibration ground truth. | Approved |
| D45 | 07-16 | cp-yates-ford-b checkpoint relocated to the D42 ford-b coordinate (45.5468,−107.4164); tolerance (150 m) and time unchanged; provenance records the 1938 marker coordinate, the GRI-documented realignment, and the Fox-archaeology caveat (finds anchor to the stable coulee-mouth approach; exact 1876 crossing within the reach is LOW). | Checkpoint and its order objective must reference the same physical crossing; the marker is documented as not the 1876 river. Marker-vs-feature convention (D27, D44 lineage). Flips MISS→HIT — hence an explicit human ruling, not a silent edit. | Approved |

| D46 | 07-16 | Deterministic threshold spotting for v1 (no RNG consumed; M4 combat gets first dice); detectability = angular size × transmittance × motion × perception with gain/lose hysteresis; per-observer event log with side-level believed-picture aggregation (v2 per-commander guard). All params [CAL] in one global table. | Probabilistic spotting would make the C4 exam flaky; per-observer data preserves the v2 upgrade path without schema break. | Approved |
| D47 | 07-16 | DEFEND_CAMP activates: believed enemy within 3 km [CAL] of an own camp → idle pool bands interpose; scheduled orders remain authoritative for named bands. | D36 lifts now that spotting exists; the village's reactive defense becomes emergent, the timed axes stay reconstruction. | Approved |
| D48 | 07-16 | Viewshed = radial-ray raster at display resolution sharing the M1 ray core (curvature + cover + atmospheric), worker-computed, cached per (observer, tick); renderer/engine ray parity enforced by gate V5. | One ray implementation — the overlay must never disagree with the engine. | Approved |
| D49 | 07-16 | C4 exam: Crow's Nest events excluded pending O3 (they are the D24 adjudication instrument); remaining HIGH/MEDIUM events gate at ≥80%; tuning legitimate only via the single global table — per-event fudges are gate-weakening. | The events are LOS ground truth; the ruler cannot also be the thing measured. | Approved |
| D50 | 07-16 | M3 ships the first app shell (map, scrubber, POV overlay, belief-vs-reality toggle, decision index auto-generated from orders — chronological); polish deferred to M6. | A viewshed without a screen is a report, not an experience; decision index costs zero new data. | Approved |
| D51 | 07-16 | Partial-visibility observation rows split into per-target sub-assertions against individual camp units (transcription refinement, D39 precedent, provenance retained). | Makes "Reno saw only the southern edge" mechanically testable. | Approved |
| D52 | 07-16 | ObserverPosition for all six D51 sub-assertions refined from Ford A (water level) to the D44-corrected valley skirmish-line coordinate, mounted height, same minute — applied to the full observation moment as one unit; both coordinates in provenance; exam re-run accepting ALL resulting verdicts, including any new failures on the north-camp rows. | Research row "Reno @ Ford A / valley" conflated two positions; the sighting historically belongs to the valley advance (Reno halted because he saw the massing). Source-semantics correction (D44/D45 lineage), ruled after the gate passed 9/11 — refinement, not rescue; D49's repositioning ban binds tuning, not sourced corrections. | Approved |
| D53 | 07-16 | Six camp-unit centers + dependent pool starts relocated into the D43 village strip at researched S→N ordering latitudes; village-s-end and village-n-end landmarks move to the strip ends (order-objective coherence, D45 principle); both coordinates in provenance, LOW. | C4 exam exposed O1's APPROX centers sitting 0.8–1.5 km west on the terrace bench (18 m above valley floor) — outside our own village polygon; the S→N ordering is the firm research fact, the centers never were. D44/D45 lineage. | Approved |
| D54 | 07-16 | Cover transmittance becomes path-length-dependent (Beer–Lambert): ∏(1−losOpacity)^(cellPathMeters/L0), L0=100 m [CAL]; losOpacity redefined as opacity per 100 m of path. Global re-tune under D49; exam re-run with all verdicts accepted as they fall (D52 integrity constraint now standing policy). | Per-polygon attenuation (M3-SPEC §2 — Fable's spec defect, owned) provably admits no global threshold: Weir pass requires T≤1.4e-3 while north-camp masking requires T>9.1e-3. Path-length law makes "bends and timber mask the extent" expressible as physics. | Approved |
| D55 | 07-16 | Engine cache-purity rule: all memoization must be a pure function of current SimState; history-dependent auxiliary state is forbidden (D31 invariant made explicit). Applied: spotting LOS cache re-keyed by 100 m-quantized endpoint cell pairs. New permanent gate: cache-equivalence test — full-day run with caching disabled vs enabled must be bit-identical. Invalidation-semantics change may shift spotting verdicts by a tick; exam/E5 re-run with verdicts accepted per standing policy. | Found by E6 going red after D53a's track change; latent through all prior E6 passes (scrub-point luck). Defect originated in Fable's M3-A work-order cache sentence — owned. Bug becomes rule becomes gate, per house pattern (D26, D29 lineage). | Approved |
| D53a | 07-16 | reno-advance objective changed from raw waypoint {45.52,−107.43} (stale terrace coordinate) to landmarkId village-s-end; sole stale-coordinate order (verified by count). E5 co-a track change accepted as it falls, diff itemized. | Completes D53's order-objective coherence: the raw coordinate is how the staleness occurred; the landmark reference is self-healing against future geometry corrections. Checkpoints untouched — unit track changes are source coherence, not tuning. | Approved |
| D56 | 07-17 | Engine full-day reconstruction re-targeted: ≤ 6 s with worker progress indication (was "<1 s" — an unvalidated D31 movement-only estimate; M2 gate timings already showed ~5 s/day pre-M3-B). Scrubbing unaffected (instant via D37 keyframes). Engine optimization → M6 backlog, revisit after M4 reshapes the profile (A* + spotting dominate today). | Measurement correcting an estimate, not a regression. Codex profiled, attempted, reverted, and declined to game the number. | Approved |
| D57 | 07-17 | M3-C UX round from first user review (Chuck): pan/zoom + zoom-to-marker (spec-gap omission, Fable owned), unit tooltips, in-app legend, ghost-marker clarity, decision-index ORDER/EMERGENT badges, inter-tick position interpolation (PRD compliance), continuous speed slider, viewshed contrast pass. UI-only; engine untouched. Encounter action tooltips deferred to M4 spec (no engine actions exist to describe until combat). | First real-user evaluation; comprehension fixes precede new capability. | Approved |
| D58 | 07-17 | M3-D from continued user review: (1) viewshed overlay becomes an explicit manual toggle, independent of leader selection — positions/movement viewable without POV shading; (2) co-located unit markers declutter at zoom into display-only fan-out (clearly cosmetic offsets; tooltips identify each company). Underlying data confirmed already company-granular (12 companies as separate units since O1); true march-order column spacing → M4 spec (engine change). | Stacked rendering, not data granularity, was the limitation; display fix now, fidelity fix in M4. | Approved |
| D59 | 07-17 | DEM full-tier east bound extended −107.15 → −107.11 (covers candidate #1 + 2 km sweep radius; still within the cached n46w108 tile; core tier unchanged). Pipeline re-run; grids/manifest regenerate. Sweep authorized per D24: candidates scored by must-flip criterion (pony-herd ray CLEAR, curvature-corrected) + Deliverable-B morphology constraints; winner applied only after Fable's Phase-2 ruling. | Candidate sits outside the clip; the battle's opening scene depends on this point. | Approved |
| D60 | 07-18 | Crow's Nest ruled: crows-nest landmark → 45.4454,−107.1392 (OSM "Traditional Crows Nest?" node 5486442915), confidence MEDIUM ceiling per the provenance inversion; prior APPROX coordinate (45.42,−107.17) and the NPS decoy coordinates retained in provenance. Basis: O3-A sweep — unique full-pass cell of 1,681 (pony-herd ray CLEAR at 26.98 km curvature-corrected; elevation 1,353.7 m in band; strongest pocket signature, 91.8 m hollow within 300 m; 600 m from the trail corridor; 14.94 mi to village; village ray BLOCKED from all 1,681 cells — Custer's failed sighting is robust terrain truth), 0 m separation from the best-scoring cell. Two Crow's Nest observation events repositioned and promoted into the C4 gate per D49; exam verdicts accepted as they fall. Reopen clause: a field GPS or specialist coordinate >1.5 km away re-adjudicates (dossier threshold). | D24 executed as designed: the sim's own LOS engine adjudicated a disputed input against HIGH-confidence observation ground truth; the sweep closed the foreground caveat. | Approved — Fable ruling per O3-A work order; Chuck review at commit |
| D61 | 07-18 | pony-herd strength: placeholder 1 → sourced Estimate 2,000 / 2,500 / 20,000 — low/best per Bigelow/INSCOM "2,000–3,000 ponies" (dossier HIGH), high carries research §I "tens of thousands" (LOW) per the spread-carries-dispute precedent (coalition casualties); O5 flag for literature re-sourcing (lodge-count arithmetic favors the higher range). Exam re-run, verdicts accepted. | The dawn sighting failed because the data said one pony stood on the bench — O1 placeholder outliving its harmlessness. Verdict insensitive to the dispute (both bounds clear threshold 17–47×): data honesty, not tuning. | Approved |
| D62 | 07-18 | M4 consumes the PRNG. Different-seed runs diverge after first contact and remain identical before it; per-seed determinism remains absolute. Randomness enters ONLY through combat resolution and malfunction rolls; spotting stays deterministic (D46 unchanged). | The first dice deliberately flips E1 at contact while preserving D31/D31a replay and D55 cache purity. | Approved (Chuck, 07-18, incl. open-question answers) |
| D63 | 07-18 | `contact-pending` graduates into an engagement state machine per contact: APPROACH → FIREFIGHT → (CHARGE → MELEE) → WITHDRAWAL / ROUT / DESTRUCTION / DISENGAGE. Transitions are driven by orders, tactics weights, morale, and range. Each engagement carries a typed descriptor — unit pair, state, range band, intensity. Engine exposes `engagementActive` per tick. | Typed descriptors and the per-tick signal are the engine data source for M4-B encounter tooltips and speed-cap UX. | Approved (Chuck, 07-18, incl. open-question answers) |
| D64 | 07-18 | Per-tick fire resolution uses `shooters_effective × rpm(weapon)·tickFraction × hitProb(rangeBand) × exposure(target formation/cover/coverFactor) × flanking × tacticsModifier × leaderTacticalSkill/50 × fireDiscipline(morale, ammo)`, applied by floor + one seeded roll on the remainder. Shooters respect horse holders and ammunition; malfunctions clear after a delay; mixed weapons participate by band; indirect bows ignore intervening cover at a [CAL] hit-probability penalty. Casualty-resolution events carry the position where losses fell that tick. | Integer casualties by construction (D26), auditable first dice, and marker-ready loss data per Chuck Q1. | Approved (Chuck, 07-18, incl. open-question answers) |
| D65 | 07-18 | CHARGE closes at gallop; on contact resolution is shock-vs-morale: attacker shock score (strength, speed, leader aggression, tactics shockCharge) vs defender morale state → ROUT, close FIREFIGHT with melee-band weapons, or repel. | M4 models decisive shock and break behavior rather than detailed individual melee. | Approved (Chuck, 07-18, incl. open-question answers) |
| D66 | 07-18 | Per-unit morale is continuous 0–100 from baseMorale with STEADY / SHAKEN / BROKEN / ROUTED [CAL] thresholds. It drains from casualty rate, leader loss, flanking, isolation, low ammunition, and suppression; recovers from leader rally, lulls, and friendly proximity. withdrawalDiscipline shapes orderly WITHDRAWAL vs ROUT; routed units seek safety and attempt rally; DESTRUCTION occurs at strength or cohesion floors. | Makes Custer collapse and Reno's timber exit mechanical rather than scripted. | Approved (Chuck, 07-18, incl. open-question answers) |
| D67 | 07-18 | Leaders are killable by per-tick exposure roll while their attached unit takes hits, weighted up in MELEE/CHARGE and for leads-by-example traits. Death shocks attached and nearby units, removes rally, and bumps order delay for the affected branch. Leader death is fully emergent; no historical-fates pinning. | History records one outcome of the distribution; Chuck Q2 closes the pinning question. | Approved (Chuck, 07-18, incl. open-question answers) |
| D68 | 07-18 | Track per-man rounds by weapon; expend ammunition per fire tick; low ammo (<20%) degrades fireDiscipline and empty weapons leave the mix. RESUPPLY transfers at [CAL] proximity/rate, capped by the 26,000-round pack reserve. Scavenging from the fallen remains backlog. | Makes hilltop ammunition and the pack-train lifeline mechanical. | Approved (Chuck, 07-18, incl. open-question answers) |
| D69 | 07-18 | Fatigue-lite is a 0–100 pool per unit: gallop and MELEE drain it, halts recover it, and high fatigue caps speed class and shaves combat effectiveness [CAL]. | Gallop becomes a spend rather than a free gear. | Approved (Chuck, 07-18, incl. open-question answers) |
| D70 | 07-18 | In-transit orders ride a 1-man courier entity issuer→recipient at gallop along a real path; couriers are spottable and killable and undelivered orders never arrive. Companies sharing a battalion order receive deterministic along-path offsets by data order at ~150 m [CAL], superseding cosmetic fan-out while moving. | Upgrades D35's timers to the information-layer casualty model and makes march columns spatially real. | Approved (Chuck, 07-18, incl. open-question answers) |
| D71 | 07-18 | Fire formula gains combatFrictionFactor [CAL], a single global range-proficiency→battlefield-effectiveness multiplier; first-cut 0.06, anchored by historical-totals arithmetic (268 US / 53 Reno-Benteen / ≤300 coalition imply 10–20× reduction from unfrictioned rates); M5 calibrates the digit. Scenario hit bands remain untouched proficiency data (D8). Binding clause: if Reno-Benteen still falls at frictioned rates, diagnose anew — no second knob without a new mechanism ruling. | First fight exposed the D8-data/D62-formula seam (Fable-owned on both sides): aimed-shot bands consumed verbatim ⇒ mutual annihilation (1,109 coalition dead, hilltop destroyed). Structural named term = honest modeling; invisible smearing across [CAL] slots = prohibited tuning. Codex's STOP was correct and is commended into the record. | Approved |
| D72 | 07-18 | Engagement machine gains PURSUIT + initiative exploitation (one mechanism, two expressions): (a) ROUT/BROKEN-under-contact triggers pursuit — engaged enemies chase at speed class, contact persists while pursuers can close (no free disengagement for the broken side), resolution continues at close range; pursuers respect their own tactics weights and morale and disengage per existing rules against steady massed fire. (b) CONSENSUS_INITIATIVE bands whose engagement ends re-target the nearest believed enemy within an initiative radius [CAL]. (c) Corollary: RESUPPLY requires recipient not ROUTED. Predicted F5 asymmetry: wing destroyed by pursuit + convergence; hill holds via formed withdrawal + defensive mass — the exam decides. | Assembly, not invention: Fox verbatim (research §H) — "very rapid transition from suppression to close-quarters killing once cavalry cohesion broke"; mounted pursuit of the broken is the coalition's documented signature; initiative re-targeting is D7's command culture generalized from camp-defense to the fight. The D71 second STOP's four findings reduce to this one mechanism's absence — knobs need calibration; mechanisms need sources, and D72 has sources. D66's withdrawalDiscipline becomes load-bearing on both sides of the asymmetry: one parameter, encoding Fox's thesis, deciding both fates. | Approved |
| D73 | 07-18 | Engagement machine gains tactical-complex continuity (one principle, two expressions): (a) rout reintegration — a ROUTED unit whose pursuit has terminated and that reaches protecting friendly mass rallies into it (rout ends, cohesion drain stops, unit reforms SHAKEN under protection); the protecting mass must itself be STEADY — no auto-stabilizing into a SHAKEN cluster; morale-contagion-on-arrival goes to backlog as its own someday-mechanism. (b) complex-scoped convergence — bands freed by a collapse re-target surviving organized members of the SAME engagement complex first; the general believed picture resumes only when the complex is resolved. Complex membership rides the existing engagement graph; parameters flagged [CAL]. | Sources, the strongest yet: (a) the Reno Hill perimeter absorbing the routed companies (research §C, 15:30–15:45 — the survivors reformed and held); (b) Fox's sequential-collapse archaeology (research §C/§F3): Calhoun Hill → Keogh → Last Stand Hill, the coalition finishing the wing as one continuing fight. Third STOP's two failures reduce to this principle's absence. Falsifiable side-prediction on record: complex-scoping should pull the 37-Arikara distortion toward history's 3 named; if not, scout behavior becomes its own diagnosis (tactics-profile, not convergence). | Approved |
| D74 | 07-18 | Rout-safety pathing gains enemy interdiction: non-routed enemy combat units project an interdiction radius [CAL]; safety paths treat interdicted space as impassable; a routed unit with no open corridor to a STEADY protecting mass has NO safety — it remains in the fight-space and existing mechanics complete the encirclement. Completes STOP-2 finding #1 (D72 built pressure-from-behind; this is denial-ahead). Prediction on record: the wing dies in place — zero Custer-company survivors reach Reno; D's adjacent-mass reintegration unaffected. | The most attested fact of the battle: the pincer (Gall south, Crazy Horse's northern sweep — research §B3/§C) closed every corridor and no one reached Reno. Fourth STOP: 31 men walked a geometric A* route through coalition-owned ground; tactical accessibility ≠ terrain reachability. | Approved |
| D75 | 07-18 | SCOUT_DETACHMENT withdrawal doctrine, scoped by TACTICS PROFILE (irregular-scout), NOT unit kind: scout units under organized band pressure withdraw off-field rather than holding in complex. EXCLUSION BY ID with provenance note: civilians-interpreters (typed SCOUT_DETACHMENT and carrying the irregular-scout placeholder profile from O1's count workaround) is excluded — Bouyer, Boston Custer, and Kellogg rode with the wing and died with it. Predictions on record: (1) Arikara casualties pull from 37 toward history's 3 named; (2) the ford-choke clustering redirects from scouts toward Reno's broken companies — or its absence is reported. Resonance noted: Curley leaving before the end — the battle's most famous scout departure and the O3 dossier's disputed observer-from-a-distance — is D75 behavior emerging naturally. | Historical Arikara doctrine per their own mission: run off the pony herd, cover the valley collapse, withdraw (research §A: three named dead, detachment gone from the battle-space by late afternoon). Scoping trap caught in review: kind-scoping would have evacuated three real men who died. | Approved |
| D76 | 07-18 | M4-B additions from user review: dynamic scale ruler (M3-B spec omission, Fable owned) and live casualty panel (per-unit + side totals, labeled "losses"). Killed-vs-wounded split deferred to M5 as a model question (D64 resolves a single casualty number; historical targets carry both — the split is a [CAL] ratio the calibration must earn, not a UI fabrication). Panel reserves the columns. | Both requested by Chuck pre-dispatch; both feasible now; the wounded column waits for the model rather than inventing data. | Approved |
| D77 | 07-18 | Viewshed presentation pass from user review: ON = persistent dark scrim with the lit viewshed as a stable "beam" (the beam moves, the darkness doesn't); temporal crossfade between per-tick rasters with hold-last-until-ready (the D57 interpolation principle applied to lighting); OFF = zero overlay, instantly. UI-only. | Per-tick raster swaps strobed during playback — positions were interpolated (D57) but lighting never was; surfaced by extended M4-era playback. | Approved |
| D84 | 07-22 | Reno approach refinement (D39/D53a lineage), lifting the M5-B round-0 suspension: (a) reno-advance type ATTACK→MOVE — the trot was M2 order-type semantics, never sourced ("as rapid a gait as he thought prudent"); (b) new reno-water HOLD at Ford A, 10–20 min, split from research §C's HIGH watering row (reno-mount precedent); (c) new reno-charge, type CHARGE, issued ~14:35 per the watering row's end, for the valley charge phase (§C ~15:00 row). Order count 23→25; count tests updated. Residual MEASURED before closure is chosen: if Ford A still misses ±25 min, the pass STOPs that item and reports the number — closure (reconstruction halt LOW-flagged vs R1 tolerance ruling) is a separate Chuck ruling made with the residual in hand. | Ford A −95 traced to unsourced pace semantics, not calibration; the sourced endpoints (12:15 departure, 14:15 crossing, both HIGH) are the pace authority. | Approved |
| D85 | 07-22 | Bloody Knife added to scenario.leaders: attached arikara-scouts, ratings 75/50/50/50/0 (perception 75 — celebrated tracker; others doctrine-average; delay 0), ratingsProvenance quoting §A (named KIA, Reno's wing) and §C/F5 (death beside Reno as the retreat's trigger — the D65 morale-shock archetype). Leader count 18→19; count tests updated. RIDER: the prediction cannot score itself into existence — D82(c) remains emergent (leader-exposure rolls only, no scripted death); the envelope verdict reports as it falls. | D82(c) presumed data that was never modeled; the record supports the entry, the mechanism exists, the dice still decide. | Approved |
| D86 | 07-22 | Ford A closure: reconstruction halt ~60 min on the Reno Creek descent, LOW, provenance "acknowledged model-invention: documented time, reconstructed placement" (D14 precedent — sourced total, invented distribution). Order count 25→26; count tests updated. R1 tolerance option rejected: ±70 makes the checkpoint unfalsifiable — a gutted instrument. | Chuck's pre-stated lean toward the tolerance option is REVERSED on the record because the number changed the case: with both endpoints HIGH-sourced, the two-hour elapsed time is data — the column demonstrably spent it; only the halting's placement is reconstruction, which is not manufacturing a hit. Pre-statements are priors, not commitments. | Approved |
| D87 | 07-22 | Infiltration fire posture for CONSENSUS_INITIATIVE bands vs formed enemies (the §H weight becomes behavior, DEFEND_CAMP precedent): engage from cover/dead ground; output weighted toward sustained suppression over kills. RIDER 1 (binding): terrain, not a modifier — the exposure reduction comes from actually occupying real cover polygons / LOS defilade; a band on open grass gets no discount; a flat "suppression mode = −X% exposure" is a knob in a mechanism's coat. RIDER 2 (binding), predictions as they fall: (a) A/G/M break in the 731–736 window and survive into the perimeter with US killed back in band; (b) coalition deaths drop toward envelope; (c) ford choke populates with broken troopers; (d) Bloody Knife's death appears (D85-asterisked — emergent only); (e) the wing fight shifts too — §H's sentence describes the Custer field as much as the valley; wing destruction must stay in envelope (ideally 40/50 improves). If infiltration breaks the wing's calibration while fixing the valley's, that is information, not failure. | The cleanest mechanism case of the project: source in §H since day one; the range test proves the gap (50/50 breaks achievable only by annihilation — fear purchasable only with proportional death); infiltration 90 a weight with no behavior since O1. | Approved |
| D78 | 07-22 | `engine/src/score.ts` grows to the full PRD §8 rubric, gating together: C1 checkpoints (weight .35): ≥70% of HIGH-confidence hit, ≥50% overall. C2 casualties (.25): per-side totals within historical low–high bands; flagship end-states (annihilated wing) exact. C3 end-states (.25): 100% of HIGH-confidence assertions by their minute. C4 observations (.15): ≥80% (currently 12/13 — already green). Output: one composite number + per-item report card (`npm run score`), written to reports/calibration-scorecard.md. Variant runs scored with counterfactual items excluded (their provenance flags already say so). | Full C1–C4 accountability before calibration; Chuck Q1 binding answer: full report card exposed to end users. | Approved |
| D79 | 07-22 | Calibration constitution (binding law for the pass): 1. Every [CAL] moves **only within its sourced range** (R1). Values without a sourced range get one derived and documented before tuning. 2. Global tables only (D49): no per-event, per-unit, per-checkpoint values. 3. **No mechanism changes during calibration.** Gates unreachable inside sourced ranges = STOP + documented finding; the R1 escape hatch (loosen a per-scenario tolerance with written rationale) is a Chuck-signed ruling, never a tuning act. 4. Every round reports before/after per value + per-gate margins; the full audit trail is a committed artifact. 5. Checkpoints, observation events, and calibration targets are frozen — no data edits during the pass (source corrections suspend calibration and restart it). | Preserves sourced limits, mechanism/calibration separation, and a complete audit trail for M5-B. | Approved |
| D80 | 07-22 | Emergent outcomes are judged as **distributions, not runs**: N=50 seeds (cheap at ~8 s/run) scored per D78; report distribution of composite, leader deaths (count + who), Arikara losses, rout compositions, wing-destruction tick. History must sit inside the envelope, not at its center. The shipping baseline seed = a **typical member** (median composite, no rare events), selected by stated criteria before results are inspected per-seed — never cherry-picked. Selection script + criteria committed. | Chuck Q2 binding answer: N=50. Chuck Q3 binding answer: all 7 variants scored, with flagships + counterfactuals deep-tier and the remaining three sanity-tier. | Approved |
| D81 | 07-22 | Fire casualties split by a global [CAL] ratio per side, sourced from the calibration targets' own bands (US: ~268 K / ~52 W hilltop-inclusive; coalition: K 31–300 / W 100–200 — both DISPUTED spreads preserved). Integers by construction (D26); C2 scores both columns; the D76 reserved UI columns go live. Wounded reduce effective strength but are not dead (terminal accounting differs); no further medical model in v1. | Makes the historical killed/wounded targets mechanically scoreable while preserving integer conservation and disputed spreads. | Approved |
| D82 | 07-22 | Reno's withdrawal must be *capable* of breaking into history's rout. In scope as calibration (not mechanism): warrior commitment against the valley fight flows through existing [CAL]s — engagement participation of the Reno-response bands, suppression weight, morale drain rates — within sourced ranges. **Pre-registered predictions**: (a) A/G/M morale reaches BROKEN during the timber→river leg in a meaningful seed fraction; (b) the ford choke's dead shift from scouts to broken troopers; (c) Bloody Knife's death (leader-exposure) appears in the envelope. If intensity cannot reach the rout inside sourced ranges → STOP: that is a mechanism finding (candidate: close-infiltration fire from cover), ruled separately. | Names the valley-fight calibration finding and freezes its falsifiable predictions before M5-B tuning. | Approved |
| D83 | 07-22 | The controls go live (UI): **Variant toggles**: all 7 variants (labels, proponents, descriptions from scenario data), exclusion groups enforced, counterfactuals visually marked; toggling re-runs and re-scores. **Parameter panel**: leader ratings, strength Estimates (low↔best↔high sweep), order timing offsets, and the headline [CAL]s (friction, herd — each showing its sourced range as the slider's rails; no travel outside). **Baseline discipline**: defaults = post-calibration values; any deviation shows a visible "modified from baseline" flag + one-click reset (PRD §4.5). **Report card**: the D78 scorecard behind a builder flag (PRD §4.6); end-user exposure per open question 1. Accuracy caveats surface confidence (PRD R4): the panel shows each parameter's confidence tier. | Ships the controllable simulation requested on day one while making calibration bounds, baseline deviations, and confidence visible. | Approved |
| D88 | 07-24 | O6-STANDOFF coordinates recorded as NON-AUTHORITATIVE; the scenario frame governs. O6's verdict (TERRAIN — defenders occupied terrain features, not a standoff range), its feature identities (timber/brush, low hill "Bench", W-edge foothills, Ford B riverbank), its turnout delay (10/15/20 min) and its evacuationScreen=false finding are ACCEPTED. Its lat/lon values are NOT transcribed: they diverge from the scenario frame by ~2.6 km at the Hunkpapa circle. Feature coordinates are re-derived in-frame from the terrain cover raster and elevation, per O6's own statement that "feature names are the load-bearing locators; lat/lon carry high positional uncertainty." | Probe 07-24: all six camp units sit on VILLAGE raster cells within 2-6 m (hunkpapa 2, oglala 2, minneconjou 5, sans-arc 5, mixed-north 6, cheyenne 6) — camp units and terrain raster were derived together and are fully self-consistent, so the divergence is O6's. Independent corroboration of the scenario frame: Reno's line centroid sits 227 m from hunkpapa-camp, inside O6's own sourced range for Reno's halt (183-274 m; NPS ~200 yd, RCOI ~274 m), derived from unrelated work. | Ruled |
| D89 | 07-24 | Cover vocabulary ruled as TWO LAYERS, not one enum. (a) STATIC SUBSTRATE = the terrain raster (NONE, TIMBER, VILLAGE, RIVER, FORD populated; RAVINE and HISTORICAL_CORRECTION declared but EMPTY, 0 cells each — nothing may be built on them). Immutable, sourced, cacheable. (b) TRANSIENT OVERLAY = simulation-emitted effects with a lifetime and an emitting unit; DUST_SMOKE_ZONE belongs here, NOT in the raster. Cover/concealment queries resolve across layers. V1 MAPPING, scenario CoverKind -> substrate: TIMBER->TIMBER, VILLAGE->VILLAGE, RAVINE->(unavailable, empty), BRUSH->TIMBER (fidelity limit: the terrain pipeline has no brush source; O6's "timber and brush" collapses to TIMBER at v1 and this is recorded as a known loss, not laundered), DUST_SMOKE_ZONE->declared-but-unimplemented, parked for v2. | Ruled against the v2 zoom-in animation target. Unifying the enums would require rewriting a shared immutable raster per tick or special-casing one code through every consumer, would overwrite the substrate an animator must draw beneath the dust, and would mix sourced terrain with simulation output in one array — provenance laundering. The layered model matches the renderer's natural query (substrate, then composite overlays) and lets dust/smoke feed the same concealment path D87 already runs on cover polygons, which is historically apt for both the valley fight and the Custer field. HISTORICAL_CORRECTION purpose is UNKNOWN — 0 cells, no manifest note; no interpretation is asserted. | Ruled |
| D90 | 07-24 | O6's "low hill / Bench" identified in-frame at 45.51659, -107.38996 (+5.5 m above local valley floor, 218 m from Reno's line, 380 m from hunkpapa-camp). O6's "low round foot-hills, W valley edge" identified as the separate, higher, farther feature set at 45.50579/-107.40259 (+21.1 m), 45.51317/-107.41571 (+31.7 m) and 45.50827/-107.41294 (+33.0 m), 1845-2191 m out — consistent with Varnum's RCOI testimony that fire from the foothills at 800-1200 yd fell short. Confidence MEDIUM. | Derived by terrace search (rise 3-18 m above local floor, <=3.5 m relief across a 60 m neighbourhood, downhill break toward the line), not by local-maximum search: a bench is a terrace, not a summit, and an earlier summit search returned only the foothills. Selected candidate is the only one at a distance consistent with O6's geometry (218 m from a line that itself sits 227 m from the lodges), has the most relief of the terraces, and lies physically between line and village on the west side. CAVEAT RECORDED: "Reno's left flank" is not computable — see D91 rider — so selection rests on absolute geometry, not flank. *Dated annotation (2026-08-02, pre-D100 gate; probes `.claude/varnum-band-relief-check.mjs`, `varnum-band-extent-check.mjs`). D90 claimed consistency with Varnum's RCOI testimony; the claim was range consistency, and range consistency is FALSE — the pinned points lie ≥1,579 m from Reno's line under the most favourable referent (D44 checkpoint: 2,244 / 1,579 / 2,014 m; camp-triangle bound 1,619–2,417 m) against Varnum's 800–1,200 yd = 732–1,097 m, 44% above the upper bound. The successor claim is weaker, was derived from the DEM after the range reading failed, and is source-corroborated only at direction and identity ("rode around on those foot-hills to the left" — direction, no distance): the western rise is one contiguous component from 320 m out, through Varnum's band (+5–9 m apron; nothing in-band survives a +10 m threshold — no detached in-band feature exists), climbing unbroken to the pinned summits (+21–33 m at 1.8–2.2 km). The corroboration that survives is region-level, and this annotation is a DEMOTION, not a rescue. The three coordinates are hereby owned as a selection convention on a measured region — D108's segment-boundary position, cited as precedent — and remain the right selection for their actual purpose, a far feature set distinct from the Bench; what they no longer are is Varnum's firing position. Estimate-low (sights at 800–1,200 yd against true 1.6–2.2 km) drops to one coherent possibility among several. STAKES: nothing behavioral rests on this — landmarks are id-lookup-only and these entries will be unreferenced; the consumers are a map label and this ledger. Registered observation, explicitly not a Bench reopening (D101 stands at LOW on absolute geometry, untouched): the in-band apron platform (~954.4 m, +9 over the ray valley, flat within 3.5 m across >60 m, downhill break toward the line) QUALIFIES under D90's own terrace criterion; the original search instrument was not preserved, so whether the search saw and rejected it on distance or never reached it cannot be settled from the record.* | Ruled |
| D91 | 07-24 | Camp-defence reconstruction ruled, defect class, D79.3 unaffected (no [CAL] value is touched). FOUR coupled changes, one work order: (1) LATCH — re-path while campDefense is active and blocked, replacing the single-shot `if (!unit.campDefense) activate(...)`; (2) STRANDED UNIT — movement may not place a unit on an impassable cell, and a unit already on one must be recoverable; findPath rejects an impassable START, so a unit that walks into the river can never path out; (3) GOAL — the D47 geometric midpoint is RETIRED and replaced by feature-based positioning from scenario data (D88/D89/D90); no standoff number, no ratchet; (4) THREAT COMMITMENT — nearest-threat re-selection is ruled correct in principle but requires a switching margin; unhysteretic per-tick re-selection produced target thrashing. Turnout delay 10/15/20 min (O6, MEDIUM) replaces activation-on-spotting. | H1 probe 07-24: 447 of 1707 warriors (26%) ever come within 500 m of Reno; seven of ten bands never move all day. Backward-walk measurement (scratch build, seed 18760603): naive continuous re-path against the midpoint goal produced 3652 m of backward travel against 6642 m forward (sans-arc), 3641/6813 (blackfeet-santee) — the artifact O6's TERRAIN verdict removes by construction, since a static feature goal cannot recede. Stranded-unit defect VERIFIED not inferred: minneconjou-pool at 45.52194/-107.39808, cover=RIVER, movementFactor 0.00, gridCost Infinity, start not passable, while its two companions 350 m away path normally. This defect is MASKED by the latch in the baseline and is UNMASKED by fix (1) — the two must ship together. | Ruled |
| D91 RIDER | 07-24 | RECORDED, NOT RULED — units are dimensionless points; Reno's co-a, co-g and co-m occupy the identical position 45.51833, -107.38873. O6 documents a ~206 m skirmish-line frontage with an unanchored left flank. There is therefore no line and no computable left flank in the simulation, and D90's Bench selection rests on absolute geometry rather than flank. This is a modelling choice surfacing, not a defect, but it bears on a valley phase that historically turned on the open left. Flagged for separate adjudication; explicitly OUT OF SCOPE for D91. | Probe 07-24, terrace search against Reno's line at minute 720. | Recorded |
| D92 | 07-24 | WO-D91 ambiguity rulings (Codex STOP upheld; three named gaps plus one found in review). (a) REPATH CADENCE: structural reuse of pursuitRepathCadenceTicks (10 ticks / 5 min, D34/D70 lineage); governs BLOCKED-RETRY ONLY — goal re-selection is event-driven on threat switch, since a feature goal is static. (b) THREAT SWITCH MARGIN: 250 m, D34 lineage structural reuse; noted that it also resolves the coincident-companies tie recorded in the D91 RIDER, and wants revisiting if unit frontage is ever added. (c) FEATURE SELECTION: eligible = cover features within campDefenseRadiusMeters (3000) of the defended camp; select nearest to the committed threat at time of selection; NO camp-side constraint; deterministic tie-break by feature id; FEATURE COMMITMENT MIRRORS THREAT COMMITMENT — a held feature is held until the committed threat changes or the feature becomes unreachable. (d) FEATURE PROVENANCE: TIMBER features derive from the substrate raster by clustering at load; the scenario declares ONLY the D90 Bench, which as a topographic terrace has no landcover representation. No values are duplicated into scenario JSON. | Codex STOP verified legitimate on all three counts by CC; WO-D91 genuinely omitted all three, and review found a fourth — the scenario declares no cover features whatsoever, so §Scope item 3 referenced data that does not exist. The camp-side constraint proposed in review was REJECTED: O6 §B records Cheyenne guards posted on the east bank, the attacker's side, at zero standoff, and a camp-side filter reintroduces the backward-walk artifact stepwise as the eligible set shrinks toward the camp — the opposite of the valley fight, where warriors advanced and drove Reno into the timber. RECORDED IN ADVANCE: feature-holding positions bands but does not close; if prediction 3 (A/G/M BROKEN) misses, that is a finding requiring a separately-ruled closing mechanism, NOT a knob. | Ruled |
| D93 | 07-25 | Camp-defence release made SYMMETRIC with activation (defect class): a band releases its committed threat when that threat leaves the radius that would have triggered activation (campDefenseRadiusMeters, 3000 — structural reuse, no new number). Activation uses the radius predicate; release() previously did not use it at all, firing only on loss of spotting or arrival of an order. The asymmetry pinned bands to a departed threat. | F4 RED post-D91, adjudicated on the envelope per the seed-fragility finding (METHODS §6): on the committed stream, complete wing destruction in 8/50 seeds and late (858-861 vs historical 825-840) — a low destruction rate arriving late is the bands-never-release signature. No baseline-seed reading is cited; the single-seed failure shape flipped across the D31a reseed, so the ruling's grounds rest on the distribution only. Ruled before any F4 re-baseline was contemplated, per the principle that a re-baseline may not substitute for a defect fix. Work order frozen pre-dispatch at docs/WO-D93.md; no dispatch preceded these rulings (NEGATIVE_RESULTS §1). | Ruled |
| D94 | 07-25 | F6 work ceiling RE-BASELINED and tied to active participant count, with the derivation recorded. F6 is a resource gate, not a fidelity gate; its function is to catch runaway computation, not to encode historical truth. The prior ceiling was set when 26% of available warriors participated, a state invalidated by the D91 defect fix. The new ceiling is NOT set to just above the current observation — a ceiling moved to wherever the current run lands is not a ceiling. | Post-D91: active participants rose ~2.2x while expansion work rose ~6%, indicating the D92 repath-cadence ruling contained the cost as intended. Prior ceiling 11.1M against observed 11.55M (0.3 stream) / 11.80M (0.2 stream). Work order frozen pre-dispatch at docs/WO-D93.md; no dispatch preceded these rulings (NEGATIVE_RESULTS §1). | Ruled |
| D95 | 07-25 | CLOSING MECHANISM ruled on local numerical superiority. The historically attested trigger is FLANK EXPOSURE (500+ warriors against Reno's left and rear on an unanchored flank), but that trigger is NOT COMPUTABLE in the current model: under the D91 rider units are dimensionless points and Reno's three companies occupy one position, so no flank exists to turn. Numerical superiority is also supported by the sources and is expressible by the engine. PRE-REGISTERED BEFORE ANY RESULT: if numerical superiority alone does not reach P3/P4, flank exposure is the next candidate and the frontage adjudication becomes its prerequisite. NOT A PARAMETER CHANGE IN EITHER CASE; nothing is to be tuned to make P3 or P4 pass. Overshoot stop registered as PR-5 (Reno killed >26.09 in >5/50 seeds halts work for adjudication). | D92 pre-recorded this exact failure mode before the D91 run; P3 then missed 0/50 with bands at effective range and the ford choke empty, confirming the prediction. P4 misses LOW (killed 6-20 vs sourced band 19.24-26.09), so the anticipated failure ahead is overshoot, and the stopping threshold was set before any result existed. Work order frozen pre-dispatch at docs/WO-D93.md; no dispatch preceded these rulings (NEGATIVE_RESULTS §1). | Ruled |
| D96 | 07-25 | WO-D93 STOP upheld; D95 closing-mechanism specifics ruled. (a) TRIGGER: a band closes when its committed threat's cohesion is degraded (SHAKEN or worse) AND bare local numerical superiority (>1:1) holds. Reuse of chargeBreakMargin (1.1) as the decision threshold is REJECTED: the decision and its resolution would share a constant, so every charge would break its target by construction and P3 would pass because the mechanism guaranteed it. The 5:1 figure from Reno's account is an observed outcome, not a trigger anyone held to. Cohesion is the discriminator per D72 (Fox: rapid transition from suppression to close-quarters killing once cohesion broke); at ~964 warriors against ~130 troopers every candidate ratio is exceeded regardless, so the ratio is not load-bearing here. AGGREGATION: strengthAvailable both sides, excluding BROKEN/ROUTED/DESTROYED from the closing side. (b) LOCALITY: friendlyRadiusMeters (450 m), centered on the committed threat, not on the band — the test is who is winning the fight around the thing being closed on. (c) EXPRESSION: existing CHARGE machinery, event-driven on trigger passed-and-held per D92 commitment semantics; D65 owns shock-vs-morale resolution, break-off and repel; D93 symmetric release handles cleanup. ATTACK+INITIATIVE and combat-pursuit reuse are rejected — both bypass shock resolution, the mechanism the sources describe. | Codex STOP verified: nothing implemented, tree clean, PREDICTIONS.md and scenario hash untouched. The ambiguities were named in the frozen work order before dispatch, so the STOP is itself a pre-registration hit. VERIFIED BEFORE RULING (CC): chargeBreakMargin is consumed as a hard deterministic threshold in D65 shock resolution (combat.ts:361, no roll — shock > defense × 1.1 sets the defender ROUTED outright), so the circularity objection stands as stated; the shock term's additional multipliers (speed, aggression, shockCharge) run in the attacker's favor at valley odds. NOTED IN ADVANCE: the cohesion condition makes P3 HARDER to reach, not easier. If Reno's morale never degrades under D87 infiltration, closing never triggers and P3 misses a second time — which would locate the defect in the suppression half rather than the closing half, and is recorded here as an anticipated outcome rather than a surprise. PR-5 overshoot stop remains armed; PR-1-PR-8 remain frozen and unedited. | Ruled |
| D97 | 07-26 | SEQUENCING RULED: the sequence-inversion defect (D98/D99 scope) is fixed and re-measured BEFORE any calibration. Testing moraleSuppressionDrain within its sourced 0-1 range is ruled LEGAL under D79 and QUEUED — it may not run against the current baseline, whose wing-morale regime is partly produced by the defect; a digit fitted now would be fitted to contamination. state.ts:241 eligibility remains HELD (PR-1's pre-committed response) for the same reason: adding warriors to a pipeline that inverts the sequence compounds the defect rather than exposing it. | Probe 07-25 (.claude/cohesion-asymmetry-probe.mjs): cohesion moves almost entirely via casualties; suppression drain clamps at moraleSuppressionDrain=0.08/tick saturation against active recovery — so the suppression half of P3 is calibration headroom, not a missing mechanism, measurable only on an uncontaminated baseline. | Ruled |
| D98 | 07-26 | CAMP DEFENCE DOES NOT CROSS THE RIVER (defect class; replaces the closing-threat/ratchet draft, which measurement killed before freezing). Bands acting under camp defence may not cross the 1876 channel: feature goals and closing movement are confined to the defended camp's side, derived at load from the committed channel geometry (D92(d) derivation pattern; no new number). Threat eligibility unchanged — a cross-river threat may hold a commitment but cannot be closed on. Order-driven movement untouched: the scheduled axes cross freely. The O6 §B east-bank guards are picket behaviour, not camp defence, and are out of this mechanism's scope. | Measured before freezing on seeds 18760643 + 18760625 (.claude/d98-ratchet-test.mjs, .claude/d98-crossing-test.mjs): (1) the ratchet draft FAILED — the approach march sets new minimum believed approaches to four camps, up to 13 each, during the exact commitment window, qualifying the passing column; (2) crossing form FIX CONFIRMED — west-only selection yields stands 713-1684 m from the column with no closing path, killing the 641-653 interception; (3) NO LEGAL LOSS — every observed camp-defence crossing is interception-class (622 vs co-i both seeds; 808.5 vs co-e baseline — the LATE cluster was partly camp-defence-driven too), the valley fight is fought entirely west-bank, and all Ford-B-area crossings are ORDER-driven (gall-calhoun, ch-strike, lwm-charge — the pincer, unaffected); (4) NO RE-FREEZE — 0 of 96 committed samples lose their last eligible feature; no fallback clause needed. DISCLOSED: substrate timber is nearly all channel-east (timber-0001: 0/984 west points), west-bank cover is thin and bench-centric; sub-700 m cross-river APPROACH contacts remain possible and are benign harassment, not interception. *Dated annotation (2026-08-02): D98's measured finding stands — 0 of 96 committed samples lost their last eligible feature to west-only confinement, and none of the 291 switch-to-nothing events in the D108 world is a confinement stranding. What falls is the inference drawn from the measurement: that camp-defence-without-feature does not arise and no fallback clause is needed. The state arises: 291 switch-to-nothing events strand the bands — switchThreat's unconditional feature clear (`camp-defense.ts:488`) on a switch to a camp with no eligible feature in radius; the 405 retry fires across 42/50 seeds [stream `ba288f09`] are the cadenced consequence of holding stranded; zero re-acquisitions (probes `.claude/d100-exclusion-branch-probe.mjs`, `d100-featureless-origin-probe.mjs`, digit-exact vs the accepted D108 campaign). Second instance of the eleventh measurement's lesson: a measured quantity right, the conclusion drawn from it wrong (first: turnout-was-never-arrival, D103 row). The fallback-clause design death is unchanged — that clause was for that cause, and it was still never needed. switchThreat's clear-on-switch is REGISTERED AS A CANDIDATE, not ruled: clearing is the unhysteretic re-evaluation D92 refused; keeping pins bands to the bench against northern threats, D93's asymmetry mirrored — measured behind break 2, reseed-free.* | Ruled |
| D99 | 07-26 | SCOUTS ARE NOT CAMP THREATS (defect class): units whose tacticsProfileId is irregular-scout are excluded from camp-threat eligibility. Profile scoping per the D75 precedent, NOT unit kind — this sweeps in civilians-interpreters, benign for threat eligibility since they ride with companies that remain eligible. Scouts historically triggered alarm, not a band sortie. REOPEN CLAUSE (D60 pattern): reopens if O5 or later sourcing documents a band-level sortie provoked by scouts at the lodges. | Observed defect: pool bands held crow-scouts (min 650) and arikara-scouts (min 660) as committed camp threats in the early-cluster seed. Source basis stated honestly: research §A scout roles (herd raid + screening; no recorded band sortie against scouts at the lodges); the alternative reading has no source either, and between two unsourced options the record's silence governs, flagged. Ruled now rather than waiting for O5 because every round dispatched with the defect contaminates the baseline D97 protects. | Ruled |
| D101 | 07-28 | D90 SUPERSEDED IN CONFIDENCE, not in coordinates (superseding row per the never-edit rule; D100 remains reserved for the foothills declaration, which rides the budgeted reseed bundle — reservation precedent, Memory.md §2). Bench identification confidence MEDIUM → LOW; the Michno p.105 attribution is marked RELAYED/likely-misattributed; the Jackson "500 warriors … shielded by a small hill" figure is a modern gloss, not verbatim Schultz 1926. Both pillars of D90's identification are now weak. THE TERRAIN IS REAL — a +5.5 m raster-derived terrace at 45.51659, -107.38996, 218 m from Reno's line; what is weak is the historical attribution of that terrace as the feature the sources describe. The feature REMAINS DECLARED: it is the sole west-bank cover post-D98 and removal would strand the valley bands. Scenario provenance bytes follow at the budgeted reseed bundle; this row is the record of the downgrade until then. FORWARD NOTE for the frontage/occupancy draft: O5's B5 finds the 225-yard line unsourced, and RCOI primary testimony (troopers 5–10 yards apart, every fourth man a horse-holder) derives ~500+ yards for ~112 men on line — real frontage plausibly ~2.5× the 206 m this project has reasoned from. Not a data fix today (units are dimensionless; no line length is encoded); it becomes live the moment frontage lands. | O5-RESOURCE report (docs/research/O5-RESOURCE_Citation Verification Research.md), B2/B3/B5: "bench" absent from Lakota Noon's index and frequent-terms list, p.105 most plausibly in the transition out of the valley fight; the ">500 warriors" phrasing propagates through a Wikipedia-mediated gloss of Schultz 1926 (not consulted at page level, held HathiTrust/MSU). The NEGATIVE_RESULTS §6 exposure flag (Bench load-bearing post-D98, recorded 07-26 BEFORE the O5 verdict landed) is hereby marked LANDED — flagged in advance, exactly as the register intends. | Ruled |
| D102 | 07-28 | UNIT FRONTAGE RULED — the occupancy primitive's cavalry half; first computable slice of the D91 rider. (a) SOURCE: Upton 1874 ¶610, eye-verified verbatim — "The habitual interval between skirmishers is five yards"; ¶652 read UNIFORM: 15 yards is the frontage a set of fours occupies base-to-base, and 15÷3 = exactly ¶610's five — the gap reading puts Upton in self-contradiction twelve pages apart, and dies, taking the 7.62 regime with it. (b) VALUE: metersPerFiringMan [CAL] with a deliberately ONE-SIDED sourced range — best 4.57 m (¶610, SOURCED-verbatim) / low 0.914 m (Carpenter reinforced practice, SOURCED-WEAK) / high NONE: a line compresses under pressure, it does not habitually exceed its own drill, and a symmetric upper bound would manufacture the number O7 refused to. (c) SCOPE: frontage = strengthAvailable × metersPerFiringMan for DISMOUNTED SKIRMISH ONLY; every other formation and all warrior bands carry ZERO frontage, disclosed simplification — extent applies where doctrine prescribes it, and nowhere else. Warriors' extent is their ground; their ground arrives with the reseed bundle. (d) CONSUMPTION per the standing rulings: edge-to-edge effective range into fire resolution and endpoint-overlap into flank determination ONLY; the 700 m opening gate stays centroid; cover-at-centroid stands disclosed; endpoint-flank instrumented separately from angular flank (no baseline exists and angular already runs 56–77%). Derivation exported for C's occupancy reuse. | strengthAvailable VERIFIED to net horse-holders before ruling (movement.ts dismount transition: floor(strength×0.25), the 1-in-4 Upton prescribes; the fraction preserved under casualties; mounted resets to full — hence the formation scoping, which is not a gap in the sourcing but the sourcing itself: O7 returned UNRESOLVED beyond a horse footprint for mounted, and warriors have no doctrinal interval). Floored arithmetic: 45-man company → 34 firers → 155 m; encoded A/G/M → 102 firers → 466 m at habitual (O5's ~112-firer testimony variant → ~512 m). CONVERGENCE OBSERVATION, corroboration not proof: doctrine prescribes 5 yd, RCOI troopers testify 5–10 yd, the derivation lands 466–512 m — the encoded 206 m is doubly dead, unsourced per B5 and below the doctrine its own troopers followed. Eighth measurement at 4.57: median effective range 144 m, 81.9% of fire ticks at/below charge range — DIRECTION NAMED IN ADVANCE (PR-15): frontage deepens the annihilation; pressure stays on C and dispersal. Correction chain (secondary → index term → printed page, three dated layers) preserved at docs/research/UPTON-1874-VERBATIM-TRANSCRIPTION.md. | Ruled |
| D103 | 07-29 | CAMP-DEFENCE ALARM RULED: DETECTION ≠ ALARM — spotting produces awareness; camp defence requires a hostile act. (a) TRIGGER (T3, river commitment): a camp's alarm fires only when a spotted eligible threat commits to a river crossing TOWARD the defended side — closing, camp-ward commitment only. An outbound/retreat crossing raises NO alarm: the ford-choke pursuit is pursuit of an already-engaged enemy and belongs to D93 release and D96 closing, which already own it — one trigger, one meaning. Structural reuse of D98's commitment distinction. The 3,000 m radius keeps its existing role in threat/commitment eligibility; it no longer constitutes the alarm. (b) RESPONSE-SPEED DEFECT: activate() set CAVALRY_WALK for a camp under attack — the wrong speed class for the act, not a wrong value; mounted responders use the existing CAVALRY_GALLOP (dismounted ON_FOOT unchanged). Both halves ship in one work order; neither works alone (T3 without gallop arrives ~800; gallop without T3 keeps the pre-assembly). T1 (attack-speed approach) REJECTED — fails to generalize at Ford B, where the wing never charges a camp yet the village historically responded; T2 (first fire) REJECTED — late in the valley and weaker at Medicine Tail. | Measurements ten through twelve, all preserved-probe lineage. TENTH: the radius has NO AUTHORITY over the outcome — alarm 587–608 across O6's entire sourced band (flat 608 for 1,600–2,600 m) because the approach road passes within 1,600 m of hunkpapa-camp ~110 min before the charge; geometry cannot express a behavioral distinction — the D98 lesson generalized. ELEVENTH: trigger candidates cluster (T1 700 / T2 706.5 / T3 700 by insideFord); the march was the dominant hidden term — 83–86.5 min at walk — TURNOUT WAS NEVER ARRIVAL, a measured quantity was right and the conclusion drawn from it wrong, recorded as a lesson; the r=250 dynamic proxy ran the full historical sequence end to end on existing machinery (charge, halt, dismount, line at 725.5, remount 749, retreat; killed 18 vs band low 19.24) — the model's first skirmish line. TWELFTH: Ford B measured — E/F commit to the ford at 798–804.5 while C/I/L stand off at 1,441 m, the D12 archaeology-weighted reading emerging from behavior rather than encoding; the retreat-crossing edge measured at 767.5–776.5 and GUARDED by (a). CLASSIFIER ARTIFACT RECORDED so it is not rediscovered as a bug: channel-side classification is unreliable south of the polyline's terminus (positions project onto the first segment; the 595.5 "west" reading was this artifact); ford-entry flags are the reliable commitment signal. | Ruled |
| D104 | 07-29 | ROUT PATHING COMPLETED, defect class (D74's routeToSafety; no [CAL] touched, no scenario byte — same stream). Three sub-issues ruled, UNEQUAL WEIGHT stated deliberately: (a) FAILURE DOES NOT WIPE A LIVE PATH — a unit already moving keeps its existing path when the corridor search fails; the least contestable of the three: a retreating unit taken off its own escape route by the rout that should hasten it along it is wrong under any reading, and routing to safety is D74's stated intent. (b) ORIGIN-BUBBLE EXEMPTION — interdiction is not tested within enemyInterdictionRadiusMeters of the routing unit's own position (structural reuse of the constant the corridor test already consumes; no new number). Without it, escape from close contact is impossible by construction — any escape path necessarily begins inside the engaging enemy's radius, so the test could not express the thing it was built to decide (the D98/D103 class: a geometric test failing a behavioral distinction). (c) FAILURE IS NOT A LATCH — a failed search re-attempts on D92's 10-tick blocked-retry cadence (pursuitRepathCadenceTicks, structural reuse on the precedent that ruled that cadence BECAUSE unhysteretic re-evaluation caused pathology; every-tick retry would contradict D92). The success latch is unchanged: a found safety path is held. WEIGHT, stated because the ledger would otherwise be misread: on the measured seed the retreat's shape rests on (a) and (b); sub-issue (c) — the one that NAMED the defect — is the least load-bearing, exercised exactly once with no cascade. F4 STATEMENT: F4's red was the latch's artifact. With routed units mobile, the five-destroyed roster returns WITHOUT re-ruling — the criterion held; the defect chain was the problem; a ruled roster change would have permanently encoded a defect as history. Mechanism on the record: the 842–880 corridor-search cluster is the wing companies routing mobile during the northern fight. N=1 evidentiary closure; N=50 confirmation pending. SEQUENCING: this work order dispatches ALONE, before the D96/D72 ownership ruling (D91-sequencing logic — the twentieth's L/LA sign-flip means batching obscures attribution; latch-first is the safe direction since ownership-without-latch leaves units pinned and latch-without-ownership does not). Ownership follows against the latch-fixed baseline, on the D92 precedent, with the seventeenth's numbers explicitly not carried over. | Measurements nineteen through twenty-one, preserved-probe lineage, all N=1 on the stop seed — the compounding caveat is part of this row: four defects were unwound in sequence on one seed and the interactions are where single-seed reasoning is weakest; the N=50 campaign this row authorizes is the first real test of the chain. NINETEENTH (.claude/immobile-withdraw-probe.mjs, .claude/rout-latch-baseline-check.mjs): one latch under both annihilations — co-a, retreating on a live 8-waypoint ford path, ROUTED at 770, corridor search fails, working path WIPED, frozen 600 m short of the crossing co-m used, k7→44 over 48 minutes; co-g identical at 768.5, k9→45; killed at the rout moments sums to 36, the exact 800 checkpoint, so the entire 73-death excess was two latched companies ground in place (the D72 override the grinder, the latch the pin). Hilltop: hunkpapa-pool latched at 892 takes ZERO fire casualties while routed-immobile; mechanical destruction at 940 converts 135 remaining strength — the bulk of the 204 coalition overshoot. TWENTIETH (.claude/latch-fix-probe.mjs): the latch fix ALONE converts the annihilation into the historical retreat — killed 36 flat from 800, all three companies cross east in the registered retreat window (co-a 756.5, co-m 767, co-g 769), end STEADY; coalition killed 56, inside O5's 36/~60/136; five-destroyed wing roster restored; ford choke EMPTY (honest miss, pointer at pursuit-versus-crossing timing; the choke was empty even during the ford episodes themselves, while crossers were west-side and unprotected — so the east-bank sanctuary invariant may be innocent and the eventual ruling should examine when pursuers arrive, not whether they may cross). TWENTY-FIRST (.claude/latch-cadence-probe.mjs, .claude/latch-cadence-log.mjs): the preview survives D92's cadence digit-for-digit, and the equality is earned — the cadence branch executed once (hunkpapa FAIL 890, one five-minute wait, SUCCESS 895, no cascade); every other corridor search in both modes succeeded first-try under the exemption; co-a never routs at all in the fixed world, so the retreat rests on (a)+(b) and cadence is a corner case. DISCLOSURE bound into the freeze: the twentieth's numbers were produced at every-tick retry and are NOT a prediction; the twenty-first reproduced them exactly at the ruled cadence. | Ruled |
| D105 | 07-29 | CLOSE-ACTION RESOLVER RULED — the melee bout, completing the architecture O8 independently prescribed (D65 shock + D96 cohesion-break trigger = Fox's disintegration model; convergence recorded, corroboration not proof). THREE COUPLED CHANGES, ONE WORK ORDER, ship-together justified by measurement rather than exempted from the sequencing discipline: each alone is measured harmful or inert (26th: standoff fix alone worsened every number; resolver at 25 m alone fires on 221 fleeting one-tick brushes; shock-attacker selection alone reaches a channel with no lethality). (a) COMBAT-pursuit movement standoff becomes meleeRangeMeters — the pursuer may arrive (structural reuse; pursuitCloseRangeMeters 50 parked pursuers at exactly twice melee range). (b) resolveShock's attacker selection accepts a COMBAT pursuer of the other party when no CHARGE-postured attacker exists; the D65 shock formula is byte-unchanged. (c) THE BOUT — event-latched resolution: one shock resolution per pursuer-quarry pair per contact episode, per the 26th's cautionary 2,998 re-shock ticks. RE-TRIGGER: CONTACT-BREAK ONLY — a resolved pair is ineligible for a new bout until its engagement leaves melee range and re-enters; no timer, no cadence (the cadence half was struck at review as an invented semantic doing work contact geometry already does, re-introducing the per-interval re-check shape; bout sparsity, if real, is visible in the realized-count observation and rulable later on evidence). OUTCOME EMERGENCE, NO DISCRIMINATOR, NO WEIGHTS: the 28th–30th measurements killed all three static discriminators the record offers (escape-availability dissolved by D104's own fix; mounted/afoot inexpressible — no horse attrition; surrounded-ness inexpressible — no encirclement; NEGATIVE_RESULTS §7 cluster), so the outcome distribution is ruled to EMERGE from movement and catching, both existing: BREAK (shock > defense × chargeBreakMargin) → defender ROUTED per existing machinery, and THE D81 EXCEPTION FIRES — the defender's wounded convert to killed in the bout, the ruled-narrow sourced exception (Snow: "close-range dispatch of the wounded"; publication-grade); flight is the broken fragment's default (D104's rout pathing works); ANNIHILATION EMERGES from repeated catch-and-bout cycles against fragments that cannot outdistance pursuit (Deep Ravine's shape), escape from those that can (co-m's shape). REPEL (shock < defense × chargeRepelMargin) → the pursuit ENDS outright via existing termination (Wolf Mountain's repulsed rushes; full termination chosen over stand-off-and-return, which would let a repelled pursuer re-close every cadence and grind the repulse's meaning away — the D96-suppression rhyme). MIDDLE → contact continues, no conversion. DRAIN INTERLOCK, ruled: destruction-by-cohesion-floor conversion is REMOVED — the resolver supersedes the conversion job the drain was never meant to carry; the morale/cohesion pressure drain during contact STAYS (it produces the break that triggers the bout); destruction at strengthCurrent ≤ destructionStrengthFloor remains and converts nothing. D81 STAYS INTACT EVERYWHERE ELSE: wounded convert ONLY in the break outcome of a resolved bout — not in flight, not in repulse, not from fire, not on any timer. NO NEW NUMBERS: meleeRangeMeters, chargeBreakMargin, chargeRepelMargin — all existing; no new [CAL]; the no-rate finding realized (an event-shaped mechanism with emergent outcomes has nowhere to hide a fitted constant). DISCLOSURE, stated in-row: THIS IS THE FIRST DESIGN IN THE ARC RULED BEFORE ITS FIRST MEASUREMENT — and why that was unavoidable is part of the ruling: the 28th–30th established that every discriminator the record offers depends on state the model does not carry, so the outcome fork was ruled to emerge precisely because it could not be decided. The exception is the discipline's consequence, not a departure from it. The predictions are sized wider than usual; the over-kill branch is registered LIVE (the 31st's conversion ceiling — tight 1,012 — sits ~3× above the 348 deaths the interlock removes); the stop holds WO-D104's referents deliberately, their meaning sharpened. | Measurements 25–31 and O8, all preserved. 25th: the pursuers are dry — fire is pursuit's only conversion channel and it dies with the quartermaster. 26th: shock breaks men, it does not kill them — melee-close alone raised killed median 45→54, dissolution 348→504, choke still 0; casualty writes exist solely in resolveFire's path (the no-kill-channel fact this row fixes). 27th: the resolver's work is real and unclaimed — 610 episodes at the 50 m ring (the planning bound), 219:2 cavalry-to-coalition (O8's kill direction reproduced from geometry alone, second convergence), zero episodes end in destruction in-episode (clean install), wounded pool median 2–3 but tail fragments carry 38–41 (the Snow scenario verbatim — hence the D81 exception's own prediction, PR-36). 28th–30th: the discriminator hunt — 100/0 flight (escape universal post-D104), 97.7% mounted with 2:1 destroyed anyway (no horse attrition), 0/826 surrounded at ≤120° (no encirclement) — three kills, one pattern; the emergence ruling is the only candidate that never asks the model a question it cannot answer. 31st: the conversion ceiling — tight 1,012 / naive 2,106 across the 45 baseline seeds vs 348 dissolution deaths removed — the over-kill branch is live, PR-31 load-bearing, known before registration. O8: event-shape SOURCED (Fox); two-stage finishing SOURCED (Snow); Fetterman the circularity-free anchor (6/81 gunshot); trauma proportions DECLINED for encoding per adjudication (perimortem/postmortem ambiguity is the report's own governing caveat — the model needs contact-kills, not a cause-of-death split). | Ruled |
| D106 | 07-30 | CAMP-DEFENCE COMMAND OWNERSHIP RULED — one authority for commitment holders. A unit holding an active camp-defence commitment is not eligible for D72 `startPursuit`, COMBAT or INITIATIVE: the commitment's own machinery — D91/D92 selection and switching, D96 closing, D93 release — governs it exclusively until release. JUSTIFICATION, D92 ALONE: the 250 m switching margin was ruled because unhysteretic re-selection thrashed targets; D72 overwriting a held commitment every half-minute is the same pathology arriving through a subsystem that was not in scope when D92 was ruled. Letting it stand means the commitment is not a commitment. This argument was made before any casualty figure existed and is deliberately independent of them all — the 16th's 330-for-330 suppression count established the defect's reality; no number since has been its reason. SCOPE, measurement-selected per the recorded pattern (precedent justifies the principle; measurement selects the scope where it holds): the generalized form — any ordered unit immune to D72 — was measured wrong-in-kind and declined pre-proposal (NEGATIVE_RESULTS §4: it deletes the north; initiative is how the north fights). The steelman that killed it — a unit in contact should respond to what is in front of it — does not reach camp-defence holders, whose commitment machinery already answers contact through D92 switching, D96 closing, and D93 release; "respond to what is in front of you" is satisfied by the commitment, not suppressed by it. FORM: the plain gate. The pursuit-informs-commitment variant (B) is NOT adopted: it degenerated to the plain gate to the digit in every world it was measured in (17th; 33rd deferred on that record), and its informed-switch semantics would enter unmeasured. REOPEN CLAUSE: evidence of commitment-switch starvation — a band pinned on a stale threat while nearer eligible targets stand unengaged, visible in the D92 switching data — reopens B on measurement; the work order instruments exactly that signature. BLAST RADIUS, disclosed with evidence and never cited as reason: the 33rd measured this gate's world at N=34 — Reno killed median 34, coalition 63, hill killed 101 with its mode flipped initiative-dominant (the pools carried ~24% of hill killing), complete wing 5/34, zero stop-class seeds. The radius was understood at ruling time; the D92 argument would stand if every one of those numbers were worse. The wing-completion deficit is explicitly NOT this ruling's subject (34th/35th: the ammunition-starved bout loop; separately owned, its scope constraints pre-stated — break-gated not contact-gated, no rate, Fetterman anchor). | Evidence chain: 16th (D96 fired 330 times and was erased 330 times in the same tick — the suppression this ruling ends), 17th (both dispositions measured in the latch world; B degenerate), 32nd (camp-defence at 0.0% of northern kills — the conflation catch: this ruling's letter and the north's command question are different objects), 33rd (two-scope measurement: this gate's world at N=34 above; the generalized scope refuted and declined into NEGATIVE_RESULTS §4), 34th/35th (the wing-completion deficit attributed elsewhere — ammunition-starved bout loop, 87.6% of routed-core contact time dry — so its numbers are not laundered into this row). Sequencing rationale on the record: this ruling precedes the bundle by SUBJECT IDENTITY, not by the 32nd's 100/0 — the gate's subjects and C's dispersal subjects are the same three pool bands, and designing where the mass goes before ruling who commands it at contact is the split the 20th measured the violence of. Stale-evidence discipline: the 17th's 109→30 and the 20th's 56→99 are both explicitly not carried; the 33rd is this ruling's only casualty evidence and it is disclosure, not justification. | Ruled |
| D107 | 07-30 | CLOSE-ACTION FINISHING RULED — the annihilation outcome, completing the half of O8's Q2 that D105 deliberately did not build. ON THE BREAK OUTCOME of a bout whose defender was ALREADY ROUTED at bout start AND IS ISOLATED, the caught fragment is finished: it is destroyed, and its remaining strength converts to killed per D81's existing terminal accounting — the dissolution timer's arithmetic, fired by a pursuer at contact instead of a clock, which the 34th established D72 had been supplying by accident all along. First-break semantics are unchanged from D105: rout plus dispatch of the wounded. ISOLATION, the scope: no eligible friendly within isolationRadiusMeters (650, the existing [CAL] the morale system already consumes; structural reuse, no new number), where eligible means same-side combat unit, STEADY, not destroyed, and not withdrawn off-field — the withdrawnOffField clause is written into the ruling's own predicate with its origin cited: the thirty-seventh measurement's instrument omitted that filter and counted withdrawn Crow scouts' frozen field-edge positions as steady friendlies, contaminating 56 of 61 sheltered classifications; a lesson learned in an instrument becomes a clause in the mechanism. KNOWN SOFTNESS, measured pre-freeze: the predicate requires STEADY but carries no strength or cohesion floor, so "formed" is approximated — a shattered-but-STEADY remnant could shelter a caught fragment. THE SOFTNESS IS MEASURED HARMLESS (thirty-eighth): all 22 shelterer instances across the 10 non-isolated catches are formed bodies — strength ratio minimum 73%, median 82%, maximum 100%; absolute strength minimum 29, median 37 — the shattered-remnant arm is empty. STEADY is a sufficient proxy for formed in practice; no floor is invented; the mechanism's instrumented shelter-suppression events carry each shelterer's strength, and a suppression by a shelterer materially below the observed floor reopens the formed question on measurement. THE GATE'S NEAR-CERTAINTY, stated explicitly: a routed defender's D65 defense is near-floor, so a catch resolves break almost surely — the break-gate is honest but performs little discrimination at the catch itself; the discrimination lives in the rout (first break, D65's full margins) and the isolation scope. NO RATE ANYWHERE: the fragment's fate resolves as an event per O8's Q2 shape; casualties-per-bout would be a manufactured number inside the grading key. OUTCOME DISTRIBUTION now complete per the sources: flight (the rout escapes, or is caught within reach of formed friendlies and survives mauled), annihilation (the rout is caught alone — Deep Ravine, Keogh's compact mass), repulse (D65's repel margin, standing, its zero-rate a registered observation). FETTERMAN ANCHOR, circularity-free: 81 dead, six by gunshot, the remainder at contact, not all previously wounded. SHELTER ADJUDICATION from the corrected which-friendly pass: seed 647's east-bank catches sheltered by the garrison (co-h/co-d/co-k at 611 m) — the sound case; seed 618's by co-a at 460 m, a recovered near-full formed sister company — ruled RIGHT REASON: rallying mass in every sense the record uses the term (Reno's survivors on the bluffs were exactly that — companies that broke, crossed, and re-formed), and restricting rallying mass to the garrison alone would encode a distinction between formed friendlies that no source supports. SEED 632 DISCLOSED AS EXPECTED BEHAVIOUR, not tolerated damage: a strength-28 co-a fragment caught west, mid-retreat-window, isolated, raising that seed to 68 killed — the historical retreat kill zone producing deaths where the record puts them. CONVERGENCE LABELED: the refinement expressing the restated sanctuary invariant (crossing toward friendly mass ends pursuit; finishing happens beyond the mass's reach) is COHERENCE, not independence — same measurement body, same week, the invariant restated because this refinement exposed the old form's error; weaker than the O7-doctrine/RCOI or O8-geometry convergences and said so. BLAST RADIUS disclosed, never cited as reason: the corrected census bounds the annihilation set at ~198 wing catches across 49/50 seeds (median-9 cores — the finisher working) and two Reno annihilations (622's isolated 8-man straggler; 632 above); the east-bank breaches are excluded by real formed bodies. | Evidence chain, all preserved: 34th (the last ten men — small able cores in contact 33–232 min with nothing finishing them; the emergence loop starves without fire), 35th (87.6% of routed-core contact time is dry — the fork resolved to the missing kill channel), O8 Q2 (the annihilation outcome is the fragment finished, distinct from Snow's dispatch-of-wounded; SOURCED), 36th (the trigger census: 203 wing / 9 Reno catches; the author's first shape killed on its own pre-registered test when five Reno catches ran east at strengths 16–28 — the sanctuary breached; the west-only repair killed on geography before drafting — Deep Ravine is east), 37th + which-friendly (the corrected instrument: wing 97.5% isolated, fragment-sheltering 4/203; the contaminated first run and the bug that produced it preserved beside the corrected runs, the declared-dead-then-revived sequence dated so neither can be reconstructed as the other; METHODS' which-one-specifically practice entered from this measurement), 38th (shelterer strength: 22/22 formed bodies, ratio min 73%, strength min 29 — the softness harmless, the reopen signal instrumented). Sequencing, stated plainly: D107 PRECEDES THE BUNDLE BECAUSE ITS SCOPE CONSUMES ONLY EXISTING STATE — no feature extents, no frontage, no encirclement, no occupancy figure; isolation-by-existing-radius (650, already a consumed [CAL]) sidesteps entirely the dependency the discriminator hunt feared, in which the finisher would need spatial state only the bundle supplies. The 28th–30th killed three discriminators for wanting state the model does not carry; this one wants none, and the row records why the dependency does not bind rather than leaving it implicit. The bundle's northern registrations also cannot be sized without this ruling's campaign. The 26th's caution governs the registrations: the direction of risk is over-kill, the valley-holds leg is scored, and the stop holds its referents. *Campaign annotation (07-30, appended at acceptance per adjudication):* the WO-D107 campaign's observed shelterer floor was 29/40 = 72.5% against the thirty-eighth's registered 73% — within half a point of the reopen signal. Ruled rounding-equivalent (the same 29-strength unit at a marginally different denominator moment) and NOT fired; stated explicitly rather than silently, because a reopen signal that comes within half a point of firing is worth a sentence. The signal remains armed at the registered floor. | Ruled |
| D108 | 07-31 | BENCH EXTENT AS GOAL GEOMETRY RULED — the lip, not the area. Camp-defence goals for `scenario-bench` distribute along the Bench's lip — the forty-fourth measurement's continuous eastward edge arc — instead of converging on the point coordinate. ALLOCATION AT BAND GRANULARITY: the arc's cell list is partitioned into equal contiguous segments among the bands currently assigned to the feature, each band's goal the middle cell of its segment; partition emerges from occupant count and arc length. NO PER-MAN FIGURE, NO NEW `[CAL]`: O7's Q2c returned UNRESOLVED on warrior linear spacing and Q3a returned "does not exist" on area-per-man — the mechanism therefore never consumes either; this is the O8 shape (where the literature returns no number, the mechanism consumes state, not a knob). Any future per-man refinement requires new sourcing, not a parameter. PARTITION ORDER BY BAND ID: deterministic and auditable; approach bearing is the physically plausible alternative and is SET ASIDE AS UNMEASURABLE rather than as wrong — no current instrument distinguishes the two; a future measurement may reopen it. SEGMENT BOUNDARY OWNED AS CONVENTION, not claimed as sourced: the segment is the edge-cell set extracted under D90's own relief criterion (shelf `|e−e0| ≤ 3.5 m`; edge = drop >3.5 m within 30 m outward; 10 m grid) at bearings 45–135 from the bench point within 200 m — the forty-fourth's stated window. Anchors, named: the arc's measured continuity (103 cells, 260 m north–south, zero gaps >20 m) and the demand footprint (41st: all pressure within 30 m of the point). The line is drawn where the instrument found continuous edge facing the fight; it is a 1-D convention on a measured feature, which is categorically weaker than the dead 2-D convention (an invented closed polygon) and the row says so rather than implying otherwise. DERIVATION, NOT DATA: the lip is a pure function of existing terrain plus the existing bench point — D102's `frontageMeters` class. No scenario byte changes; the mechanism is reseed-free; BENCH EXTENT LEAVES THE DATA COMMIT, which shrinks to D100 foothills plus O5 byte corrections. Deciding argument recorded: extent-as-scenario-cells would have spent the reseed freezing an invented polygon into the scenario; deriving a segment of a measured rim spends nothing. REGISTERED DIRECTION, pre-stated: all measured demand stood 51–100 m BEHIND the lip, so this ruling moves the stand FORWARD to the edge — closer to Reno's line, shortening stand-window ranges (39th referents: 151.9 centroid / 150 effective), and a worse Reno casualty picture is the named expected consequence (PR-15 shape; PR-53(b) registers the direction; the standing stop owns the tail). ARTIFACT-BASELINE LINE, adjudicated for this row: D108 moves the valley stand again, so every northern baseline figure inherits one more round of "measured against a world that no longer exists" — the caveat has been shrinking since the eighteenth and was partly lifted at D107; this round re-ages it by one, and the bundle's registrations are not to over-claim the northern baseline's stability when sizing against it. | Evidence chain, all preserved and committed: 39th (frontage woke with the line — the D107 valley's stand-window fire already shortened ~68 m median by sourced frontage; the north frontage-free), 40th (all occupancy demand on unrepresented ground — the vacuous never-binds verdict that reframed the fork), 41st (true pressure 708, four pools exactly, all within 30 m of the point — stacking total; the 40th's 620 was an instrument undercount and the density knife-edge at 4.57 was its artifact), 42nd (overflow's destination does not exist: sole candidate a 73-cell west sliver, unreachable 375/375 — the pre-stated gate fired and the option died before drafting), 43rd (the fifteenth design death: extent cited to a source that never described extent — NEGATIVE_RESULTS §4, METHODS §6's third standing practice; the DEM gives the Bench ONE physical edge and open shelf on every other bearing; area does not exist; packing never binds at any candidate density; the stacking is BEHAVIOURAL — point-goal convergence — which is exactly what this ruling changes), 44th (the lip traced: continuous, 260 m, facing 65° toward the channel and the line — D90's own downhill-break selection signal extracted as cells; the gate on this reframe, passed as pre-stated). Q2c/Q3a verdicts honoured by construction rather than worked around. Sequencing: this is the first mechanism of the unbundled bundle, reseed-free against the D107 baseline, same stream; the data commit (foothills + O5 bytes) opens behind it and takes the one stream break alone. *Campaign annotation (07-31, appended at acceptance per adjudication):* ACCEPTED with the misses as consequences. PR-51 hit decisively (peak-at-point 615–708 → 158–160 in 50/50; zero audit violations in 34,182 ticks) — the stacking C was created to fix is gone; F4 green again, the halt-tree red having been the uncorrected extraction exactly as Amendment 1 anticipated. PR-52's miss is recorded with its diagnosis stated plainly AND its authorship: the adjudicator registered "forward to the edge" as the expected direction and centroid ranges rose (151.9 → 163.39) — **the pre-D108 stack sat at the demand-optimal point, so any dispersal from it lengthens the median; un-stacking necessarily trades a closest-point for spread positions. The adjudicator's fourth direction-wrong registration, recorded as such rather than absorbed** — and the structural consolation stands: because it was registered in advance, the miss is informative rather than reinterpretable. THE SANCTUARY BREACH (seed 18760633, one in fifty, verified digit-exact): co-m crossed ~790 ROUTED, recovered SHAKEN, was moving toward the garrison standing STEADY at 865 m, and was annihilated 215 m outside the 650 m isolation radius — **D107's predicate executed exactly as ruled; nothing malfunctioned.** What diverged is the seam named when the invariant was restated: the meaning is "crossing toward friendly mass," the implementation is a static radius snapshot; they agreed in every D107-world seed and D108's changed timing pulled them apart — the correlate-versus-cause pattern's third instance (east-bank-as-sanctuary, the no-combat oracle rule, now isolation-as-proxy). **NAMED BENCH ITEM, recorded not repaired: approach-vector versus radius.** One seed is not the instrument that moves a ruled mechanism; if the divergence recurs at higher frequency in the data commit's world, that is when it earns a ruling. PR-54 stands as scored: the ±5 was a labelled judgment and −7 exceeded it — a labelled judgment exceeded is a miss, not a threshold to revise; the wing-completion question was separately owned before this round and stays owned. And the composite line, ordered into the record: median flat at 57.63, mean down 57.28 → 56.15 — six rounds of rises, then a round that fixes a real defect and does not move the score. **The honest instrument doesn't only pay out.** *Dated annotation (2026-08-02; original row text untouched): PR-53 SCORED, both legs, MISS — omitted from the acceptance annotation by the adjudicator, the omission found by a provenance question during a resume-brief review rather than by a reader. (a) Sanctuary hard leg: MISS — the breach is the one narrated above (seed 18760633, verified digit-exact); this line adds the label the narration lacked; the named bench item and its recorded-not-repaired disposition stand unchanged. (b) Reno A/G/M killed median was registered to RISE from 32; it FELL to 31 — campaign summary median under the registered floor-quantile convention, interpolated 30.5; a fall of one on a coarse lattice, stated so it does not read more precise than it is. THE ADJUDICATOR'S FIFTH direction-wrong registration, recorded as such and not folded into PR-52's fourth: one root — the stand moved away, not forward — producing two independently-registered wrong directions is two misses; folding would let a single diagnosis absorb an arbitrary number of registrations, and the discount runs in the adjudicator's favour. The two instruments agree against the registration: geometry (PR-52, centroid ranges 151.9 → 163.39) and casualties (this leg, killed fell) — a cleaner refutation of "forward to the edge" than either alone. Fifth adjudicator design-or-direction error.* | Ruled |
| D109 | 08-02 | THE DATA COMMIT IS SPLIT BY CHANGE CLASS — two stream breaks replace the one-commit bundle, and the budget is re-denominated: a reseed costs one recoverable N=50 re-measurement; batching costs attribution, which is not recoverable (D104 dispatched alone and D106 sequenced by subject identity on the same logic). CLASSES, three, named by the property that earns the break: **INERT-CLASS** — bytes consumed by no engine or scoring path; two present members, provenance bytes (confidence, sources, notes) and unreferenced-landmark bytes, with the D110 pins as the instruments that keep membership honest (`hashScenario` covers every byte, so inert bytes cost a break like any other — they are the class that can be PROVEN inert, not a class that is free); **VALUE-CLASS** — grading key + `[CAL]` envelope (the coalition casualty band, the pony strength; scoring moves, behavior does not at current settings); **ENGINE-INPUT** — anything a mechanism consumes; none ride this sequence (D100's landmark vehicle is id-lookup-only and lands inert-class by construction, pinned). SEQUENCE: **break 1 (D111)** carries the inert class alone — B3/B2 bench downgrade, B4 timeAnchor reframe, the D100 foothill landmarks (gated behind the D90 dated annotation, landed `3a2bd80`) — registered in the PR-55-INVERSE shape: this commit changes the stream and nothing else; the N=50 envelope median and component counts reproduce the D108 world within the composite's quantization lattice. A miss is ambiguous in advance between lattice-exceeding stream noise and load-bearing bytes — both findings — and the pre-committed response is a per-edit bisect over the payload, which is the second reason the payload stays small. Break 1's campaign is the re-baseline AND the measured pure-reseed noise floor: the null distribution every later break is judged against. **Break 2 (D112, conditional on break 1 passing)** carries the value class — killed 36/60/136 source-per-bound with 300 removed as the discredited bound; wounded 160 flat on the located source's own tier, bounds recorded UNAVAILABLE not agreed, the Red Horse coupling noted (one testimony supplies the killed high and the wounded figure; those bounds are not independent); pony strength = total herd 15000/15000/25000 LOW with the dawn-visible subset moved to the Crow's Nest observation event — plus the O5 180-claim transcription (inert-class, rides any break at zero attribution cost once break 1 proves the class inert), plus the us-7th aggregate question opened by the sixth catch (D110 evidence column: does `sideCasualties` gain a us-7th entry; does the scenario carry the hilltop-inclusive 268). D112's predictions REGISTER PIN (a) FIRING: the ratio pin is designed to turn red when the coalition band moves, and a pin that never fires is indistinguishable from a pin that isn't wired. BOUNDARY RULING BINDS PER BREAK: every absolute figure re-rolls at each break; re-measure before registering against any pre-break number; envelope-median comparisons only across breaks (D31a); each break's re-baseline sweep precedes any registration leaning on a number. | Adjudication trail committed this session: probes `14c785c` (exclusion branch 0/50; 291 strandings; Varnum arithmetic), annotations `3a2bd80` (PR-53 scored fifth; D90 demoted; D98 inference narrowed; NR §4 late row), resume surfaces `533729d`/`4198fcc`. Deciding argument recorded: an isolated inert-byte break measures the noise floor that makes attribution possible at all; without it, "did the foothills move the envelope or did the stream?" has no answer. *Dated annotation (2026-08-02, before break 1 dispatched; original row text untouched): INERT-CLASS MEMBERSHIP IS PER-FIELD, ATTESTED BY INSTRUMENT — NOT PER-CATEGORY. The row's phrase "provenance bytes" as a class member was a correlate, not a cause (the session's fifth such instance): confidence values DO gate scoring — observation events below MEDIUM are scoped `excluded-confidence` (exam.ts:50–54), and `isCalibrationExcluded` (score.ts:144) scopes checkpoints, side targets, and assertions, with HIGH filters at :182/:338. The class is properly "bytes verified unread by engine and scoring at their specific call sites," which break 1's payload satisfies by verification, not by category. CONSEQUENCE, absorbed before it hardened: the O5 180-claim transcription is NOT inert-class as a body. It SPLITS, mechanically: any claim whose edit touches a confidence value on an observation event, checkpoint, side target, or assertion is VALUE-CLASS and rides break 2 under its own registration, with the expected direction on C1/C4 stated in advance; purely textual edits — loci, notes, source keys — remain inert-class. CC computes the two counts from the O5 verdict table before break 2 drafts. The reclassification is why D110's pins were the right instinct — membership needs an instrument — and the transcription now needs one too.* | Ruled |
| D110 | 08-02 | THE PRE-BREAK PASS — three pins, one gate carried twice, two METHODS lines; reseed-free (zero scenario bytes; scenario content hash `ba288f09` byte-identical is the WO's acceptance check, a change is a STOP). PIN (a), the ratio duplicate, RESCOPED BY ITS OWN FIRST CHECK: `combat-config.ts` `KILLED_TO_WOUNDED_RATIO_RANGES` holds two sides against `calibration.sideCasualties`' ONE (coalition only), and the us-7th range is MIXED-PROVENANCE ACROSS ITS BOUNDS — low 235/60 and high 285/45 are cross-products of the summed per-company `calibration.casualties` table, while best's numerator 268 is the M5-SPEC hilltop-inclusive figure and appears nowhere in the scenario, eight characters from the 253 the table sums to. Recorded as the SIXTH CATCH, not touched here: `range.best` is consumed (`combat-config.ts:231`), so reconciling 268 is behavioral, and the byte-side answer belongs to break 2 with the other value-class questions. The pin as it lands, three assertions, all green today: (1) coalition low/best/high = the conservative cross-products of `sideCasualties['lakota-cheyenne-coalition']` (low K.low/W.high, best K.best/W.best, high K.high/W.low); (2) us-7th low and high = the same cross-product form against the side-scoped summed per-company bounds; (3) us-7th best = the literal 268/52, pinned by value as a DECLARED EXCEPTION with its outside-the-scenario provenance stated — anyone silently reconciling 268 to 253 turns CI red and argues for it. Every `sideCasualties` key must appear in the ratio map (the direction that holds). PIN (b), the camp-defence candidate set: scenario-derived `DefenseFeature`s reaching `eligibleFeatures` equal the ruled set — today exactly `scenario-bench` — so any future `coverFeatures` entry forces a ruling at the moment of addition instead of silently becoming camp-defence-eligible (the D100 vehicle finding: `coverFeatures` is defined entirely by its one consumer; declaring IS making eligible). PIN (c), landmark consumption: the set of landmark ids consumed engine-side equals the set referenced by orders, checkpoints, calibration assertions, and exam items — preferred instrument a tracking-proxy test over a run plus exam plus scoring; if the weaker source-level fallback lands instead, D111's row MUST say its inertness-by-construction claim rests on a grep rather than an instrument (consequence pre-committed here, same reason the bisect response was). THE GATE, CARRIED TWICE because hooks don't travel and tests do: a pre-commit hook rejecting BOM and CRLF in tracked text AS STAGED — **index bytes, never working copies**: Windows working copies are legitimately CRLF under autocrlf, and a hook that rejects the repo's own normal state gets disabled within a week — AND the same predicate as a suite test binding every clone, CI, and Codex exec; the hook is the courtesy that fails fast, the test is what makes the rule binding on everyone. The `8759dd0` scar promoted from prose to gate; the trap pulled again this session on a relay copy, caught pre-repo. METHODS §6, two prose lines, same duplication rule the resume surfaces now follow: the fallback-clause parenthetical becomes a pointer to the NEGATIVE_RESULTS §4 row landed `3a2bd80`, and the YIELD SENTENCE enters beside the three standing practices: five catches in one session arrived through unintended channels — a probe answering a different question, a gate killing its author's own reading, a provenance question, an executed reference, a fresh clone — and zero through the check aimed at them; the practices' yield has been dominated by incidental catches, and their value is that they put people in positions where incidental catches happen: the incidence is manufactured by running them. SCOPE DISCIPLINE: no engine-source changes, no campaign, no predictions register entries (the one genuine prediction in this material — pin (a) fires at break 2 — is registered in D112's predictions commit, where it belongs); pins must pass green as rescoped, the suite stays green, the hash stays `ba288f09`; any pin red on the current tree beyond the rescoping above is a finding that halts the WO for adjudication, not a test to fix. | Pin (a)'s original draft was itself red on the current tree — the identical-key-sets assertion fails on `sideCasualties`' single key, and chasing the us-7th values produced the mixed-provenance finding (Fable's catch, pre-freeze; CC verified: one key; per-company sums K 235/253/285, W 45/52/60, float residue 2×10⁻¹⁰ — hence the WO's integer-recovery rule and side-scoped sum, Fable's amendments 1–2; amendment 3 makes the gate test fail loudly where git is unavailable rather than skip silently). The 268-vs-253 divergence is REGISTERED AS AN OPEN QUESTION FOR BREAK 2, deliberately untouched here. Other pins verified this session: candidate-set semantics at camp-defense.ts:211–233, :312–334; landmark consumption at envelope.ts:33, exam.ts:121, objectives.ts:25/:32, score.ts:314 (all id-lookups; pin (c) turns the survey into an instrument); autocrlf behavior observed on the four commits landed 08-02. Frozen WO: docs/WO-D110.md. *Acceptance annotation (2026-08-02, appended at adjudication):* ACCEPTED after a STOP the work order manufactured deliberately — the fifth STOP this project has held and the first whose stopping condition was built by the same WO. The gate's first run found SEVENTEEN committed BOM'd files, all `.claude/`, all sediment of the sessions the scar lived through in prose; ruled REPAIR FORWARD (a grandfathered gate would assert a falsehood; the `8759dd0` precedent governs immutable commit subjects, not repairable file contents) and repaired at `aa7cc36`, each blob verified byte-identical minus exactly `EF BB BF`. Completion verified: suite 22 files/119 tests green; pins 3/3 as rescoped; pin (c) on the preferred tracking-proxy instrument, so D111's inertness-by-construction claim rests on an instrument and the pre-committed weakening does NOT trigger; both gate vehicles landed; hash `ba288f09` byte-identical; METHODS carries the yield sentence and the counterexample clause as one paragraph. **CODEX COMMENDED, three counts:** the STOP conduct (did not exclude what it had just found, did not repair, stopped mid-sequence as ordered, flagged three ambiguities rather than guessing); the audit catch against the verifier's resume prompt; the audit catch against the adjudicator's required count sentence. A worker that audits its own work order's premises is doing the job the STOP culture exists to produce. **CC's mis-sweep, its own line:** the repair commit was ruled to carry seventeen files and carried twenty-two — Codex's five staged files swept in by a pathspec-less commit, an unverified pass entering the ledger under a message describing seventeen; unwound additively at `2b380ec` with full disclosure, named by CC before adjudication. Diagnosis at the ruled weight: caught by reading the commit stats, thirty seconds late instead of thirty seconds early. **The adjudicator's sixth error of the session, inside its own rule:** the required count sentence's "31" came from `grep -qU $'\r'` in a POSIX shell — the bashism never expanded, the scan never tested for carriage returns, and the plausible aggregate silently dropped five members (the thirty-seventh measurement's shape, second instance) — withdrawn and replaced at acceptance with reproducible figures and a named predicate (report §4a: 37 excluded / 36 CR-bearing / 1 clean, `b'\r' in blob` over index bytes at HEAD). **Session catch count: EIGHT** — five incidental through unintended channels, one manufactured by the gate on its first fire, plus CC's mis-sweep and the adjudicator's broken scan; what this means is already said in METHODS §6's yield-and-counterexample paragraph, which this row points at rather than restates. | Ruled |
| D100 | 08-02 | THE FOOTHILLS DECLARED — as landmarks, the reservation spent on the vehicle that survived the scrutiny. Three entries in `terrain.landmarks` — `foothills-1` (45.50579, −107.40259, +21.1 m), `foothills-2` (45.51317, −107.41571, +31.7 m), `foothills-3` (45.50827, −107.41294, +33.0 m) — D90's in-frame summit identification of O6's "low round foot-hills, W valley edge," confidence MEDIUM, source RCOI (Varnum) with the O6 identification relayed in the locus and note per the house relay convention (the `turnoutDelayMinutes` pattern — cite the underlying source, note the relay; a commissioned deliverable is not a `WEB_MISC` forum tier), the D88 convention governing (O6 lat/lon non-authoritative, re-derived in-frame). MEDIUM against the Bench's LOW, asymmetry deliberate and answered in the note: the Bench's LOW is an attribution judgment O5 B3 undercut; the foothills' attribution is not in doubt — O6 names the set, Varnum names foot-hills to the left, the DEM shows exactly one western rise — and what is weak, point selection within a region, is declared as convention rather than discounted as confidence. VEHICLE, adjudicated: landmarks, NOT `coverFeatures` — the engine consumes landmarks by id-lookup only (envelope/exam/objectives/score, verified and now PINNED by D110 pin (c)), so an entry referenced by no order, checkpoint, assertion, or exam item is consumed by nothing: **inert by construction, with a standing instrument attesting it** — while `coverFeatures` is defined entirely by its sole consumer (camp-defence), where declaring IS making eligible; the probes that killed that vehicle (held-exclusion 0/50, harm 0/50, and the 291-stranding population that would consume any feature entering radius) are committed at `14c785c`. PURPOSE, stated before the byte was spent (the STOP's demand): display and ledger — the map's valley story gains its west-edge anchor, and the label's story is Varnum's own words with no error hypothesis, per the D90 row's dated annotation (region-level corroboration, recorded as a demotion): warriors fired from this western rise at what the troopers called 800–1,200 yards, and every bullet fell short. NOT claimed: that these points lie at Varnum's stated range (they are summit selections on a continuous rise, a convention owned like D108's segment boundary). The UI `important`-set addition and banner correction are WO-D111's code half. Consumption forecast registered as PR-60; these bytes ride break 1 (D111) and are part of its five-edit bisect payload. RESERVATION HISTORY, for the ledger: reserved 07-28 (D101), STOPPED 08-02 pending a stated purpose, vehicle adjudicated against a probe and a pin, gated on the Varnum reconciliation, spent here. | Evidence: D90 row + dated annotation (2026-08-02); D110 pin (c) (the inertness instrument); probes `14c785c` (`d100-exclusion-branch`, `d100-featureless-origin`, `varnum-band-relief`, `varnum-band-extent`); O6 §D; PR-60 registration. | Ruled |
| D111 | 08-02 | BREAK 1 — THE INERT-CLASS COMMIT, the stream broken on purpose with the instruments standing. Payload, five edits, all inert-class per D109 as annotated (per-field, attested at call sites): (1) bench provenance confidence MEDIUM→LOW with the B3/B2 substance in the note (executing D101's deferred byte); (2) the B4 timeAnchor reframe (Chicago watch time; no offset encoded, UNRESOLVED until read from Gray's page); (3–5) the three D100 foothill landmark entries. Registration: **PR-57..63, committed alone ahead of this row** — the PR-55-inverse core scored at exact median (57.63, tie-rule-independent), derived mean bound (±1.02 pp = 2 SE from the committed campaign's per-seed sample SD 3.60 pp), four component medians exact with the non-independence stated; derivation invariance; landmark inertness on pin (c); standing forms with threshold-independent watch counters (the approach-vector counter reported as a TABLE under D107's own predicate, the count derived from it); the re-baseline and noise floor as observations; seed-fragile F4 on the envelope only and the D80 criteria frozen across the reseed. Pre-committed miss response: per-edit bisect over the five, both outcomes findings — and the reading order is fixed in advance: THE CONSUMPTION INSTRUMENTS RUN FIRST and answer "load-bearing" exactly; stream noise is what remains after the instruments come back clean, not what is concluded because they are expensive. A clean return is written as a RESULT, not a formality: the measured noise floor is the asset break 2 is built on. Break 1 passing licenses nothing about break 2 beyond that floor — membership is per-field, and this payload's inertness is verified, not categorical. **One measured retirement, recorded here per adjudication: the accidental reseed's 0.03 pp mean movement is retired as a calibration** — the derived bound shows the mean's real sampling spread (SE 0.51 pp) and the 0.03 was a lucky draw; METHODS §6 has carried that number with more weight than it earned, and the weight now rests on the derived instrument instead. The campaign is the re-baseline AND the pure-reseed noise floor — the null break 2 is judged against; nothing registers against a pre-break number afterward (boundary ruling, binding per break). Code half, reseed-free: UI `important`-set gains the three foothill ids; the map banner's "LOCAL SUN TIME" becomes non-committal ("SCENARIO CLOCK"), bound to B4's wording — the UI must not assert a resolution the record withdraws in the same commit. Oracles refresh once each, cause-documented; the new hash is recorded as the stream id; the stop re-arms in registered form. | Frozen WO: docs/WO-D111.md. Registration commit: PR-57..63 (`1e0f4bf`, predictions alone, no implementation, no results; git timestamps establish order). Bound derivation verified independently by adjudicator and verifier (sample SD 3.6038, SE 0.5097, ±2 SE 1.0193 → ±1.02 as registered; median tie-rule-independent, 25th = 26th order statistic). *Dated annotation (2026-08-02, corpse drift — a defect recorded with its evidence, not repaired, not scoped): a DESTROYED unit accrues movement. Seed 18760627, stream `8e28552c` (probes `a6a4663`): co-m, destroyed at tick 1497, holds its death position (6951.47, 10965.34) through tick 1509, then drifts east at ~37.5 m/tick — gallop speed — reaching (7170.68, 10965.41) by tick 1515. THE TRIGGER OBSERVATION, named for whoever scopes it: the drift begins at tick 1510 — a NEIGHBOURING company's destruction tick (co-g), not co-m's own — which suggests the write is coupled to something processing the unit list at another unit's destruction. BLAST RADIUS UNKNOWN, stated rather than estimated: this has been silently true for an unknown number of past rounds, and every measurement that reads unit positions after destruction may have read drifted values; the WO-D111 approach table is clean only by the accident of recording at the destruction tick (its watch-counter hold was lifted on that verification — the 32/36/0 split is computed on consistent data). Twelfth catch of the session, found while resolving the ~220 m position discrepancy the adjudicator elevated. It goes behind the noise floor with the STEADY-shelter question; no probe, no repair, until break 1 is complete.* *Acceptance annotation (2026-08-02, appended at adjudication):* ACCEPTED at 50/50 on stream `8e28552c`, after three adjudicated STOPs (editor-time byte locus / payload assertion / campaign tripwire), each honored, each amended, all three histories preserved in the report. **PR-58 HIT, all legs — and the noise floor has two components, not one:** median 57.628205% unmoved to the digit across a full reseed; mean +0.5641 pp, inside the derived band at 1.11 SE — as typical a draw as the instrument could produce. Read together: THE DISTRIBUTION DEMONSTRABLY MOVED (the mean says so) WHILE THE MEDIAN DID NOT BUDGE, because the composite median is a rank statistic on a concentrated lattice. Break 2 therefore judges MEAN movements against ~0.5 pp of pure-reseed noise and treats median-unchanged as UNINFORMATIVE rather than reassuring. The derived SE (0.5097) predicted the observed movement (0.5641), retiring the 0.03 pp figure properly: the mean's real spread is thirty times what METHODS §6 had been implying, and the derived instrument is confirmed against a measurement. PR-57/59/60 HIT on complete N-independent instruments; PR-62's artifacts enter as THE NEW BASELINES; PR-63: F4 29/50 with the baseline seed green, and the D80 criteria — unmoved across a stream break — selected 18760621 from five candidates: the pre-registration record survives the reseed, which is the outcome that makes six empty rounds mean something. Seed 627 completing at exactly 135 and scoring 43.53% as the distribution's minimum is the right behaviour: an annihilation seed should be the worst seed. **PR-61(a) MISS — and the recurrence is now CROSS-STREAM:** seed 18760629, tick 1604, co-m annihilated EAST, one in fifty — against one in fifty in the D108 world, plus the stop seed's three west-side annihilations through the same STEADY-shelter predicate. Stream-specific luck is ruled out as thoroughly as this project can rule anything out; "recurrence at frequency earns a ruling" is satisfied; **the approach-vector bench item is PROMOTED from watched to rulable and heads the mechanism queue — behind break 2, per D109's sequencing, so the work measures reseed-free against the final data world.** Registered for break 2's predictions draft: the hard invariant stays at zero (a validity claim about a historically-forbidden outcome, not a forecast) AND a calibrated frequency (~1 in 50) registers as a separate expectation leg — registering zero a third time when the measured rate is 2 in 100 would be pre-registering an expected failure. CC's coordinate observation — seed 629's co-m dying at (8339.68, 11112.11), where the D108 world's garrison cluster stood — recorded flat, no diagnosis, into the bench item's file. **The adjudicator's EIGHTH registration miss, with a FOURTEENTH catch underneath it:** "the completed envelope will be worse than the partial" named no statistic — on the median it was wrong (identical), on the mean it was right (−0.7283 pp, the predicted direction and roughly the magnitude) — and a registration that doesn't name its instrument can be scored against whichever statistic flatters it afterward, the reinterpretation this register exists to forbid. Scored MISS on the median (PR-58's headline instrument, the natural referent); the mean's confirming movement recorded as a fact that does not rescue it; the ambiguity recorded as its own defect — which-one-specifically class, the adjudicator's third such this session. The failure's nameable reason, kept: extreme values don't move medians — the distribution's concentration absorbed the minimum without the middle shifting, so THE PROPERTY THAT MADE PR-58 HIT IS THE PROPERTY THAT MADE THE REGISTRATION MISS. Codex: sixth consecutive STOP honored across the arc. **The line reserved for this row: fourteen catches across the arc, four of them the adjudicator's, three of those caught by the verifier and one by the implementer — the arrangement working as designed, which is that no role in the chain audits itself.** Break 2 drafts against a measured noise floor — the first time in this project's history that a data commit will be judged against a known null instead of an assumption. | Ruled |

*M4 numbering traceability: M4-SPEC's proposed D60–D68 maps in spec order to ledger D62–D70 because D60 coordinate and D61 pony herd were already assigned.*

## 3. Artifacts Delivered

| Artifact | Date | Notes |
|---|---|---|
| `scenario-schema.ts` | 07-14 | Battle-agnostic scenario schema (TypeScript). Root: meta, clock, terrain, weapons, tacticsProfiles, sides, leaders, units, orders, checkpoints, observationEvents, variants, calibration. |
| `codex-report-m4a-d74.md` | 07-18 | Post-D74/D75 M4-A closeout report: six-column F5 scorecard, mechanism trails, F1-F6, quartet, and CAL provenance. |
| `little-bighorn-research-prompt.md` | 07-14 | Deep Research prompt, deliverables A–K keyed to the schema. |
| Research output (Chuck-supplied) | 07-14 | Full A–K dataset + gaps/reliability report. Received and reviewed; quality strong. |
| `IMPLEMENTATION_HISTORY.md` | 07-14 | This file. |
| `PRD-battle-simulator.md` | 07-14 | Product requirements + reproducible per-battle pipeline. |
| `TRANSCRIPTION-DECISIONS.md` | 07-14 | Frozen judgment calls for O1 assembly; authoritative on conflicts with research doc. |
| `CODEX-WORKORDER-O1.md` | 07-14 | Work order: scenario transcription + validation harness. |
| `CODEX-WORKORDER-O1B.md` | 07-14 | Work order: schema v0.2 + D19 ambiguity burn-down. |
| `M1-TERRAIN-SPEC.md` | 07-14 | Terrain pipeline spec (D20–D25); §8 gates became the M1-A exit tests. |
| `CODEX-WORKORDER-M1A.md` | 07-14 | Work order: terrain pipeline + loader + validation gates. |
| `scenario.json` v0.2 | 07-14 | Little Bighorn scenario data; 33 units / 18 leaders / 22 orders / 7 variants; 37-flag ambiguity ledger. |
| `codex-report.md`, `codex-report-o1b.md`, `codex-report-m1a.md` | 07-14 | Codex execution reports with verbatim proof output and AMBIGUITIES sections. |
| `data/terrain/little-bighorn-1876/` | 07-14 | Processed terrain assets: two-tier elevation/slope grids, hillshades, 5 m contours, cover + movement-cost layers (D29 .br packaging, 07-15). |
| `M2-ENGINE-SPEC.md` | 07-15 | Engine-core spec (D30–D37); gates E1–E6 define M2 exit. |
| CODEX-WORKORDER-M2A.md | 07-15 | Engine-core work order (D30–D37 frozen). |
| reports/e5-baseline.md | 07-15 | Movement-only checkpoint baseline (gate E5); the score M4 must beat. |
| `O4-CORRECTIONS.md` | 07-16 | Approved O4 Tier A river/timber/village geometry rulings D41–D44. |
| `o4-corrections-data.json` | 07-16 | O4 Tier A channel, ford, timber, village, and Garryowen correction geometry with per-item provenance. |
| `reports/e5-baseline.md` (O4-A regeneration) | 07-16 | Movement-only checkpoint baseline regenerated at tick 2160 after Tier A geometry integration. |
| `reports/c4-observation-exam.md` | 07-16 | M3-A C4 movement-only observation exam with per-event margins, factor audit, O3 informational rows, mismatches, and global tuning audit. |
| `CODEX-WORKORDER-M3A.md` | 07-16 | Headless M3-A work order for spotting, believed picture, DEFEND_CAMP, D51, C4, and gates V1–V3/V7. |
| `CODEX-WORKORDER-M3B.md` | 07-17 | App-shell work order for the map, POV viewshed, belief modes, decision index, and gates V4–V6. |
| `app/` | 07-17 | First static-export UI shell: full-viewport period map, worker-backed timeline, POV viewshed, belief-vs-reality controls, and chronological decision index. |
| `codex-report-m3b.md` | 07-17 | M3-B execution report: V4 timings, V5 exact parity 200/200, V6 counts, gzip weight table, D50 perf finding (→ D56). |
| `docs/screenshots/` (m3b-*) | 07-17 | First rendered proof: full map 15:40, Custer POV at Weir vicinity with belief on, decision index at the Cooke note. |
| `.claude/launch.json` + `.claude/static-server.mjs` | 07-17 | Dev-only static-export preview tooling (dependency-free node server on :4173) used for live UI verification. |
| `CODEX-WORKORDER-M3C.md` | 07-17 | UI-only D57 work order implementing the first-user comprehension and navigation fixes with gates U1–U3. |
| `docs/screenshots/` (m3c-*) | 07-17 | U2 visual proof set: three M3-B frames re-captured at identical viewport framing plus the Reno Hill 16:20 viewshed contrast before/after. |
| `CODEX WORK ORDER-M3D.md` | 07-18 | UI-only D58 work order for the independent viewshed switch and display-only marker decluttering. |
| `docs/screenshots/m3d-reno-battalion-ford-a-1420-decluttered.png` | 07-18 | Zoomed Ford A proof showing Reno's company-granular battalion fanned for legibility with cosmetic tether cues. |
| `reports/o3-sweep.md` | 07-18 | O3-A Phase 1 41×41 sensitivity sweep: 1,681 cells, per-criterion scoring, full-ray coverage proof, top 10, and separate OSM-vs-best result. |
| `codex-report-o3a-phase1.md` | 07-18 | O3-A Phase 1 proof: terrain extension, core-tier byte identity, sweep excerpt, ambiguities, deviations, and green quartet. |
| `codex-report-o3a-phase2.md` | 07-18 | O3-A Phase 2 proof: D60 coordinate application, C4 promotion/exam, E5 statement, ambiguities, deviations, and green quartet. |
| `codex-report-m4a.md` | 07-18 | M4-A first-fight STOP report: unfrictioned mutual annihilation, full F5 finding, [CAL] audit, and the correctly invoked escalation that led to D71. Retained as historical record. |
| `codex-report-m4a-d71.md` | 07-18 | Post-D71 frictioned-baseline report: hilltop preserved but Custer wing survives; three-column F5 evidence and failed-asymmetry mechanism diagnosis; second binding STOP, no additional knob. |
| `codex-report-m4a-d72.md` | 07-18 | Post-D72 pursuit/initiative report: four-column F5 evidence; C/I/L collapse but E/F survive, D Company cohesion-destroys after pursuit break-off; third binding STOP with ford, courier, resupply, and pursuit-termination trails. |
| `codex-report-m4a-d73.md` | 07-18 | Post-D73 tactical-complex report: five-column F5 evidence; D reintegrates/holds and complex convergence nearly destroys E/F, but E/F/I escape into Reno's steady mass; fourth binding STOP. Scout distortion remains 37 vs 3. |
| `CODEX WORKORDER-M4B.md` | 07-18 | UI-only work order for the M4-A fight surface: live strength/morale, encounters, fall and terminal markers, engagement speed cap, combat scrubber ticks, rout/reintegration cues, scale ruler, and live losses panel. |
| `docs/screenshots/` (m4b-*) | 07-18 | M4-B visual proof: Reno retreat crossing with fall markers, Keogh collapse with morale and pursuit tooltip, full-map end-of-day fall-marker landscape, and the end-of-day losses panel beside the landscape. |
| `CODEX WORK ORDER-M4C.md` | 07-18 | UI-only D77 work order for stable viewshed darkness, held-raster temporal crossfade, and immediate OFF teardown. |
| `docs/screenshots/` (m4c-*) | 07-18 | M4-C playback evidence: Custer bluff-route viewshed at 15:35, 15:40, and 15:45 with the stable scrim and moving lit region. |
| `M5-SPEC.md`, `CODEX-WORKORDER-M5A.md` | 07-22 | Authoritative M5 D78–D83 specification and M5-A scorer/split/envelope dispatch, including Chuck's binding Q1–Q3 rulings. |
| `engine/src/score.ts`, `engine/score-cli.ts` | 07-22 | Full presentation-ready D78 C1–C4 composite scorer and `npm run score` report writer with provenance-driven counterfactual exclusions and Q3 tier tags. |
| `engine/src/baseline-selection.ts`, `engine/src/envelope.ts`, `engine/envelope-cli.ts` | 07-22 | D80 N=50 deterministic envelope extraction/reporting and mechanical typical-seed selection with G-M5-2 criteria-hash ordering evidence. |
| `data/calibration/baseline-seed-criteria.json` | 07-22 | Predeclared N=50 seed list, median-band/no-rare-event selection criteria, tie-breaks, and sourced historical-envelope references. |
| `reports/calibration-scorecard.md`, `reports/seed-envelope.md` | 07-22 | M5-A pre-calibration baseline scorecard and full seed-envelope starting line for M5-B. |
| `codex-report-m5a.md` | 07-22 | M5-A execution report: quartet verbatim, full scorecard and envelope summaries, G-M5-2 ordering evidence, ambiguities, and deviations. |

## 4. Open Items

| # | Item | Owner | Notes |
|---|---|---|---|
| O1 | Transcribe research → `scenario.json` + validation harness | Claude | ✅ Closed — committed (d6b1da2 + 3c2f1f8); D19 rulings; 37-flag ambiguity ledger carried in provenance. |
| O2 | Leader rating numbers review | Chuck | ✅ Closed — ratings approved as proposed (D15, 07-14). |
| O3 | Crow's Nest coordinate | Both | ✅ Closed — ruled per D60 at 45.4454,−107.1392 after the D24/O3-A sweep; reopen if field GPS or a specialist coordinate is >1.5 km away. |
| O4 | 1876 river channel corrections | Claude | ✅ Closed — Tier A shipped; Tier B georeferencing backlogged. |
| O5 | Re-source weak citations flagged in D16 | Claude | Open — before any publication-grade use. |
| O6 | DEM acquisition + terrain pipeline | Claude | ✅ Closed — M1-A delivered pipeline, loader, raycast, gates G1–G5 (see D20–D23, D29). |

## 5. Conversation / Session History

### Session 1 — 2026-07-14 (claude.ai)

1. **Feasibility.** Chuck asked how hard an animated, controllable historical battle
   simulation would be (contour terrain, troop movement, LOS, weapon effectiveness,
   leadership, tactics, real-battle timeline; reference: YouTube battle-map genre).
   Claude assessed: very doable; decomposed into easy parts (terrain/DEM, LOS raycast,
   tick engine, combat math) and the one hard design decision — the tension between
   "simulation" and "matches history" — resolved by the order-driven approach (D1).
   Scale estimate: Mah Jongg Simulator–class or somewhat larger.
2. **Battle selection.** Chuck chose Little Bighorn, motivated by Custer's
   miscalculation and wanting to "see what he saw." Claude endorsed (D2) — the battle
   is a viewshed problem (Crow's Nest, Reno's partial view, Weir Point) plus an
   expectations problem (Custer feared escape, not defeat → tactics parameter).
3. **Research method.** Chuck proposed using deep Research mode to gather archives and
   synthesize/average contradictory accounts. Claude endorsed Research but pushed back
   on averaging → weighted reconciliation with source tiers; disputes as labeled
   alternatives (D3, D5). Identified the three source tiers (archaeology / time-motion
   scholarship / testimony) and set expectations (published scholarship, not unscanned
   archive boxes).
4. **Deliverables.** Claude authored `scenario-schema.ts` and the deep research prompt
   (D3–D10). Checked the codex-first skill: Claude Code sessions only, and this is
   design work — authored directly.
5. **Research returned.** Chuck ran Research and uploaded the full A–K dataset.
   Claude mapped it to the schema, posed five gating decisions, and flagged O3/O4.
6. **Decisions approved.** Chuck approved D11–D14 and the D15 approach (numbers
   pending review). Requested this implementation history file and a PRD to make the
   process reproducible for future battles — both delivered to close the session.

### Session 2 — 2026-07-14/15 (claude.ai)

"O1 executed via Codex, reviewed by CC + Fable; D19 rulings; O1b burn-down 57→42 flags; D26 integer rule from review; terrain decisions D20–D25 approved; M1-A queued."

**State at session end:** repo live and source of truth; O1 + O1b committed (schema
v0.2, 42-flag ambiguity ledger); M1-A terrain pipeline complete with all five gates
green incl. G4 terrain-mask; D19–D37 logged; O3/O4 Tier A and the M2 engine
implementation (spec approved, D30–D37) are the open fronts.

M3-B: first UI shipped — map, POV viewshed, belief toggle, decision index.

### Session 3 — 2026-07-22 (Claude Code)

M5 calibration arc. **The honest-instrument moment, logged per Chuck:** D84's
repacing of Reno's approach made the day more historically correct and dropped
C2 from 88.9% to 77.8% (US killed fell below the low band as the timeline
shifted) — the scorecard punished increased correctness until the rest of the
model catches up. An instrument that behaves this way is the only kind worth
calibrating against. Round-0 and round-2 STOPs held under the D79 constitution
(Ford A data class; valley-intensity mechanism class); rulings D84–D87 issued.

**Ford A closed — the method's proof-of-work:** the project's oldest number,
carried since the first E5 table (−93 minutes, movement-only, M2), closed at
**5.1 m / +0.5 min** by sourced repacing (D84) plus one honestly-flagged
reconstruction halt (D86; 70 simulated minutes vs the ruled ~60 — delivery-
lattice quantization, accepted as documented implementation truth). Session
ended at the round-3/4 mechanism STOP: composite 60.41%, two hypotheses
pre-framed for the next session (H1 terrain starvation → O4 Tier B;
H2 relent-timing → D73 coupling), H1 probed first and gating H2.
