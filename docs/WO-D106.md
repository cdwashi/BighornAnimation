# WO-D106 — Frozen Work Order: camp-defence command ownership, the plain gate

**Status: frozen before dispatch.** Written 2026-07-30 against ruling D106 (`docs/IMPLEMENTATION_HISTORY.md`) and the tier-A predictions PR-39–PR-44 with their re-armed stop (`docs/PREDICTIONS.md`, commit `bcb6ad6`, committed alone before this file existed). Written to match those predictions; predictions are never amended to match this file.

**Self-contained. Assume zero prior context.**

## Context

The sixteenth measurement established that D96's ruled closing mechanism fired 330 times and was overwritten by D72 `startPursuit` 330 times in the same tick — a held camp-defence commitment is not a commitment while a competing writer can seize its holder every half-minute. D106 rules one authority for commitment holders on the D92 hysteresis precedent. The thirty-third measurement previewed this exact gate over seeds 18760600–18760633 (dist-toggle form) on this exact tree; those 34 seeds are an implementation audit, and the campaign's evidentiary content lives in the sixteen never-run seeds.

**The re-armed stop is live this round** (`docs/PREDICTIONS.md`): halt for adjudication if Reno A/G/M killed exceeds 60 in more than 5/50 registered seeds, or any seed reaches killed ≥ 100. Never tune in response to results, in either direction.

## Scope — one gate, one round

**`engine/src/morale.ts`, `startPursuit`:** a unit with an active camp-defence commitment (`pursuer.campDefense` set) is not eligible to begin a pursuit of either kind. The function returns false for such a unit before any other work. Nothing else in `startPursuit`, `endPursuit`, pursuit maintenance, or camp-defence changes. No new number, field, config, or `[CAL]`.

**Tests, named:** `gate-blocks-combat-pursuit-on-holder`, `gate-blocks-initiative-on-holder`, `gate-inert-for-ordered-units` (a unit with an active order and no commitment pursues exactly as before — the declined generalized scope must not creep in), `release-restores-eligibility` (a released unit pursues normally).

## Expected behavioral consequences — registered, do not "fix"

- The 34 previewed seeds should reproduce the thirty-third's world near-digit-exactly (Reno median 34-class, coalition 63-class, hill mode initiative-dominant). Divergence from the preview is reportable, not repairable.
- **Wing completion is expected LOW (~4–5/34-class rate) and that is the known, separately-owned deficit** (ammunition-starved bout loop, measurements 34–35). Do not attempt to move it.
- Combat behavioral oracle pins are stale across two accepted-but-stopped rounds (D104, D105 — neither ran its refresh; both stops preceded it). Full-suite stale failures are expected; document each refresh with cause. The F4 roster is a historical criterion: never edit it in any direction — the D105 baseline preserves five-destroyed, so F4 is expected green under refreshed pins. No-combat oracles must not move; movement there is an AMBIGUITY to report.

## Out of scope — do not touch

Any `[CAL]` value; any scenario byte (STOP; stream `ba288f09`, PR-44); the bout, its latch, or its outcomes; the drain; fire economy, ammunition, resupply; rout/rally semantics; ordered units' pursuit eligibility (the declined ORD scope); D93/D96/D98/D99/D102/D103/D104/D105 semantics beyond the single gate; the F4 roster; `docs/PREDICTIONS.md`; prior codex reports.

## Predictions — PR-39 through PR-44 and the re-armed stop

Governing text at commit `bcb6ad6`; binding; N=50, seeds 18760600–18760649; verdicts as they fall. PR-40 (the hill mode decomposition) is the primary instrument; PR-43 is an audit leg (no commitment holder ever carries a pursuit — failure is an implementation error, not a miss). Registered observations: wing completion expected-low-unchanged; reopen-clause instrumentation (D92 switching events per pool band; any sustained commitment-while-nearer-eligible-threat-unengaged window — the starvation signature); bout counts and full hill/wing mode tables for the bundle's baseline.

## Proof required

Quartet verbatim (refreshes documented); the four named D106 tests passing; PR-39–PR-44 verdicts with distributions (killed distribution and median; hill/wing mode decomposition tables; east-survival counts; coalition killed median; the per-tick commitment-holder pursuit audit; switching-event and starvation-window data; per-seed comparison of the 34 previewed seeds against the thirty-third's preserved figures); before/after composite and envelope; `[CAL]` and protected-content byte-identity; stop status stated explicitly.

Output: `codex-report-wo-d106.md` at repo root, house format, AMBIGUITIES and DEVIATIONS included. **Do not commit or push.**
