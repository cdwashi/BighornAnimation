# WO-D127 — RUN-253 (DRAFT FOR ADJUDICATION 2026-08-05; number provisional until ruled; frozen on Chuck's word after Fable's attack)

*The D122 fix WO, second of the five-WO arc: the engine's us-7th killed-best
numerator moves from the assembled 268/52 to the scenario-consistent 253/52 —
value, pin literal, and exception comment TOGETHER, per D122's bound. Codex
implements on a fresh exec; CC verifies; fix-nothing binds. THE LEDGER SEARCH RAN
FIRST: this WO's prior rows are D122 (the ruling and its registered prediction),
D110 (pin (a)(3), the declared exception built so this argument would happen in
the open), WO-D111 Amendment 2 (the payload-pin class licensing the pin update),
and the M-FLIP artifacts (`bbd09a1`) — the measured counterfactual this WO must
reproduce.*

## §1 — The payload: three edits, one datum

- **T1 — the value** (`engine/src/combat-config.ts`, the us-7th range):
  `best: 268 / 52` → `best: 253 / 52`.
- **T2 — the pin literal** (`engine/tests/d110-pins.test.ts:167`):
  `expect(usRange.best).toBe(268 / 52)` → `toBe(253 / 52)` — the SIMULTANEOUS
  update of the D110 PR-65 pattern; D122 is the ruling that licenses it under
  WO-D111 Amendment 2's class (a payload pin updates only when a ruling changes
  the datum).
- **T3 — the comment, REWRITTEN, never deleted** (the range's `provenance`
  string and the pin test's exception language): the entry stops being a
  DECLARED EXCEPTION and becomes a SOURCED VALUE. Required content, wording
  free: 253 is the per-company killed-best sum — the population the engine's
  casualties come from (99.9%, measured at D122); the former 268/52 mixed
  counting frames (a monument-plus-evacuees numerator over an
  officers-and-troopers denominator; Scott pairs 268 with 55); changed at
  D122/D127 on M-FLIP's measurement. **If the comment is deleted rather than
  rewritten, the register loses the reason the number changed, and the next
  reader finds 253 with no history — that deletion is a red, not a style
  choice.**

- **T4 — the m4a behavioral-oracle re-pin, SIMULTANEOUS like the pin (added at
  the dry-run's finding):** `engine/tests/m4a-gates.test.ts:54` and `:145` —
  the combat-only behavioral oracle `hashState(...) === 'a114bb7b'` fires
  under the payload because the full-state hash reads the killed/wounded
  ledger columns the ratio shift moves. **The oracle is doing its designed
  job** (its own comment: it fires on engine behavioral change without
  scenario or PRNG change, and refreshes intentionally when a ruling lands).
  Re-pin BOTH sites to the dry-run-measured post-payload value
  **`a72fd7ef`**, and update the two cause comments (`:52-53`, `:143-144`)
  to name BOTH D-numbers (ruled at adjudication): **D122 as the ruling that
  changed the value, D127 as the WO that measured the new hash** — replacing
  the D112 text with only D127 would lose the ruling that authorised the
  change. If Codex's build produces a hash OTHER than `a72fd7ef`, that is a
  red and a finding (build nondeterminism or payload difference), not a value
  to adopt.

No other byte: no scenario bytes (hash `68325eff` byte-identical, checked in
the report), no other engine logic, no schema, no reseed.

## §2 — Acceptance: divergence-from-M-FLIP, stated operationally

**The patched world must reproduce `bbd09a1`'s MEASURED world exactly — the
counterfactual was measured before the change was authorised, and any
divergence is a defect in the patch, adjudicated as such, never as a surprise
about the world.** Operationally, against the committed artifacts:

- **P1 (trajectory):** annihilation bout census matches the committed 120
  (`reports/d112-campaign-results.json` `annihilations`) ROW FOR ROW;
  application counts identical per seed.
- **P2 (envelope):** composite AND every component equal the committed rows at
  full precision on 50/50 seeds (M-FLIP measured exact composite equality;
  C1/C3/C4 cannot move; a C2 leg flip moves the composite 1.667 pp).
- **P3 (the ledger shift, per-seed against the measured counterfactual):**
  per-seed US fire-killed/fire-wounded and final-killed/final-wounded equal
  `.claude/us268-m-flip.json` `perSeed` EXACTLY on every seed — entailing the
  measured pooled deltas (fire −78/+78, final −48/+48) as a consequence, not a
  target.
- **P4 (the pin):** post-payload the d110-pins suite is GREEN with the updated
  literal — the pin fires by construction against the old value and this WO is
  its licensed simultaneous update; the report shows the pin file's before and
  after.
- **P5 (the oracle):** post-payload both m4a oracle sites are GREEN at the
  re-pinned `a72fd7ef` — the dry-run-measured value, frozen here so the WO's
  acceptance tests a measurement, not an adoption.

## §3 — Pre-freeze red enumeration (TWO SURFACES, per D126's standing amendment — this WO is the rule's first execution and a test of it)

**Dry-run EXECUTED at drafting** (value + pin literals verbatim — the payload's
entire test surface at that point; comment rewrites carry none — full suite,
corrected capture preserving the complete output tail so both surfaces are
visible): `.claude/wo-d127-dryrun.out.txt`. **RESULT — THE RULE'S FIRST
EXECUTION RETURNED A RED NOBODY NAMED, exactly the branch the adjudication
pre-framed as a finding:** the m4a behavioral oracle fired at both sites
(expected `a114bb7b`, observed `a72fd7ef`), because the full-state hash reads
the ledger columns the ratio moves — **a surface M-FLIP could not see, because
measurement phases run probes and WOs run suites, and the oracle class lives
only in suites.** The finding was absorbed as §1's T4 (the licensed
simultaneous re-pin) and §2's P5 BEFORE the freeze — the two-surface full-suite
dry-run doing precisely what D126's amendment built it to do.

- **ASSERTION SURFACE, enumerated post-dry-run:** `repository-text-integrity`
  ENOBUFS — EXPECTED-PRE-EXISTING, gate-repair WO; the m4a oracle ×2 —
  PAYLOAD-CAUSED AND LICENSED, green post-payload via T4's re-pin; `d110-pins`
  GREEN under the simultaneous update (VERIFIED at the dry-run); all other
  assertions green (117/120 observed, the three above accounting for every
  red).
- **PROCESS SURFACE, enumerated:** the `vitest-worker` RPC timeout DID NOT
  fire this run — now 2-of-3 across today's full-suite runs: **INTERMITTENT
  at current scale, not deterministic** (a refinement the harness-repair WO
  inherits). When it fires on the payload run it is EXPECTED-PRE-EXISTING;
  its absence is not evidence of repair.
- **The exit criterion, stated independently of the pre-existing reds:** the
  payload's acceptance is §2's P1–P5 plus a suite whose only reds on either
  surface are ENOBUFS and (if it fires) the RPC timeout. **Any OTHER red on
  either surface is a FINDING and a STOP.**

## §4 — Scope discipline and report

Exactly the three edits; Codex declares the final comment wording in the
report (the register reviews the history it preserves); quartet run with BOTH
surfaces reported verbatim; the campaign re-run for P1–P3; per-prediction
verdicts with evidence; scenario hash check; file SHAs; no commit, no push.
