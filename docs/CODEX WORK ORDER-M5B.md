# CODEX WORK ORDER — M5-B: The calibration pass (under the D79 constitution)

## Goal
Tune the accumulated [CAL] ledger to pass G-M5-1/2/4/5 (UI = M5-C). The
constitution (M5-SPEC §2) is binding law: sourced ranges only, global tables
only, no mechanism changes, no data edits (suspension rule), full audit.

## Starting line
reports/calibration-scorecard.md + seed-envelope.md at the M5-A commit:
composite 59.68% (C1 40%/HIGH 25%, C2 88.9%, C3 38.5%, C4 92.3%),
envelope median 56.91%, wing destroyed 40/50, baseline seed 18760602.

## Round protocol (repeat until gates or STOP)
1. Diagnose the largest miss from per-item cards + mechanism trails.
2. Move [CAL]s globally within sourced ranges (cite range + source per move).
3. Re-run FULL scorecard + N=50 envelope; report all four components —
   a round that trades C2 down to buy C1 is a net-loss round (Fable rider).
4. Append the round to the audit trail artifact (before/after, margins).

## Pre-registered (Fable riders, binding)
- 10/50 incomplete-destruction: expected to shrink as a side effect; if it
  persists large post-calibration → D80 adjudication to Fable.
- C1 misses tracing to PRE-CONTACT approach legs (Ford A −93 class) are NOT
  tunable: STOP, report as data-semantics finding (missing historical halts
  = D39-class refinement), calibration suspends per D79.5 for a ruling.
- D82 predictions adjudicated as they fall: (a) A/G/M reach BROKEN on the
  timber→river leg in a meaningful seed fraction; (b) ford-choke dead shift
  to broken troopers; (c) Bloody Knife death appears in the envelope.
  If sourced ranges cannot produce the rout → STOP (mechanism finding).

## Also in scope (G-M5-5 debts)
- Spotting refit against the 13-event exam (Crow's Nest rows promoted),
  replacing the 11-event overfit; global table only; report margins.
- F6 gate ported to work-metric primary (node-expansion + allocation
  ceilings), wall-clock informational.
- Arikara digit (0 vs 3) via D75 withdrawal thresholds within range.

## Exit
G-M5-1 (baseline seed passes composite gates), G-M5-2 (envelope re-run,
history inside, selection re-executed mechanically), G-M5-4, G-M5-5;
quartet; all prior gates green. Every [CAL]'s final value + sourced range
in the audit artifact.

## Proof
codex-report-m5b.md: per-round audit, final scorecard + envelope in full,
D82 verdicts, STOPs if any. No commit/push — Fable reads the trails.