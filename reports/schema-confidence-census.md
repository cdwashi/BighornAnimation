# Q-COST — the census (schema confidence item, registration `41f2e6b`; committed artifacts and ledger only, zero probes)

## The primary leg: the gate-exclusion hunt

**The exhaustive DISPUTED roster (tier map, all rows with DISPUTED in a tier
column): FIVE.** Classified per surface, checked not inferred:

| row | claim | surface | gate status |
|---|---|---|---|
| #133 | `obs-custer-weir-village` | observation (gated: `exam.ts:50-54` excludes below MEDIUM) | **THE ONE REALIZED INSTANCE** — pre-upgrade, its DISPUTED label gate-excluded it from the gateable set; the C4 identity (D112's nineteenth catch) priced that exclusion at exactly (12/13 − 12/14) × 0.15 = **0.989 pp on every composite**; corrected at the D112 freeze by the RCOI tier ruling (a cross-axis crossing: a quality promotion for a claim whose real property was contestation) |
| #137 | `cp-v-mtc-ef-ford-b` (variant patch) | variant — UNSCORED surface (counterfactual toggle; the baseline never scores it) | no gate to reach |
| #138 | `v-mtc-crossing` (variant) | variant — UNSCORED surface | no gate to reach |
| #166 | `sideCasualties…killed` | side target | **SCORED DESPITE DISPUTED — checked from the committed artifact**: the coalition killed leg is present and scored in 50/50 committed seeds (`us268-m-flip.json` casualtyLegs; sample: passed=true, actual 55, expected 36–136). D112's measurement holds: side-target exclusion rides the NOTE flag (pin (d)'s ruled three), not confidence |
| #167 | `sideCasualties…wounded` | side target | **SCORED DESPITE DISPUTED** — present in 50/50 (sample: passed=false, actual 150 vs =160; the C2 ceiling leg, failing on the band, never excluded) |

**The hunt's answer, stated in the two halves the ruling needs kept apart:**

- **CURRENTLY: EMPTY.** No claim in the committed world is DISPUTED-gate-excluded
  with a scored consequence — the variants are unscored, the side targets are
  scored despite the label, and #133 was corrected before the committed
  `68325eff` world was measured. The committed figures carry ZERO uncorrected
  DISPUTED-exclusion distortion.
- **HISTORICALLY: ONE.** #133 pre-upgrade is a realized instance of the defect's
  exact form — a contested observation gate-excluded as if weakly sourced, its
  exclusion carrying 0.989 pp in every composite — found by the
  double-denominator instrument, priced by the C4 identity, and corrected by a
  ruling that had to reason ACROSS the incommensurable axes (RCOI adjudicating
  the dispute to justify a quality rung).

## The secondary leg: the prose-collision count

**FIVE instances,** all committed prose: (1) D112's three-sentence tier ruling on
the wounded band; (2) the referent clause ("the tier rates the SOURCE, not the
band"); (3) the non-tension clause ("absence of dispute through absence of
alternatives is not agreement"); (4) #133's RCOI cross-axis reasoning; (5) the
DISPUTED-spreads-preserved clauses (D71/D81 era). No collision has recurred once
its clause existed.

## The evidence state for the ruling

The adjudicator pre-committed: empty confirms the lean on measurement; non-empty
changes the cost calculus entirely. **The census returns both, and the split is
the finding: non-empty historically (one instance, corrected, priced), empty
currently (zero uncorrected distortion in any committed figure).** Whether a
singular, corrected, instrument-caught historical instance re-prices the split
outcome — against a fix costing scenario bytes plus scoring-source changes on the
costliest shape on the board — is the adjudication's call. The census notes only
this: the one realized instance was caught and corrected by the register's
EXISTING machinery (the double-denominator report, ordered for a different
reason), which is an argument that cuts both ways — the defect is real enough to
have fired once, and the register's instruments are sharp enough to have caught
it when it did. Verdict lines are the adjudication's.
